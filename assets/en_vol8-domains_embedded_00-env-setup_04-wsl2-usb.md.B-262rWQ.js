import{_ as s,o as i,c as a,a2 as n}from"./chunks/framework.BuBU3QrY.js";const u=JSON.parse('{"title":"Environment Setup (Part 4): WSL2 USB Passthrough, Letting ST-Link Cross the Virtualization Boundary","description":"","frontmatter":{"title":"Environment Setup (Part 4): WSL2 USB Passthrough, Letting ST-Link Cross the Virtualization Boundary","description":"","tags":["beginner","cpp-modern","stm32f1"],"difficulty":"beginner","platform":"stm32f1","chapter":14,"order":4},"headers":[],"relativePath":"en/vol8-domains/embedded/00-env-setup/04-wsl2-usb.md","filePath":"en/vol8-domains/embedded/00-env-setup/04-wsl2-usb.md","lastUpdated":null}'),t={name:"en/vol8-domains/embedded/00-env-setup/04-wsl2-usb.md"};function o(l,e,r,h,p,d){return i(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="environment-setup-part-4-wsl2-usb-passthrough-making-st-link-cross-the-virtualization-boundary" tabindex="-1">Environment Setup (Part 4): WSL2 USB Passthrough, Making ST-Link Cross the Virtualization Boundary <a class="header-anchor" href="#environment-setup-part-4-wsl2-usb-passthrough-making-st-link-cross-the-virtualization-boundary" aria-label="Permalink to &quot;Environment Setup (Part 4): WSL2 USB Passthrough, Making ST-Link Cross the Virtualization Boundary&quot;">​</a></h1><h2 id="preface-the-biggest-pitfall-on-this-journey" tabindex="-1">Preface: The Biggest Pitfall on This Journey <a class="header-anchor" href="#preface-the-biggest-pitfall-on-this-journey" aria-label="Permalink to &quot;Preface: The Biggest Pitfall on This Journey&quot;">​</a></h2><p>If you have been following the previous tutorials, your WSL2 environment now has the ARM toolchain, OpenOCD, and perhaps even your first compiled firmware file. When you eagerly plug in the ST-Link debug probe, ready to flash the program to the STM32, reality hits you hard—WSL2 cannot see USB devices at all.</p><p>I am currently going through this phase myself, where the <code>lsusb</code> output is completely empty. Let alone an ST-Link, you cannot even see a mouse. This is not a user error; it is an inherent limitation of the WSL2 architecture. WSL2 uses Hyper-V virtualization technology, and Linux runs as a true virtual machine under Windows, but Microsoft did not implement USB device passthrough. Your ST-Link is plugged into a Windows USB port, claimed by Windows drivers, and the Linux side is completely unaware of its existence.</p><p>This problem plagued me for days. I searched online for all sorts of resources; some recommended using a virtual machine approach, while others suggested giving up on WSL2 entirely and installing native Ubuntu. But I did not want to give up, because the rest of WSL2 is simply too convenient—its integration with the Windows file system, terminal experience, and package management are all hard to match with native Linux. Eventually, I found the usbipd-win project, a tool officially maintained by Microsoft specifically designed to solve the WSL2 USB passthrough problem.</p><p>Today, we will fill this pitfall once and for all, allowing the ST-Link to smoothly cross from Windows into WSL2, and then complete your first OpenOCD flash.</p><h2 id="the-wsl2-usb-problem-what-is-actually-happening" tabindex="-1">The WSL2 USB Problem: What Is Actually Happening <a class="header-anchor" href="#the-wsl2-usb-problem-what-is-actually-happening" aria-label="Permalink to &quot;The WSL2 USB Problem: What Is Actually Happening&quot;">​</a></h2><p>Let us first understand the root of the problem. Although WSL2 feels like a Linux program inside Windows, it is actually a complete virtual machine. When you open a WSL2 terminal, you are interacting with a Hyper-V virtual machine named &quot;WSL&quot;. This virtual machine has its own kernel, its own memory management, and its own device tree.</p><p>In the PC architecture, USB devices are managed by host controllers. Your motherboard has several USB controllers, with multiple USB ports under each controller. When a USB device is plugged in, the controller assigns it an address, and the operating system loads the corresponding driver to communicate with the device. The problem is that inside the WSL2 virtual machine, the USB controller is virtualized and cannot connect to the physical USB controllers, making physically plugged-in devices invisible to WSL2.</p><p>The Windows host can see your ST-Link, and Device Manager recognizes it normally, but the WSL2 Linux kernel cannot see it. This is why we need a passthrough mechanism to &quot;lend&quot; the USB devices seen by Windows to WSL2. usbipd-win does exactly this; it implements the USB/IP protocol, which allows USB devices to be transmitted from one machine to another over the network protocol stack. In the WSL2 scenario, this means transmitting from Windows to the &quot;virtual machine&quot; that is WSL2.</p><p>Now let us start configuring.</p><h2 id="windows-side-installing-and-configuring-usbipd-win" tabindex="-1">Windows Side: Installing and Configuring usbipd-win <a class="header-anchor" href="#windows-side-installing-and-configuring-usbipd-win" aria-label="Permalink to &quot;Windows Side: Installing and Configuring usbipd-win&quot;">​</a></h2><p>First, make sure you are using WSL2 and not WSL1. WSL1 is a translation layer that directly uses the Windows kernel, so the USB problem does not exist in WSL1 at all—but WSL1 has many other limitations, such as lack of Docker support, which is why most people use WSL2 now. You can verify this in PowerShell with <code>wsl --status</code>, and if your version is 1.x, you need to upgrade to 2.</p><p>Next, we install usbipd-win. This tool is available on Microsoft&#39;s official package manager winget, making installation very simple. Open a <strong>PowerShell terminal with administrator privileges</strong>—note that administrator privileges are mandatory because USB device operations require elevated rights. Run:</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">winget install usbipd</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>After installation, you should be able to use the <code>usbipd</code> command. Now let us check which USB devices are on the system:</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">usbipd list</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command will list all USB devices, and you will see a long list including your mouse, keyboard, webcam, and so on. Each device has a BUSID, in a format like &quot;1-5&quot; or &quot;2-3&quot;. Your ST-Link should also be in the list, likely shown as &quot;STMicroelectronics ST-LINK...&quot; or a similar name. Remember its BUSID; for example, mine shows as &quot;1-8&quot;.</p><p>Next, you need to bind this device to usbipd-win. Binding is a one-time operation that tells Windows this device can be passthrough in the future. After binding, the device will disappear from Windows Device Manager, its driver will be unloaded, and usbipd-win will take over. Run the bind command:</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">usbipd bind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">busid </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Replace <code>BUSID</code> with the actual BUSID you see. If successful, you will see a confirmation message. The device has now disappeared from Windows&#39; view; you can verify this in Device Manager, where the ST-Link entry should be gone.</p><p>But WSL2 still cannot see the device at this point, because binding is just preparation. You also need to &quot;attach&quot; the device to WSL2. This attach operation must be done every time you restart WSL2 or re-plug the device. Let us run:</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">usbipd attach </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">wsl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">busid </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command transmits the device to WSL2 via the USB/IP protocol. The <code>--wsl</code> parameter specifies that the target is our default WSL distribution. The device should now appear in WSL2.</p><p>The difference between bind and attach is important: bind is a one-time operation that tells Windows &quot;this device can be passthrough in the future,&quot; while attach must be done each time, equivalent to &quot;I am now connecting this device to WSL2.&quot; After you restart your computer, the bind state is preserved, but the attach is lost and needs to be re-executed.</p><h2 id="linux-side-verifying-device-passthrough" tabindex="-1">Linux Side: Verifying Device Passthrough <a class="header-anchor" href="#linux-side-verifying-device-passthrough" aria-label="Permalink to &quot;Linux Side: Verifying Device Passthrough&quot;">​</a></h2><p>Now go back to your WSL2 terminal. You can use the <code>lsusb</code> command to view the USB device list:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lsusb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stlink</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>If all goes well, you should see output similar to this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Bus 001 Device 005: ID 0483:3748 STMicroelectronics ST-LINK/V2</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Or it might be <code>374b</code>, depending on your ST-Link version. The V2 version is 3748, and V2-1 is 374b, but this makes little difference to OpenOCD, as it supports both.</p><p>The device number information in this line of output is important: <code>Device 005</code> means this device is at <code>/dev/bus/usb/BUS/005</code>. This device node file is the interface we will use later to access the ST-Link.</p><p>Now we need to let WSL2 access this device. On a native Linux system, you would typically configure udev rules to let the system automatically set the correct permissions for USB devices. But in WSL2, udev does not work by default—WSL2 skips the udev service startup, which means udev rules never take effect. This is another pitfall of WSL2.</p><p>You can try creating a udev rules file <code>/etc/udev/rules.d/99-stlink.rules</code> with the following content:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># STM32 ST-LINK/V2</span></span>
<span class="line"><span>SUBSYSTEM==&quot;usb&quot;, ATTR{idVendor}==&quot;0483&quot;, ATTR{idProduct}==&quot;3748&quot;, MODE=&quot;0666&quot;</span></span>
<span class="line"><span># STM32 ST-LINK/V2-1</span></span>
<span class="line"><span>SUBSYSTEM==&quot;usb&quot;, ATTR{idVendor}==&quot;0483&quot;, ATTR{idProduct}==&quot;374b&quot;, MODE=&quot;0666&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Then on native Ubuntu, you would need <code>sudo udevadm control --reload-rules</code> to reload the rules. But in WSL2, these commands might not have any effect because the udev service is not running at all.</p><p>So we need another approach: manually modifying device permissions.</p><h2 id="permission-handling-in-wsl-the-infuriating-libusb-error-access" tabindex="-1">Permission Handling in WSL: The Infuriating LIBUSB_ERROR_ACCESS <a class="header-anchor" href="#permission-handling-in-wsl-the-infuriating-libusb-error-access" aria-label="Permalink to &quot;Permission Handling in WSL: The Infuriating LIBUSB_ERROR_ACCESS&quot;">​</a></h2><p>When you first try to connect to the ST-Link with OpenOCD, you will very likely encounter the <code>LIBUSB_ERROR_ACCESS</code> error. The meaning of this error is clear: OpenOCD does not have permission to access the <code>/dev/bus/usb/...</code> device file.</p><p>The solution is simple and brute-force: use sudo to modify the permissions:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> chmod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 666</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/bus/usb/001/005</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>But the problem is that every time you re-attach the USB device, the device number might change. Sometimes the ST-Link is Device 005, and the next time you restart WSL2, it might become Device 006. So typing the command manually is tedious, and we need an automation script.</p><p>I wrote a simple <code>fix-stlink.sh</code> script that automatically finds the ST-Link device node and modifies its permissions:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/bin/bash</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 自动修复 ST-Link 权限的脚本</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 用 lsusb 找到 ST-Link 设备，提取总线号和设备号，我这边是类似ST-Link，建议你自己lsusb先看看再修一下这个脚本</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BUSDEV</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lsusb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stlink</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> awk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{print &quot;/dev/bus/usb/&quot;$2&quot;/&quot;substr($4,1,3)}&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-z</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$BUSDEV</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ]; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;没有找到 ST-Link 设备，请先在 Windows 侧执行 usbipd attach&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    exit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;找到 ST-Link 设备: </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$BUSDEV</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> chmod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 666</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $BUSDEV</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;权限已设置为 666&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>The working principle of this script is: it uses <code>lsusb</code> to find the ST-Link line, then uses awk to extract the bus number (second column) and device number (the first three characters of the fourth column). The <code>substr($4, 1, 3)</code> trick is there because the device number in the lsusb output is followed by a colon, such as &quot;005:&quot;, and we only want the first three characters.</p><p>You can put this script in the <code>~/bin</code> directory, add execute permission with <code>chmod +x</code>, and run it after re-attaching the USB device each time. Or you can add it as an alias in your <code>.bashrc</code> or <code>.zshrc</code>, such as <code>alias fix-stlink=&#39;sudo ~/bin/fix-stlink.sh&#39;</code>, so that in the future you only need to type <code>fix-stlink</code>.</p><h2 id="openocd-flashing-in-action-the-moment-of-truth" tabindex="-1">OpenOCD Flashing in Action: The Moment of Truth <a class="header-anchor" href="#openocd-flashing-in-action-the-moment-of-truth" aria-label="Permalink to &quot;OpenOCD Flashing in Action: The Moment of Truth&quot;">​</a></h2><p>Now that the device is passthrough and the permissions are set, we can start actually flashing the firmware. OpenOCD&#39;s configuration file system is very flexible. You need to specify two configuration files: one is the interface configuration, describing which debug probe you are using; the other is the target configuration, describing which chip you are flashing.</p><p>For ST-Link V2 and STM32F103C8T6, the configuration files are:</p><ul><li><code>interface/stlink.cfg</code> — ST-Link debug probe interface</li><li><code>target/stm32f1x.cfg</code> — STM32F1 series chips</li></ul><p>OpenOCD will automatically search its configuration file directory, usually under <code>/usr/share/openocd/scripts</code>, so you do not need to write the full path.</p><p>The most basic manual flash command looks like this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openocd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> interface/stlink.cfg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> target/stm32f1x.cfg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;program firmware.bin verify reset exit 0x08000000&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Let me explain the parts of this command. The <code>-f</code> parameter specifies the configuration files; here we specified two. The <code>-c</code> parameter executes OpenOCD commands directly on the command line, rather than using those in a configuration file.</p><p><code>program firmware.bin</code> tells OpenOCD to flash the binary file named <code>firmware.bin</code>. <code>verify</code> automatically verifies after flashing to ensure the data was written correctly. <code>reset</code> resets the chip after flashing is complete, making it start executing the new program from the beginning. <code>shutdown</code> tells OpenOCD to exit after doing all this, rather than continuing to listen for GDB connections. Finally, <code>0x08000000</code> is the Flash start address of the STM32F103, which is the standard address for the ARM Cortex-M series.</p><p>If you need to fully erase the chip before flashing (for example, if you previously flashed a large program and now want to flash a smaller one, where residual data might remain without erasing), you can add an <code>erase</code> command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openocd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> interface/stlink.cfg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> target/stm32f1x.cfg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;flash erase_address 0x08000000 0x20000&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;program firmware.bin verify reset exit 0x08000000&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>erase</code> will erase 128KB of Flash starting from 0x08000000 (the total capacity of the STM32F103C8T6). <code>0x20000</code> is in hexadecimal, which converts to exactly 131072 bytes = 128KB in decimal.</p><p>In real projects, you will not type such a long command manually every time. Using CMake&#39;s flash target is more convenient:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flash</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This will find the generated firmware file in the <code>build</code> directory and automatically invoke OpenOCD to flash it. The prerequisite is that you have configured the flash target in CMakeLists.txt beforehand; you can refer to the previous tutorials for details.</p><h2 id="common-error-troubleshooting-when-flashing-fails" tabindex="-1">Common Error Troubleshooting: When Flashing Fails <a class="header-anchor" href="#common-error-troubleshooting-when-flashing-fails" aria-label="Permalink to &quot;Common Error Troubleshooting: When Flashing Fails&quot;">​</a></h2><p>During this process, you may encounter various errors. Let me summarize the most common ones and their corresponding solutions.</p><p><code>LIBUSB_ERROR_ACCESS</code> is the most common one, meaning OpenOCD does not have permission to access the USB device. The solution is to re-run the <code>fix-stlink.sh</code> script, or manually <code>chmod 666</code> that device node. If you re-attached the USB device, the device number might have changed, so you need to set the permissions again.</p><p><code>Error: open failed</code> is a more generic error, usually meaning OpenOCD cannot find the USB device at all. The first step here is to confirm whether the device was successfully passthrough to WSL2; check with <code>lsusb</code>. If you cannot see the device, go back to the Windows side and re-execute <code>usbipd attach</code>. If the device is there but OpenOCD still reports an error, it might be a permission issue, so continue troubleshooting following the LIBUSB_ERROR_ACCESS flow.</p><p><code>target not found</code> usually means OpenOCD&#39;s configuration files do not match the actual hardware. For example, if you are actually using an STM32F4 series chip but the configuration file says <code>stm32f1x.cfg</code>, or if you are using a J-Link debug probe but the configuration file says <code>stlink.cfg</code>. Check whether your hardware model matches the configuration files.</p><p>Another situation is when WSL2 cannot see any USB devices at all, and the <code>lsusb</code> output is empty. This might be because usbipd-win is not working correctly, or the WSL2 kernel modules are not loaded. You can use <code>lsmod | grep usbip</code> in WSL2 to check whether the USB/IP related modules are loaded. If they are not loaded, you can try <code>sudo modprobe usbip-core</code>, but usually the WSL2 kernel configuration should already include these modules.</p><h2 id="a-brief-guide-for-native-ubuntu-users" tabindex="-1">A Brief Guide for Native Ubuntu Users <a class="header-anchor" href="#a-brief-guide-for-native-ubuntu-users" aria-label="Permalink to &quot;A Brief Guide for Native Ubuntu Users&quot;">​</a></h2><p>If you are using native Ubuntu Linux (not WSL2), congratulations, things are much simpler. You do not need usbipd-win because your Linux kernel can directly access USB devices. You only need to configure udev rules to let the system automatically set the correct permissions for the ST-Link.</p><p>Create the <code>/etc/udev/rules.d/99-stlink.rules</code> file with the following content:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># STM32 ST-LINK/V2</span></span>
<span class="line"><span>SUBSYSTEM==&quot;usb&quot;, ATTR{idVendor}==&quot;0483&quot;, ATTR{idProduct}==&quot;3748&quot;, MODE=&quot;0666&quot;, TAG+=&quot;uaccess&quot;</span></span>
<span class="line"><span># STM32 ST-LINK/V2-1</span></span>
<span class="line"><span>SUBSYSTEM==&quot;usb&quot;, ATTR{idVendor}==&quot;0483&quot;, ATTR{idProduct}==&quot;374b&quot;, MODE=&quot;0666&quot;, TAG+=&quot;uaccess&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Then reload the udev rules:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> control</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reload-rules</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> trigger</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Unplug and re-plug the ST-Link, and udev will automatically apply the new rules. After that, your regular user account can directly access the device without needing sudo or manually modifying permissions each time. The native Linux udev system works very well; this is one of its advantages over WSL2.</p><h2 id="conclusion-the-price-of-cross-platform" tabindex="-1">Conclusion: The Price of Cross-Platform <a class="header-anchor" href="#conclusion-the-price-of-cross-platform" aria-label="Permalink to &quot;Conclusion: The Price of Cross-Platform&quot;">​</a></h2><p>After wrestling with WSL2&#39;s USB passthrough, you should now be able to complete the entire STM32 development workflow in the WSL2 environment: editing code, compiling firmware, and flashing the chip, all within a unified environment. Although the usbipd-win attach operation is a bit tedious, once you write it into a small script or PowerShell function, daily use is quite convenient.</p><p>The WSL2 approach is essentially a compromise—it gives you a development experience close to native Linux on Windows, but the price is having to take some detours in certain areas. USB passthrough is just one of them; later you might also encounter issues like serial device passthrough and network configuration. But the good news is that all these pitfalls have solutions, and once configured, subsequent usage is smooth.</p><p>In the next article, we will dive into real embedded development: starting with blinking an LED, we will step by step explore STM32 peripheral programming. You will see how modern C++ makes embedded code cleaner and safer. For now, get your development environment fully set up, practice using the flashing toolchain, and we will soon start writing real code.</p>`,78)])])}const m=s(t,[["render",o]]);export{u as __pageData,m as default};
