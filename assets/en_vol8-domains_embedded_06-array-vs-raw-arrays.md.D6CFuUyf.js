import{_ as t,E as o,o as s,c as i,a2 as n,H as d,m as a,e as c}from"./chunks/framework.BuBU3QrY.js";const w=JSON.parse('{"title":"std::array vs C-Style Arrays","description":"Comparing `std::array` with traditional arrays","frontmatter":{"chapter":5,"cpp_standard":[11,14,17,20],"description":"Comparing `std::array` with traditional arrays","difficulty":"intermediate","order":6,"platform":"stm32f1","prerequisites":["Chapter 3: 内存与对象管理"],"reading_time_minutes":5,"tags":["cpp-modern","intermediate","stm32f1"],"title":"std::array vs C-Style Arrays","translation":{"source":"documents/vol8-domains/embedded/06-array-vs-raw-arrays.md","source_hash":"5ba1a0a5b190946c88d042ddd4962f04ee54aa4d95df3de90b371f337ca695ae","translated_at":"2026-05-26T12:23:23.370605+00:00","engine":"anthropic","token_count":696}},"headers":[],"relativePath":"en/vol8-domains/embedded/06-array-vs-raw-arrays.md","filePath":"en/vol8-domains/embedded/06-array-vs-raw-arrays.md","lastUpdated":null}'),l={name:"en/vol8-domains/embedded/06-array-vs-raw-arrays.md"};function h(u,e,y,m,p,f){const r=o("OnlineCompilerDemo");return s(),i("div",null,[e[0]||(e[0]=n('<h1 id="embedded-c-tutorial-—-std-array-vs-c-arrays-do-you-know-the-difference" tabindex="-1">Embedded C++ Tutorial — <code>std::array</code> vs C Arrays, Do You Know the Difference? <a class="header-anchor" href="#embedded-c-tutorial-—-std-array-vs-c-arrays-do-you-know-the-difference" aria-label="Permalink to &quot;Embedded C++ Tutorial — `std::array` vs C Arrays, Do You Know the Difference?&quot;">​</a></h1><p>When writing embedded code, we often hesitate between two approaches: <code>int buf[16];</code> and <code>std::array&lt;int, 16&gt; buf;</code>. If we care about both performance and elegance, we naturally want to know: which one is more &quot;embedded-friendly&quot;?</p><hr><h2 id="why-std-array-looks-like-a-c-array-wearing-a-coat-—-but-is-actually-smarter" tabindex="-1">Why <code>std::array</code> Looks Like &quot;a C Array Wearing a Coat&quot; — But Is Actually Smarter <a class="header-anchor" href="#why-std-array-looks-like-a-c-array-wearing-a-coat-—-but-is-actually-smarter" aria-label="Permalink to &quot;Why `std::array` Looks Like &quot;a C Array Wearing a Coat&quot; — But Is Actually Smarter&quot;">​</a></h2><p>On the surface, <code>std::array&lt;T, N&gt;</code> is simply an aggregate type containing a <code>T elems[N]</code>: the elements are contiguous in memory, and the layout has no mysterious overhead. In many scenarios, <code>std::array</code> is equivalent to a raw array in terms of performance and memory footprint. In other words, we don&#39;t pay any extra runtime cost for switching to <code>std::array</code>.</p><p>But <code>std::array</code> wraps the array in a type: it has value semantics (it can be copied and assigned), provides <code>.size()</code>, offers <code>.data()</code>, includes <code>begin()</code>/<code>end()</code>, integrates seamlessly with STL algorithms, supports <code>constexpr</code> (with modern compilers), and can be better deduced as a template parameter. Most importantly, it makes &quot;length is part of the type&quot; explicit, making it much harder to lose size information when calling interfaces.</p><p>In short: <code>std::array</code> is a &quot;safer, more modern&quot; array.</p><hr><h2 id="the-blunt-honesty-and-fatal-naivety-of-raw-c-arrays" tabindex="-1">The Blunt Honesty and Fatal Naivety of Raw C Arrays <a class="header-anchor" href="#the-blunt-honesty-and-fatal-naivety-of-raw-c-arrays" aria-label="Permalink to &quot;The Blunt Honesty and Fatal Naivety of Raw C Arrays&quot;">​</a></h2><p>The advantage of raw arrays is &quot;zero abstraction&quot; — we have complete control over memory. This is crucial in startup code, driver layers, and buffers located in specific address spaces (such as those mapped to peripheral register addresses). Raw arrays don&#39;t pose challenges regarding ABI, the linker, or alignment — as long as we know what we are doing, they are highly reliable.</p><p>However, raw arrays also bring a host of common pitfalls: they decay into pointers in function parameters (so <code>sizeof</code> yields a pointer size inside a function), cannot be directly copied or assigned (<code>b = a;</code> will fail to compile), and offer no bounds or size protection. In embedded code, these &quot;missing conveniences&quot; force us to frequently write <code>memcpy</code>, constantly double-check if <code>N</code> is correct, and make rookie mistakes like &quot;forgetting to pass the length&quot; during code reviews.</p><p>A real-world scenario: we pass a raw array to a C API for DMA, but forget to tell the caller the length. As a result, DMA writes out of bounds and overwrites our most precious variables. Raw arrays don&#39;t warn us about these low-probability, high-cost errors.</p><hr><h2 id="advantages-of-std-array-safer-more-readable-and-more-modern-c-friendly" tabindex="-1">Advantages of <code>std::array</code>: Safer, More Readable, and More Modern C++ Friendly <a class="header-anchor" href="#advantages-of-std-array-safer-more-readable-and-more-modern-c-friendly" aria-label="Permalink to &quot;Advantages of `std::array`: Safer, More Readable, and More Modern C++ Friendly&quot;">​</a></h2><p>The everyday advantages of <code>std::array</code> can be summarized as: clear semantics, friendly interfaces, and direct compatibility with algorithms. For example, <code>std::sort(a.begin(), a.end())</code> or <code>std::span(a)</code> are readily available benefits. <code>std::array</code> can be <code>=</code>, copied, or even safely returned as a function return value (without decaying), which makes code in mid-level logic more concise and less prone to memory manipulation bugs.</p><p>In an embedded context, this means test code, unit test stubs, and buffer wrappers will be much cleaner: we can write functions that return <code>std::array</code> instead of a messy pile of <code>memcpy</code>. Furthermore, when the compiler supports <code>constexpr</code>, <code>std::array</code> can construct constant tables at compile time, resulting in code that is both efficient and safe.</p><hr><h2 id="so-when-should-we-stick-with-raw-c-arrays" tabindex="-1">So When Should We Stick with Raw C Arrays? <a class="header-anchor" href="#so-when-should-we-stick-with-raw-c-arrays" aria-label="Permalink to &quot;So When Should We Stick with Raw C Arrays?&quot;">​</a></h2><p><code>std::array</code> is great, but it&#39;s not invincible. In the following scenarios, raw arrays remain the more appropriate choice:</p><ol><li><strong>Initialization phases or early boot code (startup / crt0)</strong>: Before <code>main()</code>, C++ global construction rules and runtime support can be troublesome. Raw arrays are more straightforward and reliable in such code, especially when we need to absolutely guarantee that no constructors or runtime code are involved.</li><li><strong>Placing objects in specific linker sections / at fixed addresses</strong>: Things like interrupt vector tables, device-mapped buffers, and bootloader tables often require precise declaration of object location and byte order in the linker script. Raw arrays map more directly to the desired memory layout, reducing unnecessary abstraction.</li><li><strong>Strict ABI or interoperability with external C APIs where raw pointers are required</strong>: Although <code>std::array</code> has <code>.data()</code>, in scenarios that are highly particular about binary compatibility, using raw arrays is more intuitive during audits (especially in legacy codebases).</li><li><strong>Extreme resource constraints where any extra compiler-generated metadata must be avoided</strong>: Such situations are rare, but they do exist in some ultra-embedded or lowest-level kernel code.</li></ol><hr><h2 id="the-bottom-line" tabindex="-1">The Bottom Line <a class="header-anchor" href="#the-bottom-line" aria-label="Permalink to &quot;The Bottom Line&quot;">​</a></h2><p>Raw arrays are simple, reliable tools suited for the layer closest to the hardware; <code>std::array</code> is a more modern, safer container that aligns better with C++ philosophy, suited for business logic, algorithm layers, and the vast majority of embedded application code. Treat them as two different knives in our toolbox: use the survival knife (raw array) to fix chip pins, and use the precision knife (<code>std::array</code>) to write protocol parsing and buffer logic.</p><p>One final piece of advice: when we can express the array size as a template parameter of <code>std::array&lt;T, N&gt;</code>, we should use <code>std::array</code>; when we must control every single byte precisely in a linker script or the earliest boot code, we should fall back to raw arrays without hesitation. Embedded development isn&#39;t about &quot;staying pure&quot; — it&#39;s about using the right tool for the actual need. <code>std::array</code> will often result in less code and fewer bugs, but occasionally we still need to roll up our sleeves and reach into raw memory to fix the lowest levels.</p><hr><h2 id="run-online" tabindex="-1">Run Online <a class="header-anchor" href="#run-online" aria-label="Permalink to &quot;Run Online&quot;">​</a></h2><p>Run the <code>std::array</code> vs C array comparison example online to verify zero-overhead abstraction:</p>',27)),d(r,{title:"std::array vs C Array: Zero-Overhead Comparison","source-path":"code/examples/compiler_explorer/array_vs_carray_host.cpp","arm-source-path":"code/examples/compiler_explorer/array_vs_carray_arm.cpp",description:"Run online to verify sizeof consistency between std::array and C arrays, and observe the pointer decay issue.","allow-run":"","allow-x86-asm":"","allow-arm-asm":""}),e[1]||(e[1]=a("h2",{id:"code-examples",tabindex:"-1"},[c("Code Examples "),a("a",{class:"header-anchor",href:"#code-examples","aria-label":'Permalink to "Code Examples"'},"​")],-1))])}const g=t(l,[["render",h]]);export{w as __pageData,g as default};
