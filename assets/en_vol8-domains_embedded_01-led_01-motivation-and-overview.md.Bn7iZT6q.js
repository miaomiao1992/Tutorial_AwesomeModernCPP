import{_ as t,o as a,c as n,ag as i}from"./chunks/framework.Cu2CLAe3.js";const u=JSON.parse('{"title":"Part 6: Starting with Lighting Up the First LED — Why We Use Modern C++ to Write STM32","description":"","frontmatter":{"title":"Part 6: Starting with Lighting Up the First LED — Why We Use Modern C++ to Write STM32","description":"","tags":["beginner","cpp-modern","stm32f1"],"difficulty":"beginner","platform":"stm32f1","chapter":15,"order":1},"headers":[],"relativePath":"en/vol8-domains/embedded/01-led/01-motivation-and-overview.md","filePath":"en/vol8-domains/embedded/01-led/01-motivation-and-overview.md","lastUpdated":null}'),s={name:"en/vol8-domains/embedded/01-led/01-motivation-and-overview.md"};function o(r,e,l,d,h,c){return a(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="part-6-starting-with-the-first-led-—-why-we-use-modern-c-for-stm32" tabindex="-1">Part 6: Starting with the First LED — Why We Use Modern C++ for STM32 <a class="header-anchor" href="#part-6-starting-with-the-first-led-—-why-we-use-modern-c-for-stm32" aria-label="Permalink to &quot;Part 6: Starting with the First LED — Why We Use Modern C++ for STM32&quot;">​</a></h1><blockquote><p>For everyone who just finished setting up their toolchain and can&#39;t wait to make the board do something. This is where we truly start writing hardware control code. We won&#39;t rush into code just yet — let&#39;s thoroughly discuss the &quot;why&quot; first.</p></blockquote><hr><h2 id="starting-with-a-single-led" tabindex="-1">Starting with a Single LED <a class="header-anchor" href="#starting-with-a-single-led" aria-label="Permalink to &quot;Starting with a Single LED&quot;">​</a></h2><p>Every embedded developer&#39;s journey begins with the exact same thing — lighting up an LED. This is no trivial matter; it is the embedded world&#39;s &quot;Hello World,&quot; your first successful conversation with a silent chip. Whether you later use STM32 for motor control, USB communication, or running an RTOS, GPIO (General-Purpose I/O) operations remain the foundation of everything. Just as learning programming starts with <code>print(&quot;Hello World&quot;)</code>, learning embedded development starts with driving a pin high and low — an unavoidable first step.</p><p>I remember the first time I made the LED on the Blue Pill light up. That feeling is hard to describe. It&#39;s just a tiny light bulb, but you realize that your code — through compilation, linking, format conversion, and SWD protocol transmission — ultimately becomes an electrical signal that physically changes the voltage on a pin, and then the LED turns on. This experience of &quot;code becoming a physical phenomenon&quot; is something pure software development can never give you. At that moment, you feel that the weekend spent wrestling with the toolchain was totally worth it.</p><p>Speaking of the toolchain, I must admit I have mixed feelings writing this. The previous five env_setup tutorials — from installing arm-none-eabi-gcc to configuring CMake, from wrestling with WSL2 USB passthrough to getting the OpenOCD debugger working — every step was a struggle. Especially that time trying to get the ST-Link recognized under WSL2, I almost gave up entirely. But when we could finally type <code>cmake --build build</code> in the terminal, then <code>openocd -f ...</code>, and see nothing happen on the board — because we hadn&#39;t written the right code yet — that feeling was actually reassuring. The environment was fine, the toolchain worked, flashing ran successfully, and now all we needed was one line of code to actually control the hardware.</p><p>Now we have finally reached this step. No more battling the compiler in the terminal, no more hunting for typos in config files — it&#39;s time to write real code that makes a chip work for us.</p><h2 id="just-how-painful-is-traditional-embedded-development" tabindex="-1">Just How Painful Is Traditional Embedded Development? <a class="header-anchor" href="#just-how-painful-is-traditional-embedded-development" aria-label="Permalink to &quot;Just How Painful Is Traditional Embedded Development?&quot;">​</a></h2><p>But before we get to that, I want to talk about why this isn&#39;t inherently simple, and why we chose a slightly different path.</p><p>What does traditional STM32 development look like? If you&#39;ve used Keil MDK or IAR, you&#39;re surely familiar with the experience — a bloated IDE taking up several gigabytes, an editor with features stuck in the last century, code completion that basically relies on guessing, and an ugly debug interface that just annoys you. What&#39;s worse is that it locks you firmly to the Windows platform. Want to develop on Linux? Sorry, either use Wine to emulate it (and face all sorts of inexplicable crashes) or dutifully fire up a virtual machine. Moreover, Keil&#39;s compiler is closed-source, its optimization behavior is opaque, and when something goes wrong, you have no idea how it optimized your code.</p><p>Of course, these are just surface-level inconveniences. What truly made me decide to abandon traditional development methods was the bad practices that the C language has accumulated over decades in the embedded field. Look at what a typical STM32 project looks like: <code>#define</code> macros everywhere, things like <code>GPIO_PIN_5</code>, <code>GPIOA</code>, these preprocessor symbols have no types, no scope, their real values are invisible during debugging, and the compiler&#39;s type checking completely fails on them. Then there are those HAL library callback functions, passing function pointers and <code>void*</code> userdata back and forth, making type safety a joke. Add layers of conditional compilation <code>#ifdef</code>, <code>#if defined()</code>, and cross-platform adaptation turns the code into spaghetti.</p><p>The most fatal issue is code reusability. You write an LED driver for the Blue Pill, hardcoding <code>GPIOC</code> and <code>GPIO_PIN_13</code> inside. Next time you switch to an STM32F407 board where the LED is on PD12, what do you do? Copy, paste, and change the parameters? What if the project has ten pins to control? Twenty? C macros and structs can solve part of the problem, but ultimately you&#39;ll still end up buried in runtime checks and switch-case statements — neither elegant nor efficient.</p><p>This isn&#39;t bashing the C language — C is a great language, and there&#39;s a reason it has dominated the embedded field for decades. But times have changed, compilers have improved, and can&#39;t we pursue better abstractions without paying a runtime cost?</p><h2 id="why-c-23" tabindex="-1">Why C++23? <a class="header-anchor" href="#why-c-23" aria-label="Permalink to &quot;Why C++23?&quot;">​</a></h2><p>This is where modern C++ enters the picture. Note that I said &quot;modern C++,&quot; not the C-with-classes from the 90s. The features brought by the C++23 standard are exactly what embedded development has been dreaming of.</p><p>Zero-overhead abstraction is C++&#39;s core design philosophy — you don&#39;t pay for what you don&#39;t use. Templates are expanded at compile time, <code>constexpr</code> functions are evaluated at compile time, and <code>if constexpr</code> makes branch selections at compile time. These mechanisms give your code beautiful abstraction layers at the source level, but the compiled machine code is just as efficient as hand-written C. Your LED template class looks like an elegant type system, but the few <code>STR</code> and <code>LDR</code> instructions the compiler ultimately generates are exactly the same as if you had directly manipulated registers. No virtual function overhead, no RTTI overhead, no exception handling overhead — because we explicitly disabled these features at compile time.</p><p>Compile-time type safety is another killer advantage. In C, if you pass <code>13</code> to a function expecting a port number, the compiler won&#39;t make a peep, because they&#39;re both just integers. But in our C++ template system, <code>Port::C</code> is a distinct type. You encode port and pin information into the type system at compile time, so any wrong parameter is exposed during compilation, rather than you scratching your head over the code only after the LED on the board fails to light up.</p><p>The code reuse brought by templates goes without saying. Our GPIO template accepts the port and pin as non-type template parameters, which means <code>Gpio&lt;Port::A, 5&gt;</code> and <code>Gpio&lt;Port::C, 13&gt;</code> are two different classes, each with its own <code>set()</code> and <code>clear()</code> methods. The clock enable branching is done at compile time using <code>if constexpr</code> — if it&#39;s port A, enable the GPIOA clock; if port B, enable the GPIOB clock — all of this happens at compile time with zero runtime overhead. You never have to write those runtime lookup-table codes to find port addresses again.</p><p>Then there are those C++23 sweeteners: the <code>[[nodiscard]]</code> attribute makes the compiler warn you when you ignore a return value — this is crucial in embedded, your clock configuration failed and you didn&#39;t check it? The system goes off the rails. <code>enum class</code> wraps bare integers into strongly-typed enumerations, eliminating implicit conversions between different enum values. <code>constexpr</code> makes port address conversion a compile-time constant. Individually, these features seem unremarkable, but combined, they can take the safety and maintainability of embedded code up a major notch.</p><p>So we chose C++23 not to chase trends, but because it genuinely solves practical problems in embedded development. Later on, we will use plenty of code to prove this point.</p><hr><h2 id="what-you-need-to-prepare" tabindex="-1">What You Need to Prepare <a class="header-anchor" href="#what-you-need-to-prepare" aria-label="Permalink to &quot;What You Need to Prepare&quot;">​</a></h2><p>Before we officially begin, there are a few things we need to confirm are in place.</p><p>First are those five env_setup tutorials. If you&#39;ve been skipping around, I strongly recommend going back and reading parts 01 through 05 in their entirety: toolchain installation, project structure, CMake configuration, USB flashing, and debugger configuration. Every line of code in this part is built on the environment set up in those five parts. Your <code>main.cpp</code> must compile normally, CMake must build successfully, and OpenOCD must be able to flash firmware to the board. If you haven&#39;t got these sorted out, stop now and get them done — it won&#39;t take more than half an hour.</p><p>Then there&#39;s basic programming knowledge. I won&#39;t start from &quot;what is a variable,&quot; but I also won&#39;t assume you&#39;re a C++ template metaprogramming expert. You need to be familiar with basic C or C++ syntax: variable declarations, function definitions, basic concepts of structs and classes, and the purpose of <code>#include</code> header files. If you&#39;ve written code in any programming language and understand what &quot;function call&quot; and &quot;return value&quot; mean, then you have a sufficient starting point. Advanced features like templates, CRTP (Curiously Recurring Template Pattern), and <code>constexpr</code> will be gradually introduced and explained as we use them.</p><p>On the hardware side, you only need three things for this entire article: an STM32F103C8T6 Blue Pill development board, an ST-Link V2 debug probe, and a USB cable. Blue Pills can be bought for under ten yuan on Taobao, and ST-Link V2s are even cheaper, just a few yuan. Together, these three items might cost less than a cup of milk tea, but they can take you through the entire journey from lighting an LED to understanding the modern embedded development paradigm. The ST-Link connects to the Blue Pill via three wires: SWDIO, SWCLK, and GND, plus 3.3V to power the board. We covered the specific wiring in detail in the USB section of env_setup, so we won&#39;t repeat it here.</p><p>The software environment is the same set we configured in env_setup: the <code>arm-none-eabi-gcc</code> toolchain, OpenOCD, and CMake 3.22 or higher. Use whatever editor you like; VSCode with the clangd plugin gives a decent code completion experience, but it doesn&#39;t matter if you use Vim, Neovim, or even just <code>cat</code> — since we build with CMake, it&#39;s editor-agnostic.</p><blockquote><p>⚠️ If you&#39;re developing under WSL2, make sure USB/IP passthrough is properly configured and <code>lsusb</code> can see the ST-Link device. This is a prerequisite for flashing; if it&#39;s not set up, the subsequent <code>openocd</code> commands will definitely fail.</p></blockquote><hr><h2 id="the-road-ahead" tabindex="-1">The Road Ahead <a class="header-anchor" href="#the-road-ahead" aria-label="Permalink to &quot;The Road Ahead&quot;">​</a></h2><p>Now that both tools and mindset are ready, I want to map out the entire road ahead so you have a mental map. The LED control tutorial series isn&#39;t a single article, but a complete learning path from &quot;understanding hardware&quot; to &quot;mastering the API&quot; to &quot;redesigning with modern C++,&quot; totaling 13 parts. Why do we need so many parts just to light an LED? Because our goal isn&#39;t &quot;just make it light up and call it done,&quot; but to understand the principles behind every line of code and the trade-offs in every design decision.</p><p>We&#39;ll start with the hardware principles of GPIO, which is the most foundational layer. GPIO sounds like just five characters for &quot;General-Purpose I/O,&quot; but the circuit structure behind it — push-pull output, open-drain output, pull-up resistors, pull-down resistors, Schmitt triggers — each directly affects how you configure pins and choose operating modes. Without understanding these, you&#39;re just memorizing incantations when writing code, and you&#39;ll be lost when the scenario changes. We&#39;ve allocated 3 parts for hardware principles, covering everything from the GPIO internal structure block diagram to circuit analysis of the four operating modes, to the register organization of the STM32F103. Don&#39;t be afraid of hardware — these things are actually quite easy to understand when drawn out as diagrams.</p><p>Then the question arises — knowing the hardware principles of GPIO, how do we control it with software? The official ST-provided HAL library is exactly this bridge. HAL stands for Hardware Abstraction Layer (HAL), and it wraps low-level register operations into function calls like <code>HAL_GPIO_Init()</code> and <code>HAL_GPIO_WritePin()</code>. We&#39;ll use 3 tutorials to break down HAL&#39;s GPIO interface: initialization configuration, read/write operations, and clock management. This part will use C language style directly for the code, because HAL itself is a C interface, and we need to learn the &quot;fundamentalist&quot; usage first before we can talk about building better abstractions on top of it.</p><p>Then comes 1 part on traditional C language implementation. Here we&#39;ll connect the knowledge from the previous two sections: determine configuration parameters based on hardware principles, and use the HAL API to write a working LED blink program. But this C language version of the code will expose the problems we mentioned earlier — hardcoded macros, lack of type safety, and poor reusability. The purpose of this part is to let you see the pain points with your own eyes, laying the motivational groundwork for the C++ refactoring that follows.</p><p>But we&#39;re not done yet. Once we recognize the pain points, we enter the most core C++ refactoring stage, spanning 4 tutorials. The first introduces the CRTP singleton pattern and clock configuration encapsulation; the second dives deep into GPIO template design, explaining non-type template parameters, <code>if constexpr</code> branching, and the safe use of <code>constexpr</code>; the third builds an LED template on top of the GPIO template, demonstrating the practical effects of zero-overhead abstraction; the fourth compares the compiled output of the C and C++ versions, using disassembly to prove that C++ templates truly introduce no extra overhead. These 4 parts are the main event of this series, and they represent the core value of our tutorial.</p><p>After that, there&#39;s 1 part dedicated to C++23 features, systematically reviewing the modern features we use in our code: <code>[[nodiscard]]</code>, <code>enum class</code>, <code>constexpr</code>, <code>if constexpr</code>, and more. Finally, there&#39;s 1 part on pitfall exercises, compiling all the weird issues we encountered during development — forgetting to enable the clock causing pins not to work, PC13&#39;s special limitations, choosing push-pull vs. open-drain incorrectly causing wrong signals — to help you clear mines in advance.</p><p>The design logic of this entire path is very clear: understand the hardware first to configure parameters correctly, learn the API first to operate the hardware, and experience C&#39;s pain points first to understand the value of C++ refactoring. This isn&#39;t a tutorial that &quot;throws the final code at you right away,&quot; but one that walks you through the complete cognitive process from bottom to top. After completing it, you won&#39;t just know &quot;how to write it,&quot; but also &quot;why it&#39;s written this way.&quot;</p><hr><h2 id="the-board-in-your-hands" tabindex="-1">The Board in Your Hands <a class="header-anchor" href="#the-board-in-your-hands" aria-label="Permalink to &quot;The Board in Your Hands&quot;">​</a></h2><p>Before we write any code, let&#39;s take a clear look at the Blue Pill development board in front of us.</p><p>Blue Pill is the common name for the STM32F103C8T6 minimum system board, named because the board shape resembles a blue pill (although the origin of this name is a bit questionable). The STM32F103C8T6 chip it carries is a microcontroller based on the ARM Cortex-M3 core, with a maximum clock frequency of 72MHz, 64KB Flash, and 20KB RAM. In 2026, this spec looks incredibly meager — your phone easily has 12GB RAM and 256GB storage, and this chip doesn&#39;t even have enough memory for a single icon on your phone screen. But don&#39;t forget, this chip&#39;s design goal is real-time control and low power consumption, not running Android. A 72MHz Cortex-M3 is more than enough to drive motors, sample sensors, run communication protocols, and even run a lightweight RTOS.</p><p>What we care about most is the LED on the board. Blue Pill usually has an onboard LED connected to the PC13 pin, wired through a current-limiting resistor to VCC3.3V. Pay attention to this connection method — the LED&#39;s anode is connected to VCC through a resistor, and the cathode is connected to PC13. This means when PC13 outputs a low level, current flows from VCC through the resistor and LED into PC13, and the LED lights up; when PC13 outputs a high level (3.3V), the voltage difference across both ends is zero, no current flows, and the LED turns off. So this is an &quot;active-low&quot; LED, which will be reflected in the code later as <code>ActiveLow</code>. In the next part, we&#39;ll draw out the LED&#39;s circuit diagram in detail for analysis; for now, you just need to remember &quot;PC13, lights up on low.&quot;</p><blockquote><p>⚠️ The PC13 pin has some special limitations on the STM32F103 — it&#39;s connected to the RTC domain, the maximum output current is only 3mA, and the drive speed is limited. So you wouldn&#39;t use it to drive high-current loads, but lighting an onboard LED is more than enough. This limitation doesn&#39;t need special handling in our C++ template, because the LED template only needs to correctly output high and low levels, and doesn&#39;t involve high-current scenarios.</p></blockquote><p>For debugging, the ST-Link V2 communicates with the Blue Pill through the SWD (Serial Wire Debug) interface. SWD only needs two signal lines: SWDIO (data line, bidirectional) and SWCLK (clock line, host output). Add the ground line GND, and just three wires can complete all debugging and flashing operations. The Blue Pill board has a four-pin SWD interface on the right side (labeled SWDIO, SWCLK, GND, 3.3V); just connect the corresponding pins from the ST-Link. If this interface is hard to wire to, you can also use the pin headers on the left side of the board — PA13 is SWDIO, PA14 is SWCLK, and these two pins have alternate function mappings in SWD mode.</p><p>The STM32F103C8T6 has three main GPIO port groups: GPIOA, GPIOB, and GPIOC, each with 16 pins (PA0-PA15, PB0-PB15, PC0-PC15), for a total of 48 programmable GPIO pins. GPIOA and GPIOB have relatively complete functionality, and most of their pins can be freely configured as input, output, alternate function, or analog mode. PC13 through PC15 on GPIOC have the RTC domain limitations mentioned above, while PC0 through PC12 don&#39;t have these constraints. In our later exercises, the pins you&#39;ll use are basically concentrated on GPIOA and GPIOC, with GPIOB being used relatively less.</p><hr><h2 id="what-our-project-looks-like" tabindex="-1">What Our Project Looks Like <a class="header-anchor" href="#what-our-project-looks-like" aria-label="Permalink to &quot;What Our Project Looks Like&quot;">​</a></h2><p>Alright, we&#39;ve talked enough about hardware; now let&#39;s look at the software. The code for the entire LED control project is in the <code>led_blink/</code> directory, structured as follows:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>led_blink/</span></span>
<span class="line"><span>├── CMakeLists.txt</span></span>
<span class="line"><span>├── src/</span></span>
<span class="line"><span>│   ├── main.cpp</span></span>
<span class="line"><span>│   ├── hal/</span></span>
<span class="line"><span>│   │   ├── gpio.hpp</span></span>
<span class="line"><span>│   │   ├── led.hpp</span></span>
<span class="line"><span>│   │   └── clock.hpp</span></span>
<span class="line"><span>│   └── system/</span></span>
<span class="line"><span>│       ├── clock_config.cpp</span></span>
<span class="line"><span>│       ├── clock_config.hpp</span></span>
<span class="line"><span>│       ├── fault_handler.cpp</span></span>
<span class="line"><span>│       ├── fault_handler.hpp</span></span>
<span class="line"><span>│       ├── stm32f1xx_hal_conf.h</span></span>
<span class="line"><span>│       ├── systick_handler.cpp</span></span>
<span class="line"><span>│       └── cxx_init.cpp</span></span>
<span class="line"><span>└── drivers/</span></span>
<span class="line"><span>    ├── CMSIS/</span></span>
<span class="line"><span>    └── STM32F1xx_HAL_Driver/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>Let&#39;s quickly go through each file&#39;s responsibilities top-down to build an overall impression; we&#39;ll dive into each one in subsequent tutorials.</p><p><code>src/main.cpp</code> is the entry point of the entire program, currently with less than 20 lines of code. It calls <code>HAL_Init()</code> to initialize the HAL library, configures the system clock to 64MHz, then constructs an LED object and enters an infinite blink loop. It&#39;s that simple — but behind this simplicity, the template classes in the <code>src/hal/</code> directory do a lot of heavy lifting.</p><p><code>src/hal/gpio.hpp</code> is the absolute core of this series. It defines a <code>Gpio</code> class template that accepts two non-type template parameters: the port (a <code>Port</code> enum, with values being hardware base addresses like <code>GPIOA_BASE</code>, <code>GPIOC_BASE</code>) and the pin number (<code>uint8_t</code>, with values from <code>0</code> to <code>15</code>). Inside the template, the port address is converted to a <code>GPIO_TypeDef*</code> pointer, encapsulating initialization, read/write, and toggle operations. It also uses a nested class <code>ClockEnable</code> with <code>if constexpr</code> to implement compile-time clock enable branching. The entire template has no virtual functions and no dynamic memory allocation; the compiled code is identical to hand-written C calling HAL directly.</p><p><code>src/hal/led.hpp</code> builds a dedicated LED template on top of the GPIO template. It inherits from <code>Gpio&lt;Port, Pin&gt;</code> and adds an <code>ActiveLevel</code> template parameter to indicate whether the LED is active-high or active-low. The constructor automatically calls <code>init()</code> to configure push-pull output mode, and the <code>on()</code> and <code>off()</code> methods decide whether to write high or low based on the value of <code>ActiveLevel</code>. <code>toggle()</code> simply delegates to the underlying <code>Gpio::toggle()</code>. This is a textbook example of zero-overhead abstraction — the LED template provides a semantically clear interface at the source level, but after the compiler inlines it, <code>led.on()</code> is just a single <code>HAL_GPIO_WritePin()</code> call.</p><p><code>src/hal/clock.hpp</code> is a CRTP (Curiously Recurring Template Pattern) singleton base class. It gives any subclass automatic singleton semantics through template inheritance — <code>instance()</code> returns a reference to a static local variable, ensuring thread-safe lazy initialization while avoiding global variable initialization order issues. Copy and move constructors are explicitly deleted. Currently, only <code>SystemClock</code> uses this base class, but more hardware abstraction classes will inherit from it later.</p><p>The files in the <code>src/system/</code> directory are all system-level infrastructure. <code>clock_config.cpp</code> and <code>clock_config.hpp</code> encapsulate the RCC clock configuration: first using the HSI internal oscillator to multiply up to 64MHz (HSI 8MHz ÷ 2 × 16 = 64MHz), then configuring the AHB/APB1/APB2 dividers. If clock configuration fails, it calls the <code>halt()</code> function in <code>fault_handler.cpp</code> to put the system into an infinite loop — in a bare-metal environment without exception handling mechanisms, &quot;stopping&quot; is the safest error response. <code>systick_handler.cpp</code> does only one thing: provide the <code>SysTick_Handler</code> interrupt service routine (ISR) to drive HAL&#39;s timebase. <code>cxx_init.cpp</code> provides an empty <code>__libc_init_array()</code> function to satisfy the C++ runtime&#39;s linking needs — in an environment without an operating system, these initialization stub functions must be provided by us.</p><p><code>CMakeLists.txt</code> is the build configuration we broke down in detail in the env_setup series. It sets up the cross-compile toolchain, brings in HAL driver source code, configures compiler flags (<code>-Wall -Wextra</code>), disables exceptions and RTTI (<code>-fno-exceptions -fno-rtti</code>), and defines CMake custom targets for flashing and erasing. The C++23 standard is enabled here through <code>-std=c++23</code>, which is the prerequisite for the entire project to use modern C++ features.</p><p>For now, let&#39;s not look at the specific implementation, just the final result. Here is the complete code for our <code>src/main.cpp</code>:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;hal/led.hpp&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;system/clock_config.hpp&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    HAL_Init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    SystemClock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">instance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Led</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::C, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">13</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ActiveLevel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Low</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> led;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        HAL_Delay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        led.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        HAL_Delay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        led.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">off</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><p>Look closely at this code. <code>HAL_Init()</code> and <code>SystemClock::instance().init()</code> are system initialization that every STM32 project must do; there&#39;s nothing special about this part. The exciting part is the third line — <code>Led&lt;Port::C, 13, ActiveLevel::Low&gt; led;</code>. This single line accomplishes three things simultaneously: it tells the compiler we&#39;re using pin 13 of the GPIOC port, automatically calls the constructor&#39;s <code>init()</code> to configure the pin as push-pull output mode, and automatically enables the GPIOC peripheral clock. And as the caller, you only need to declare a variable with the correct type, leaving everything else to the template to handle at compile time.</p><p>The blink loop that follows is so straightforward it needs no explanation: delay 500 milliseconds, turn on, delay 500 milliseconds, turn off. The method names <code>led.on()</code> and <code>led.off()</code> are self-documenting — you can tell what the code is doing without reading any comments. Compare this with the traditional C approach of <code>HAL_GPIO_WritePin(GPIOC, GPIO_PIN_13, GPIO_PIN_RESET)</code>, and it&#39;s obvious at a glance which is easier to understand.</p><p>Of course, I&#39;m just showing the final result right now. This &quot;simplicity&quot; is built on the carefully designed templates in <code>gpio.hpp</code> and <code>led.hpp</code>. Our goal is to ensure that everyone reading this can eventually fully understand the design motivation, implementation details, and underlying C++23 features of these templates. By that time, you&#39;ll be able to design similar hardware abstraction templates yourself and extend this approach to any peripheral like UART, SPI, or I2C.</p><hr><h2 id="where-to-next" tabindex="-1">Where to Next <a class="header-anchor" href="#where-to-next" aria-label="Permalink to &quot;Where to Next&quot;">​</a></h2><p>Both hardware and software are ready, the learning path is mapped out, and we&#39;ve gone through the project structure. Starting from the next part, we&#39;re going to dive headfirst into the hardware principles of GPIO.</p><p>In the next part, we&#39;ll answer a question that seems simple but actually has quite a bit of depth: what exactly is GPIO? It&#39;s not just a wire. Inside a GPIO pin, there&#39;s an input data register, an output data register, a push-pull driver, an open-drain driver, a pull-up resistor, a pull-down resistor, a Schmitt trigger, and an alternate function selector — all of these together form a rather exquisite circuit structure. The STM32F103&#39;s GPIO supports four operating modes: general-purpose input, general-purpose output, alternate function, and analog mode. Understanding these internal structures is a prerequisite for correctly configuring and using GPIO. In the next part, we&#39;ll start with the GPIO internal structure block diagram, clearly explaining the differences between push-pull and open-drain output, the roles of pull-up and pull-down resistors, and the meaning of pin speed settings.</p><p>To do a good job, one must first sharpen their tools. Once we thoroughly understand the hardware, writing code becomes a breeze.</p>`,67)])])}const m=t(s,[["render",o]]);export{u as __pageData,m as default};
