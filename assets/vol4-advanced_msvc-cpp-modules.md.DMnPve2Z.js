import{_ as t,o,c as a,a2 as s}from"./chunks/framework.BuBU3QrY.js";const u=JSON.parse('{"title":"一文读懂 MSVC C++ Modules：原理、动机与工程实践","description":"","frontmatter":{"title":"一文读懂 MSVC C++ Modules：原理、动机与工程实践","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":11,"order":8},"headers":[],"relativePath":"vol4-advanced/msvc-cpp-modules.md","filePath":"vol4-advanced/msvc-cpp-modules.md","lastUpdated":null}'),n={name:"vol4-advanced/msvc-cpp-modules.md"};function d(r,e,i,l,c,p){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="一文读懂-msvc-c-modules-原理、动机与工程实践" tabindex="-1">一文读懂 MSVC C++ Modules：原理、动机与工程实践 <a class="header-anchor" href="#一文读懂-msvc-c-modules-原理、动机与工程实践" aria-label="Permalink to &quot;一文读懂 MSVC C++ Modules：原理、动机与工程实践&quot;">​</a></h1><p>仙人指路，如果你之前就不知道如何在MSVC上使用模块，笔者的确会很严肃的向您推介，先试试，再说。</p><ul><li><a href="https://blog.csdn.net/charlie114514191/article/details/155929743" target="_blank" rel="noreferrer">如何快速在 VS2026 上使用 C++ 模块 — 完整上手指南-CSDN博客</a></li><li><a href="https://zhuanlan.zhihu.com/p/1983806788118783552" target="_blank" rel="noreferrer">如何快速在 VS2026 上使用 C++ 模块 — 完整上手指南 - 老老老陈醋的文章 - 知乎</a></li><li><a href="https://awesome-embedded-learning-studio.github.io/Tutorial_AwesomeModernCPP/%E7%8E%AF%E5%A2%83%E9%85%8D%E7%BD%AE/%E5%A6%82%E4%BD%95%E5%BF%AB%E9%80%9F%E5%9C%A8VS2026%E4%B8%8A%E4%BD%BF%E7%94%A8C%2B%2B%E6%A8%A1%E5%9D%97%E2%80%94%E5%AE%8C%E6%95%B4%E4%B8%8A%E6%89%8B%E6%8C%87%E5%8D%97/" target="_blank" rel="noreferrer">如何快速在 VS2026 上使用 C++ 模块 — 完整上手指南 - Tutorial_AwesomeModernCPP的文档</a></li></ul><hr><h2 id="我们为什么需要-modules-——从-include-的本质缺陷说起" tabindex="-1">我们为什么需要 Modules？——从 <code>#include</code> 的本质缺陷说起 <a class="header-anchor" href="#我们为什么需要-modules-——从-include-的本质缺陷说起" aria-label="Permalink to &quot;我们为什么需要 Modules？——从 `#include` 的本质缺陷说起&quot;">​</a></h2><p>在很长一段时间里，C++ 的&quot;模块系统&quot;其实只有一个：</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;foo.h&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>我相信大家都是知道<code>#include</code>的原理的，不必我说，就是纯纯的文本替换而已。这种基于<code>#include</code>的依赖引入有的时候更加像是被发现而不是因此而设计的（大家都是知道C语言历史的）</p><p>编译器看到 <code>#include &lt;vector&gt;</code> 时，<strong>不会认为你在&quot;依赖一个库&quot;</strong>，而是：把 <code>&lt;vector&gt;</code> 头文件的内容，<strong>原封不动地拷贝进当前 <code>.cpp</code></strong>，再继续编译。</p><p>这听起来好像没啥，但是这些问题，我相信大家多少干工程的会有所体会的：</p><h4 id="问题一-编译速度灾难-指数级放大" tabindex="-1">问题一：编译速度灾难（指数级放大） <a class="header-anchor" href="#问题一-编译速度灾难-指数级放大" aria-label="Permalink to &quot;问题一：编译速度灾难（指数级放大）&quot;">​</a></h4><p>头文件机制的核心问题在于<strong>重复解析</strong>。每个 <code>.cpp</code> 文件都需要重新解析它 <code>#include</code> 进来的所有头文件，如 <code>&lt;vector&gt;</code>、<code>&lt;string&gt;</code>、<code>&lt;iostream&gt;</code>。当遇到<strong>模板、宏和条件编译</strong>时，这种重复工作就变成了性能地狱，导致编译时间呈指数级增长。</p><p><strong>预编译头文件（PCH）</strong> 只是将解析结果<strong>缓存</strong>起来，而非从根本上解决重复解析的<strong>结构性缺陷</strong>。本质上，这是因为<strong>编译器不知道哪些声明是&quot;已经处理过的模块接口&quot;</strong>，只能盲目地一遍又一遍地处理。</p><h4 id="问题二-宏污染是不可控的" tabindex="-1">问题二：宏污染是不可控的 <a class="header-anchor" href="#问题二-宏污染是不可控的" aria-label="Permalink to &quot;问题二：宏污染是不可控的&quot;">​</a></h4><p><strong>宏（Macro）是无作用域的</strong>，这是导致宏污染不可控的根本原因。一旦定义了 <code>#define min(a,b) ...</code> 这样的宏，并且通过 <code>#include</code> 引入，它就会<strong>永久污染后续代码</strong>，直到文件结束或被 <code>#undef</code>。（这就是为什么你会看到一些工程会习惯性的#undef下定义的宏，你也不想定义的宏被哪个不知道什么人写的包含顺序出问题了搞炸了吧！例如，引入 <code>&lt;windows.h&gt;</code> 这样的库可能会引入大量宏，这些宏可能意外地替换掉你代码中的同名函数或变量。编译器<strong>无法阻止，也无法隔离</strong>这种全局性的宏污染。</p><h4 id="问题三-接口与实现强耦合-传染式依赖" tabindex="-1">问题三：接口与实现强耦合（传染式依赖） <a class="header-anchor" href="#问题三-接口与实现强耦合-传染式依赖" aria-label="Permalink to &quot;问题三：接口与实现强耦合（传染式依赖）&quot;">​</a></h4><p>头文件机制强制要求在接口（<code>.h</code> 文件）中暴露不必要的实现细节。例如，即使一个类 <code>Foo</code> 仅仅在其内部使用了 <code>std::vector&lt;int&gt;</code>：</p><div class="language-c++ vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c++</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// foo.h</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // &lt;-- 不必要的暴露</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;int&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>你只是想使用 <code>Foo</code> 类，却被迫通过 <code>#include &quot;foo.h&quot;</code> 引入了 <strong><code>&lt;vector&gt;</code> 的全部依赖</strong>。这被称为<strong>传染式依赖（Transitive Includes）</strong>：用户被迫依赖了接口底层的所有实现细节所依赖的头文件，导致编译依赖网状膨胀。</p><h4 id="问题四-odr、abi、隐式规则过多" tabindex="-1">问题四：ODR、ABI、隐式规则过多 <a class="header-anchor" href="#问题四-odr、abi、隐式规则过多" aria-label="Permalink to &quot;问题四：ODR、ABI、隐式规则过多&quot;">​</a></h4><p>头文件机制带来了一系列复杂且隐式的规则，如 <code>inline</code>、模板定义、<code>static</code> 变量以及在头文件中实现函数等。最危险的是 <strong>ODR（One Definition Rule，单一定义规则）</strong>。ODR 错误常常能通过编译阶段（因为每个编译单元只看到了一个定义），但会在<strong>链接阶段才暴露</strong>出来，导致难以调试的&quot;链接器错误&quot;（Linker Error），极大地增加了代码的脆弱性。</p><hr><h2 id="c-modules-的核心思想-让编译器真正-理解模块" tabindex="-1">C++ Modules 的核心思想：<strong>让编译器真正&quot;理解模块&quot;</strong> <a class="header-anchor" href="#c-modules-的核心思想-让编译器真正-理解模块" aria-label="Permalink to &quot;C++ Modules 的核心思想：**让编译器真正&quot;理解模块&quot;**&quot;">​</a></h2><p>所以聪明的你就知道，既然有这些问题，modules就是来解决的嘛！（虽然笔者吐槽下，我现在在的工程里用模块感觉就那会事情，所以还在尝试），简单的说：<strong>Modules = 编译器可理解、可缓存、可隔离的接口单元</strong></p><h4 id="import关键字-include" tabindex="-1"><code>import</code>关键字 ≠ <code>#include</code> <a class="header-anchor" href="#import关键字-include" aria-label="Permalink to &quot;`import`关键字 ≠ `#include`&quot;">​</a></h4><p><code>import std;</code>就是将现在的标准库模块导入到我们的代码中，他告诉我们的MSVC编译器：&quot;请把 <strong><code>std</code> 模块的已编译接口信息</strong> 导入到当前翻译单元。&quot;</p><h4 id="模块的最小单位-bmis-binary-module-interface" tabindex="-1">模块的最小单位：BMIs（Binary Module Interface） <a class="header-anchor" href="#模块的最小单位-bmis-binary-module-interface" aria-label="Permalink to &quot;模块的最小单位：BMIs（Binary Module Interface）&quot;">​</a></h4><p>在 MSVC 中，每个模块接口单元会被编译成一个 <strong><code>.ifc</code> 文件</strong>，他是模块的中间产物，方便接入原来的编译系统，这里面存放的就是前端AST 的序列化结果——类型、函数、模板的结构化描述（额，笔者真的第一反应就是&quot;C++ 版的 <code>.class</code> 文件（Java）&quot;）</p><h4 id="流程差异" tabindex="-1">流程差异 <a class="header-anchor" href="#流程差异" aria-label="Permalink to &quot;流程差异&quot;">​</a></h4><p>之前头文件的处理是依赖预处理的，直接将头文件粘贴到了源文件里去了作为一个编译单元搞，现在的话，模块就会好很多，他只会编译一次模块，然后你用的时候直接加载 .ifc文件，时间上可以打折扣了。MSVC Modules 的设计特点（非常实用）</p><h2 id="import-std-到底发生了什么" tabindex="-1"><code>import std;</code> 到底发生了什么？ <a class="header-anchor" href="#import-std-到底发生了什么" aria-label="Permalink to &quot;`import std;` 到底发生了什么？&quot;">​</a></h2><p>当你写下<code>import std;</code>的时候，MSVC 会：</p><ol><li><p>查找标准库模块 <code>std</code></p></li><li><p>加载其 <code>.ifc</code> 文件（由 STL 官方预编译）</p></li><li><p>把所有导出的符号注入当前 TU</p></li><li><p><strong>不引入任何宏</strong>（这点极其重要），这也是为什么 <code>min/max</code> 宏问题在 Modules 世界里自然消失。</p><p>注意，模块 <strong>默认不导出宏</strong>，宏不会跨 <code>import</code> 传播，所以你写的宏是没办法泄露到依赖文件上去的。</p></li></ol><hr><h2 id="今日要在什么时候使用-msvc-modules" tabindex="-1">今日要在什么时候使用 MSVC Modules？ <a class="header-anchor" href="#今日要在什么时候使用-msvc-modules" aria-label="Permalink to &quot;今日要在什么时候使用 MSVC Modules？&quot;">​</a></h2><p>上面就说了，C++ Modules 是对传统头文件机制的结构性解决方案，但在实际应用于生产环境时，特别是在 MSVC (Visual Studio) 环境下，需要采取策略性使用。</p><h4 id="强烈推荐的使用场景" tabindex="-1">强烈推荐的使用场景 <a class="header-anchor" href="#强烈推荐的使用场景" aria-label="Permalink to &quot;强烈推荐的使用场景&quot;">​</a></h4><h4 id="_1-使用-import-std-替代标准库头文件" tabindex="-1">1. 使用 <code>import std;</code> 替代标准库头文件 <a class="header-anchor" href="#_1-使用-import-std-替代标准库头文件" aria-label="Permalink to &quot;1. 使用 `import std;` 替代标准库头文件&quot;">​</a></h4><p>这是目前最安全、最有价值的 Modules 用法。现在咱们彻底解决了标准库头文件（如 <code>&lt;vector&gt;</code>, <code>&lt;string&gt;</code>, <code>&lt;iostream&gt;</code>）带来的<strong>编译速度灾难</strong>和<strong>宏污染</strong>问题。</p><p>而且只用一个<code>import std;</code>，咱们就不用费劲心思写一大堆include了，编译器只需要处理一次预编译的 Standard Library Module 接口，极大提升编译速度。标准库内部的宏也不会污染您的代码。</p><h4 id="_2-新项目内部的模块化-业务模块隔离" tabindex="-1">2. 新项目内部的模块化（业务模块隔离） <a class="header-anchor" href="#_2-新项目内部的模块化-业务模块隔离" aria-label="Permalink to &quot;2. 新项目内部的模块化（业务模块隔离）&quot;">​</a></h4><p>对于新创建的、主要针对 Windows 平台或内部使用的项目，可以考虑将项目内部的业务逻辑划分为独立的 Modules。用户代码只需要 <code>import MyModule;</code>，而不会被迫 <code>#include</code> 模块内部依赖的所有头文件。在写法上，业务逻辑组织成 <code>.ixx</code> 或 <code>.cppm</code> 模块接口文件，<code>export</code> 仅需暴露的接口。<strong>接口与实现彻底解耦</strong>。更改模块内部的实现细节和私有依赖时，依赖该模块的用户代码<strong>不需要重新编译</strong>（除非接口本身发生变化）。</p><h4 id="谨慎的使用场景" tabindex="-1">谨慎的使用场景 <a class="header-anchor" href="#谨慎的使用场景" aria-label="Permalink to &quot;谨慎的使用场景&quot;">​</a></h4><h4 id="_1-大型跨平台库的公共接口" tabindex="-1">1. 大型跨平台库的公共接口 <a class="header-anchor" href="#_1-大型跨平台库的公共接口" aria-label="Permalink to &quot;1. 大型跨平台库的公共接口&quot;">​</a></h4><p>如果我们正在做的事情是：正在开发一个需要被多种编译器（如 MSVC、GCC、Clang）稳定使用的<strong>公共/开源库</strong>，请谨慎将 Modules 用于其公开 API。毕竟这玩意没几年还，目前主流编译器的 Modules <strong>实现仍存在差异</strong>和潜在 Bug。他作为准备派发的库，似乎还是会为库的用户带来额外的配置复杂度。</p><h4 id="_2-需要-gcc-clang-完全一致行为的项目" tabindex="-1">2. 需要 GCC / Clang 完全一致行为的项目 <a class="header-anchor" href="#_2-需要-gcc-clang-完全一致行为的项目" aria-label="Permalink to &quot;2. 需要 GCC / Clang 完全一致行为的项目&quot;">​</a></h4><p>如果您的项目需要在不同平台和编译器上实现<strong>完全一致且高度稳定</strong>的行为（例如嵌入式系统、高完整性金融应用），Modules 的潜在实现差异可能带来风险。毕竟Modules 的语义（尤其是涉及<strong>导入顺序、链接和 ODR</strong> 的复杂场景）可能在不同编译器之间存在微妙的差异。</p><p>这一件事情上，保守点的依赖传统头文件是目前最能保证多平台行为一致性的方式，因为它依赖的是成熟数十年的 <code>#include</code> 预处理语义。</p><table tabindex="0"><thead><tr><th><strong>场景</strong></th><th><strong>推荐等级</strong></th><th><strong>原因/价值</strong></th></tr></thead><tbody><tr><td><strong>使用 <code>import std;</code></strong></td><td><strong>✅ 强烈推荐</strong></td><td>解决标准库的编译速度和宏污染问题，价值高，风险极低。</td></tr><tr><td><strong>新项目/内部业务模块化</strong></td><td><strong>✅ 推荐</strong></td><td>消除传染式依赖，实现接口与实现解耦，提升内部编译效率。</td></tr><tr><td><strong>公共/跨平台库的 API</strong></td><td><strong>⚠️ 谨慎</strong></td><td>跨编译器实现差异和工具链成熟度问题，可能影响兼容性。</td></tr><tr><td><strong>对行为一致性要求极高</strong></td><td><strong>⚠️ 谨慎</strong></td><td>避免潜在的编译器实现差异导致的不可预知行为。</td></tr></tbody></table>',49)])])}const g=t(n,[["render",d]]);export{u as __pageData,g as default};
