import{_ as e,o as i,c as a,a2 as n}from"./chunks/framework.CZcFxxdw.js";const k=JSON.parse(`{"title":"once_callback Design Guide (Part 1): Motivation and Interface Design","description":"Starting from Chromium's OnceCallback, designing a C++23 move-only, single-use callback component—Part 1 focuses on motivation analysis and API design","frontmatter":{"title":"once_callback Design Guide (Part 1): Motivation and Interface Design","description":"Starting from Chromium's OnceCallback, designing a C++23 move-only, single-use callback component—Part 1 focuses on motivation analysis and API design","chapter":1,"order":1,"tags":["host","cpp-modern","advanced","回调机制","函数对象"],"difficulty":"advanced","platform":"host","cpp_standard":[23],"reading_time_minutes":20,"prerequisites":["std::function、std::invoke 与可调用对象","移动语义与完美转发"],"related":["OnceCallback 与 RepeatingCallback","bind_once / bind_repeating 与参数绑定"]},"headers":[],"relativePath":"en/vol9-open-source-project-learn/chrome/01_once_callback/hands_on/01-once-callback-design.md","filePath":"en/vol9-open-source-project-learn/chrome/01_once_callback/hands_on/01-once-callback-design.md","lastUpdated":null}`),t={name:"en/vol9-open-source-project-learn/chrome/01_once_callback/hands_on/01-once-callback-design.md"};function l(o,s,r,c,h,p){return i(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="once-callback-design-guide-part-1-motivation-and-interface-design" tabindex="-1">once_callback Design Guide (Part 1): Motivation and Interface Design <a class="header-anchor" href="#once-callback-design-guide-part-1-motivation-and-interface-design" aria-label="Permalink to &quot;once_callback Design Guide (Part 1): Motivation and Interface Design&quot;">​</a></h1><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>Honestly, the most common pitfall I&#39;ve encountered in async programming is callbacks being invoked multiple times. The scenario is classic: you register a callback for file I/O completion, expecting it to run exactly once, but a slip of logic triggers it an extra time. The resources freed inside the callback get accessed a second time, and you are promptly rewarded with a segfault. A major characteristic of this kind of bug is that it is extremely hard to reproduce in tests, because the normal async path typically invokes the callback only once; the real trigger is some race condition or error retry path.</p><p><code>std::function</code> can&#39;t help us here. It allows multiple invocations, permits copy propagation, and callback objects can end up scattered everywhere. In Volume 2, we already dissected the internal mechanisms of <code>std::function</code> (type erasure + SBO) and its <code>LightCallback</code> simplified implementation—that version solved the type erasure overhead problem, but it completely missed the semantic question of &quot;how many times should a callback be invoked.&quot;</p><p>When the Chromium team designed <code>base::OnceCallback</code>, they provided a beautifully elegant answer: <strong>let the callback&#39;s type system itself constrain the invocation semantics</strong>. <code>OnceCallback</code> is a move-only type, and its <code>Run()</code> method can only be called through an rvalue reference (<code>std::move(cb).Run()</code>). After being invoked once, the callback object is consumed, and any subsequent invocation is either a no-op or triggers an assertion failure. This design has been thoroughly validated through the tens of billions of task dispatches the Chrome browser handles every day.</p><p>The goal of this series is not to blindly copy Chromium&#39;s implementation (which is extremely complex, involving hand-rolled reference counting, <code>TRIVIAL_ABI</code> annotations, and function pointer dispatch tables). Instead, we will leverage new C++23 features—particularly <code>std::move_only_function</code> and deducing this—to implement a <code>OnceCallback</code> component that retains the essence of Chromium&#39;s design while keeping the codebase manageable.</p><blockquote><p><strong>Learning Objectives</strong></p><ul><li>Understand why &quot;move-only + single-use consumption&quot; is the correct semantic constraint for callbacks</li><li>Design the complete public interface of <code>OnceCallback&lt;R(Args...)&gt;</code></li><li>Analyze the internal architecture of Chromium&#39;s <code>OnceCallback</code> and understand the reasoning behind each design decision</li></ul></blockquote><hr><h2 id="our-problem-three-major-flaws-of-std-function-in-async-scenarios" tabindex="-1">Our Problem: Three Major Flaws of <code>std::function</code> in Async Scenarios <a class="header-anchor" href="#our-problem-three-major-flaws-of-std-function-in-async-scenarios" aria-label="Permalink to &quot;Our Problem: Three Major Flaws of \`std::function\` in Async Scenarios&quot;">​</a></h2><p>Before we start designing, let&#39;s clearly break down the problem. As a general-purpose callable object wrapper, <code>std::function</code> is a design success—but in the specific scenario of async callbacks, it has three issues that will send your blood pressure through the roof.</p><p><strong>First, it is copyable.</strong> <code>std::function</code> inherently supports copying, meaning a single callback can be duplicated to arbitrary locations. In an async system, this equates to allowing multiple execution paths to simultaneously hold copies of the same callback. If the callback captures move-only resources (like a <code>std::unique_ptr</code>), copying fails at compile time; if it captures raw pointers or references, multiple copies executing simultaneously will introduce data races. The Chrome team&#39;s approach is very straightforward: since async task callbacks fundamentally should not be copied, make them non-copyable at the type level.</p><p><strong>Second, it is repeatedly invocable.</strong> <code>std::function::operator()</code> places no constraints on the number of invocations. You can call the same <code>std::function</code> a thousand times, and it will happily execute every time. But in async callback scenarios, invoking a file-read-completion callback twice is a logic error—it might trigger double resource releases, double state transitions, or double message sends. This kind of error is completely undetectable by the type system, relying solely on runtime assertions (if they exist) or—more commonly—being discovered at the bug crime scene.</p><p><strong>Third, it cannot express consumption semantics.</strong> In Chrome&#39;s task dispatch model, once a <code>PostTask(FROM_HERE, callback)</code> is invoked, the <code>callback</code> should no longer be used—its ownership has been transferred to the task system. <code>std::function</code>&#39;s <code>operator()</code> is <code>const</code>-qualified, so invoking it does not alter the state of the <code>std::function</code> object itself, meaning you cannot use the invocation interface to express the &quot;invoke-to-consume&quot; semantic.</p><p>These three issues boil down to a single point: <code>std::function</code>&#39;s interface design cannot express the constraint that &quot;this callback can only be invoked once, and becomes invalid afterward.&quot; Chrome&#39;s <code>OnceCallback</code> was designed precisely to fill this semantic gap.</p><hr><h2 id="chromium-s-answer-the-oncecallback-design-philosophy" tabindex="-1">Chromium&#39;s Answer: The <code>OnceCallback</code> Design Philosophy <a class="header-anchor" href="#chromium-s-answer-the-oncecallback-design-philosophy" aria-label="Permalink to &quot;Chromium&#39;s Answer: The \`OnceCallback\` Design Philosophy&quot;">​</a></h2><p>Chrome&#39;s callback system is built on a core principle: <strong>message passing over locks, serialization over threads</strong>. Under this principle, every callback posted to the task system (called a task in Chrome) is an independent, one-time message. Once posted, the callback&#39;s ownership transfers from the caller to the task system; once executed, the callback is destroyed. No sharing, no reuse, no ambiguity.</p><p>This philosophy is directly reflected in <code>OnceCallback</code>&#39;s type design:</p><ul><li><strong>Move-only</strong>: <code>OnceCallback</code> deletes copy construction and copy assignment, retaining only move operations. This guarantees at the type level that the callback has exactly one owner at any given time.</li><li><strong>Rvalue-qualified <code>Run()</code></strong>: <code>OnceCallback::Run()</code> can only be invoked through an rvalue reference (<code>std::move(cb).Run(args...)</code>). Lvalue invocation triggers <code>static_assert</code>, producing a clear compile-time error. This serves as a syntactic reminder to the caller: &quot;You are consuming this callback; don&#39;t use it afterward.&quot;</li><li><strong>Single-use consumption</strong>: Internally, <code>Run()</code> uses a reference counting mechanism to destroy <code>BindState</code>, making any subsequent access to the same object a safe no-op.</li></ul><p>Chrome actually also has <code>RepeatingCallback</code>—a copyable, repeatedly invocable version. The two callback classes share the same <code>BindState</code> internal implementation, differing only in the value category qualification of <code>Run()</code> and the ownership semantics of <code>BindState</code>. This design allows the same binding infrastructure to simultaneously serve two fundamentally different usage patterns: &quot;one-time tasks&quot; and &quot;repeating listeners.&quot;</p><h3 id="overview-of-chromium-s-internal-implementation" tabindex="-1">Overview of Chromium&#39;s Internal Implementation <a class="header-anchor" href="#overview-of-chromium-s-internal-implementation" aria-label="Permalink to &quot;Overview of Chromium&#39;s Internal Implementation&quot;">​</a></h3><p>We don&#39;t need to dive into every line of Chromium&#39;s source code, but we do need to understand its core architecture, because our <code>OnceCallback</code> will borrow the same layered approach, simply using C++23 standard facilities to simplify the implementation.</p><p>Chromium&#39;s callback system consists of three layers, from bottom to top:</p><p><strong>Bottom layer: <code>BindStateBase</code></strong>—the type-erased base class. It carries a reference count, but interestingly, it <strong>does not use virtual functions</strong>. Instead, it has three function pointer members: <code>polymorphic_invoke_</code> (responsible for invocation), <code>destructor_</code> (responsible for destruction), and <code>query_cancellation_traits_</code> (responsible for cancellation queries). The Chrome team chose function pointers over virtual functions to reduce binary bloat. Virtual functions generate a separate vtable for each template instantiation; if a project has 100 different <code>BindState&lt;Functor, BoundArgs...&gt;</code> instantiations, it will have 100 vtables. The function pointer approach can reuse the same static functions, differing only in the pointer values, without generating additional code sections.</p><p><strong>Middle layer: <code>BindState&lt;Functor, BoundArgs...&gt;</code></strong>—the templated concrete class, inheriting from <code>BindStateBase</code>. It stores the actual callable object (<code>Functor</code>) and the arguments bound via <code>BindOnce</code> (<code>BoundArgs...</code>). You can think of it as a &quot;box that holds everything&quot;: inside the box are your lambda, the bound arguments, and the function pointers required by the base class. Instances of this class have their lifetimes managed through <code>scoped_refptr</code> (Chromium&#39;s own intrusive reference-counted smart pointer)—<code>OnceCallback</code> releases the reference on <code>Run()</code>, while <code>RepeatingCallback</code> retains the reference on each <code>Run()</code>.</p><p><strong>Top layer: <code>OnceCallback&lt;Signature&gt;</code> and <code>RepeatingCallback&lt;Signature&gt;</code></strong>—the types users directly interact with. They are essentially thin wrappers around <code>BindStateHolder</code>, and <code>BindStateHolder</code> is simply a <code>scoped_refptr&lt;BindStateBase&gt;</code> with a <code>TRIVIAL_ABI</code> annotation. <code>TRIVIAL_ABI</code> is a Clang extension attribute that tells the compiler &quot;this type can be passed in a register just like an int,&quot; making the actual size of a <code>OnceCallback</code> only one pointer (8 bytes), where move operations are merely copying a pointer—extremely lightweight.</p><p>The relationship between these three layers can be summarized in one sentence: <strong>the top-layer callback object is just a pointer to the middle-layer box, and the box holds the function pointers required by the bottom layer and the actual data</strong>. The <code>OnceCallback</code> we design next will retain this &quot;outer interface + middle storage + type erasure&quot; layered approach, but we will use <code>std::move_only_function</code> to replace Chromium&#39;s hand-rolled <code>BindState</code> + <code>scoped_refptr</code> combination, and use deducing this to replace the <code>const&amp;</code> overload + <code>static_assert</code> hack.</p><hr><h2 id="environment-setup" tabindex="-1">Environment Setup <a class="header-anchor" href="#environment-setup" aria-label="Permalink to &quot;Environment Setup&quot;">​</a></h2><p>First, let&#39;s confirm our toolchain. <code>OnceCallback</code> depends on the following C++23 features:</p><ul><li><strong><code>std::move_only_function</code></strong> (<code>&lt;functional&gt;</code>): A move-only type-erased callable wrapper introduced in C++23, serving as our core building block</li><li><strong>Deducing this</strong> (explicit object parameter <code>this auto&amp;&amp; self</code>): A C++23 feature that allows deducing the value category of <code>this</code> in member functions</li><li><strong><code>if consteval</code></strong>: Compile-time conditional logic (may be used in some implementations)</li></ul><p>In terms of compiler requirements, GCC 12+ or Clang 16+ fully supports the above features. Simply add <code>-std=c++23</code> when compiling. You can use the following code to quickly verify your environment:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;functional&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 验证 std::move_only_function 可用</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static_assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(__cpp_lib_move_only_function </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 202110</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">L</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 验证 deducing this 可用（编译通过即说明支持）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> auto&amp;&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Check c;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    c.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><p>If this code compiles successfully, your environment is good to go. That said, as of the time of writing, some compilers&#39; <code>std::move_only_function</code> implementations still have bugs (for example, early versions of GCC 12 fail to compile in certain SFINAE scenarios), so I recommend using the latest stable versions of GCC 13+ or Clang 17+.</p><h3 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h3><p>We assume readers are already familiar with the following topics (covered in the corresponding Volume 2 articles):</p><ul><li><strong>Move semantics and perfect forwarding</strong>: The core of <code>OnceCallback</code> is move-only; if you aren&#39;t familiar with the principles of <code>std::move</code> and <code>std::forward</code>, the implementation process will be quite painful. Corresponding article: Volume 2 ch00 Move Semantics series.</li><li><strong>Type erasure and SBO in <code>std::function</code></strong>: We build directly on top of <code>std::move_only_function</code>, so you need to understand the basic principles of type erasure and what small buffer optimization is and why it matters. Corresponding article: Volume 2 ch03 <code>std::function</code> and Callable Objects.</li><li><strong><code>std::invoke</code> and the uniform call protocol</strong>: Internally, <code>bind_once</code> uses <code>std::invoke</code> to uniformly handle different types of callable objects, such as function pointers, member function pointers, and functors. Corresponding article: Ibid.</li><li><strong>Variadic templates and parameter pack expansion</strong>: Template specialization of <code>OnceCallback&lt;R(Args...)&gt;</code> and argument binding in <code>bind_once</code> both require familiarity with parameter pack syntax. Corresponding articles: Volume 2 ch00 Perfect Forwarding, Volume 4 Template Basics.</li><li><strong><code>std::invoke</code> and the uniform call protocol</strong>: Internally, <code>bind_once</code> uses <code>std::invoke</code> to uniformly handle different types of callable objects, such as function pointers, member function pointers, and functors. Corresponding article: Ibid.</li><li><strong>Variadic templates and parameter pack expansion</strong>: Template specialization of <code>OnceCallback&lt;R(Args...)&gt;</code> and argument binding in <code>bind_once</code> both require familiarity with parameter pack syntax. Corresponding articles: Volume 2 ch00 Perfect Forwarding, Volume 4 Template Basics.</li></ul><hr><h2 id="designing-the-interface-what-api-do-we-want" tabindex="-1">Designing the Interface: What API Do We Want? <a class="header-anchor" href="#designing-the-interface-what-api-do-we-want" aria-label="Permalink to &quot;Designing the Interface: What API Do We Want?&quot;">​</a></h2><p>Let&#39;s nail down the target API first, and then circle back to discuss each design decision. This is how engineers work—first figure out &quot;what I want,&quot; then figure out &quot;how to do it.&quot;</p><h3 id="core-usage" tabindex="-1">Core Usage <a class="header-anchor" href="#core-usage" aria-label="Permalink to &quot;Core Usage&quot;">​</a></h3><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;once_callback/once_callback.hpp&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. 构造：从 lambda 创建</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tamcpp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chrome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cb </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OnceCallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)&gt;([](</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> a</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. 调用：必须通过右值（std::move）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cb).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // result == 7</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. 调用后，cb 被消费</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// std::move(cb).run(1, 2);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 运行时断言失败：callback already consumed</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><h3 id="argument-binding" tabindex="-1">Argument Binding <a class="header-anchor" href="#argument-binding" aria-label="Permalink to &quot;Argument Binding&quot;">​</a></h3><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// bind_once：预绑定部分参数，返回一个 OnceCallback</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tamcpp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chrome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bound </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> bind_once</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)&gt;(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [](</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> z</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> z; },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 预绑定前两个参数</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(bound).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // r == 60</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><h3 id="cancellation-checks" tabindex="-1">Cancellation Checks <a class="header-anchor" href="#cancellation-checks" aria-label="Permalink to &quot;Cancellation Checks&quot;">​</a></h3><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tamcpp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chrome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cb </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OnceCallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)&gt;([](</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 检查回调是否仍然有效</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cb.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">is_cancelled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cb).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// maybe_valid：乐观检查，适用于跨序列场景</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (cb.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">maybe_valid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // &quot;可能&quot;有效，不保证</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cb).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><h3 id="chained-composition" tabindex="-1">Chained Composition <a class="header-anchor" href="#chained-composition" aria-label="Permalink to &quot;Chained Composition&quot;">​</a></h3><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tamcpp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chrome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// then()：将当前回调的返回值传给下一个回调</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pipeline </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OnceCallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)&gt;([](</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> a</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([](</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sum </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> final_result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pipeline).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// final_result == 14  (3+4)*2</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="interface-design-decision-analysis" tabindex="-1">Interface Design Decision Analysis <a class="header-anchor" href="#interface-design-decision-analysis" aria-label="Permalink to &quot;Interface Design Decision Analysis&quot;">​</a></h3><p>Now let&#39;s discuss the design decisions behind these APIs one by one.</p><p><strong>Why <code>run()</code> instead of <code>operator()</code>?</strong></p><p>Chromium uses <code>Run()</code> (Google C++ style requires capitalized names). We use <code>run()</code> to conform to snake_case naming conventions. But the deeper reason is semantic distinction: <code>operator()</code> is too generic; any callable object has a <code>operator()</code>. <code>run()</code> explicitly expresses the &quot;execute task&quot; semantics, making it immediately obvious during code review that you are consuming a <code>OnceCallback</code>, rather than invoking an ordinary callable object.</p><p><strong>Why must <code>run()</code> be called through an rvalue?</strong></p><p>This is the most critical point of the entire design. We need a mechanism that causes <code>cb.run(args)</code> (lvalue invocation) to fail at compile time, while <code>std::move(cb).run(args)</code> (rvalue invocation) compiles successfully. Chromium&#39;s implementation achieves this through two overloads: one <code>Run() &amp;&amp;</code> is the actual execution version, and a <code>Run() const&amp;</code> internally contains a <code>static_assert(!sizeof(*this))</code> to produce a compile-time error. This hack works but is ugly.</p><p>We can do this more elegantly using C++23&#39;s <strong>deducing this</strong> (explicit object parameter). Simply put, deducing this allows us to explicitly write <code>this</code> as a template parameter in a member function, and the compiler deduces this parameter&#39;s type based on whether the object is an lvalue or rvalue at the call site. Leveraging this feature, <code>run(this auto&amp;&amp; self, Args... args)</code> distinguishes between lvalue and rvalue invocations by deducing the value category of <code>self</code>, intercepting illegal usage at compile time:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">this</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Self</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">FuncArgs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">... </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) -&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ReturnType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    static_assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::is_lvalue_reference_v</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Self</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;OnceCallback::run() must be called on an rvalue. &quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;Use std::move(cb).run(...) instead.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ... 实际调用逻辑</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>When the caller writes <code>cb.run(args)</code>, <code>Self</code> is deduced as <code>OnceCallback&amp;</code> (an lvalue reference), <code>static_assert</code> triggers, and the error message directly tells the caller what to do. When writing <code>std::move(cb).run(args)</code>, <code>Self</code> is deduced as <code>OnceCallback</code> (an rvalue), and compilation succeeds. We will dive into the specific working mechanism of deducing this and a detailed comparison with Chromium&#39;s approach in the next implementation article.</p><p><strong>Why distinguish between <code>is_cancelled()</code> and <code>maybe_valid()</code>?</strong></p><p>This design comes directly from Chromium&#39;s <code>CancellationQueryMode</code>. The difference lies in the strength of the safety guarantees. <code>is_cancelled()</code> provides a deterministic answer—it can only be called on the sequence to which the callback is bound, guaranteeing an accurate result. <code>maybe_valid()</code> provides an optimistic estimate—it can be called from any thread, but the result may be stale. In practice, <code>is_cancelled()</code> is used for &quot;checking whether it still makes sense before posting,&quot; while <code>maybe_valid()</code> is used for the optimization path of &quot;quickly checking cross-thread whether it&#39;s worth posting.&quot;</p><p>In our simplified implementation, both methods query through <code>CancelableToken</code>—<code>is_cancelled()</code> checks whether the state is valid and whether the token is still valid, while <code>maybe_valid()</code> is simply a thin wrapper around <code>!is_cancelled()</code>. If more fine-grained thread-safety semantics are needed later, we can differentiate between these two methods.</p><p><strong>Why does <code>then()</code> consume <code>*this</code>?</strong></p><p>The semantics of <code>then()</code> are &quot;pass the execution result of the current callback to the next callback.&quot; This requires the current callback to be fully captured inside the new callback returned by <code>then()</code>. If <code>then()</code> did not consume <code>*this</code>, it would lead to the same callback existing in two places simultaneously—the original location and the new callback returned by <code>then()</code>—which violates the move-only semantic constraint. Therefore, <code>then()</code> is declared as an rvalue-qualified member function (<code>then(...) &amp;&amp;</code>), and after invocation, the original callback object enters a consumed state.</p><hr><h2 id="internal-mechanisms-the-two-layer-architecture-of-type-erasure" tabindex="-1">Internal Mechanisms: The Two-Layer Architecture of Type Erasure <a class="header-anchor" href="#internal-mechanisms-the-two-layer-architecture-of-type-erasure" aria-label="Permalink to &quot;Internal Mechanisms: The Two-Layer Architecture of Type Erasure&quot;">​</a></h2><p>With the interface designed, let&#39;s look at how to organize the internals. Chromium uses the combination of <code>BindStateBase</code> + <code>scoped_refptr</code> + function pointer tables to implement type erasure, which works great but results in an astonishing amount of code. Our strategy is to let <code>std::move_only_function</code> handle the dirty work of type erasure and small object optimization, allowing us to focus on the interesting parts: consumption semantics, argument binding, and chained composition.</p><h3 id="why-choose-std-move-only-function" tabindex="-1">Why Choose <code>std::move_only_function</code> <a class="header-anchor" href="#why-choose-std-move-only-function" aria-label="Permalink to &quot;Why Choose \`std::move_only_function\`&quot;">​</a></h3><p><code>std::move_only_function&lt;R(Args...)&gt;</code> was introduced in C++23, positioned as the &quot;move-only version of <code>std::function</code>&quot;. It internally implements type erasure and SBO, behaving similarly to <code>std::function</code> but with copy operations deleted.</p><p>You may have already noticed the syntax <code>OnceCallback&lt;R(Args...)&gt;</code>—<code>R(Args...)</code> looks like a function declaration, but in the context of a template parameter, it is a <strong>function type</strong>. <code>int(int, int)</code> describes &quot;a function that takes two int parameters and returns an int,&quot; and it is a valid C++ type. We deconstruct this type through template partial specialization—a technique we will explain in detail in the next article.</p><p>Using <code>std::move_only_function</code> for internal storage has several benefits. It saves us from hand-writing type erasure—recall that in Volume 2&#39;s <code>LightCallback</code>, we spent an entire chapter hand-writing function pointer tables, SBO buffers, and move/destruction operations, whereas <code>std::move_only_function</code> encapsulates all of this, ready to use out of the box. It also natively supports move-only callable objects—if our callback captures a <code>std::unique_ptr</code>, <code>std::function</code> will fail to compile directly due to its copy semantics requirements, but <code>std::move_only_function</code> has no such issue. Furthermore, its SBO implementation has been carefully tuned by standard library authors, eliminating the need for heap allocation in the vast majority of cases—for lambdas capturing a small number of arguments, the performance is more than adequate.</p><h3 id="three-state-management" tabindex="-1">Three-State Management <a class="header-anchor" href="#three-state-management" aria-label="Permalink to &quot;Three-State Management&quot;">​</a></h3><p>After introducing <code>std::move_only_function</code>, there is a design problem to solve: how do we distinguish between a &quot;null callback&quot; and a &quot;consumed callback&quot;?</p><p>A <code>std::move_only_function</code> can inherently be empty (default-constructed or constructed from <code>nullptr</code>), but &quot;empty&quot; and &quot;already consumed by <code>run()</code>&quot; are two different states. A null callback means &quot;it was never assigned,&quot; and invoking it should trigger a clear error (&quot;callback is null&quot;). A consumed callback means &quot;it once held a value, but has already been invoked,&quot; and invoking it should also trigger an error (&quot;callback already consumed&quot;), but the error message is different, which is very helpful for debugging.</p><p>Therefore, our internal state needs three states:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    kEmpty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // 默认构造，从未被赋值</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    kValid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // 持有有效的可调用对象</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    kConsumed</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // 已被 run() 消费</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Combined with <code>std::move_only_function</code>, our internal storage structure looks roughly like this:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReturnType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">... </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">FuncArgs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OnceCallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ReturnType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(FuncArgs...)&gt; {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::move_only_function</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FuncSig</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> func_;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Status status_ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::kEmpty;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 取消令牌（可选）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::shared_ptr</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CancelableToken</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> token_;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Upon move construction, <code>func_</code> and <code>status_</code> are moved together, and the source object&#39;s state is set to <code>kEmpty</code>. When <code>run()</code> executes, it first checks whether <code>status_</code> is <code>kValid</code>, and after execution, it nullifies <code>func_</code> and sets <code>status_</code> to <code>kConsumed</code>. This way, during debugging, we can provide precise error messages based on the value of <code>status_</code>.</p><h3 id="trade-offs-compared-to-the-original-chromium-version" tabindex="-1">Trade-offs Compared to the Original Chromium Version <a class="header-anchor" href="#trade-offs-compared-to-the-original-chromium-version" aria-label="Permalink to &quot;Trade-offs Compared to the Original Chromium Version&quot;">​</a></h3><p>By using <code>std::move_only_function</code> for the underlying storage, we gain a concise implementation, but we also sacrifice some things. Chromium&#39;s <code>OnceCallback</code> is only one pointer in size (8 bytes), thanks to the <code>TRIVIAL_ABI</code> annotation and the reference-counted <code>BindState</code>—the callback object itself is merely a pointer to a heap-allocated <code>BindState</code>. Our <code>OnceCallback</code> wraps a <code>std::move_only_function</code> (typically 32 bytes) plus a <code>Status</code> enum and an optional <code>CancelableToken</code> pointer (16 bytes), totaling around 56-64 bytes.</p><p>Another difference is reference counting. Chromium&#39;s <code>BindState</code> is reference-counted, allowing multiple callbacks to share the same bound state (which is necessary for the copy semantics of <code>RepeatingCallback</code>). In our implementation, <code>std::move_only_function</code> itself has exclusive ownership and does not support sharing. For the move-only semantics of <code>OnceCallback</code>, this is not a problem, but when implementing <code>RepeatingCallback</code> later on, we will need to rethink this design.</p><p>These trade-offs are reasonable—we exchanged size and reference counting flexibility for a significantly reduced implementation complexity. In practice, a 56-64 byte callback object is not a bottleneck in the vast majority of scenarios, and the clean code structure makes maintenance and extension much cheaper.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>In this article, we established the design foundation for <code>once_callback</code>. The key takeaways are:</p><ul><li><code>std::function</code> has three major flaws in async callback scenarios: it is copyable, it is repeatedly invocable, and it cannot express consumption semantics</li><li>Chromium&#39;s <code>OnceCallback</code> constrains callback semantics through move-only + rvalue-qualified <code>Run()</code> + single-use consumption</li><li>Our <code>OnceCallback</code> uses <code>std::move_only_function</code> for underlying type erasure and deducing this to implement the rvalue-qualified <code>run()</code></li><li>Internally, we use three-state management (<code>kEmpty</code> / <code>kValid</code> / <code>kConsumed</code>) to distinguish between null callbacks and consumed callbacks</li></ul><p>In the next article, we will enter the implementation phase: starting from the core skeleton <code>run()</code>, and progressively adding <code>bind_once</code>, cancellation checks, and <code>then()</code> chained composition.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="https://chromium.googlesource.com/chromium/src/+/main/docs/callback.md" target="_blank" rel="noreferrer">Chromium Callback Documentation</a></li><li><a href="https://chromium.googlesource.com/chromium/src/+/HEAD/base/functional/callback.h" target="_blank" rel="noreferrer">Chromium callback.h Source Code</a></li><li><a href="https://en.cppreference.com/w/cpp/utility/functional/move_only_function" target="_blank" rel="noreferrer">cppreference: std::move_only_function</a></li><li><a href="https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2021/p0847r7.html" target="_blank" rel="noreferrer">P0847R7 - Deducing this Proposal</a></li></ul>`,88)])])}const u=e(t,[["render",l]]);export{k as __pageData,u as default};
