import{_ as i,o as a,c as o,a2 as t}from"./chunks/framework.Dim5NDkF.js";const u=JSON.parse('{"title":"In-depth Understanding of C/C++ Compilation and Linking Techniques 8: Library File Search Logic","description":"","frontmatter":{"title":"In-depth Understanding of C/C++ Compilation and Linking Techniques 8: Library File Search Logic","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":13,"order":8},"headers":[],"relativePath":"en/compilation/08-library-search-logic.md","filePath":"en/compilation/08-library-search-logic.md","lastUpdated":null}'),n={name:"en/compilation/08-library-search-logic.md"};function r(s,e,l,d,c,h){return a(),o("div",null,[...e[0]||(e[0]=[t(`<h1 id="deep-dive-into-c-c-compilation-and-linking-part-8-library-file-search-logic" tabindex="-1">Deep Dive into C/C++ Compilation and Linking Part 8: Library File Search Logic <a class="header-anchor" href="#deep-dive-into-c-c-compilation-and-linking-part-8-library-file-search-logic" aria-label="Permalink to &quot;Deep Dive into C/C++ Compilation and Linking Part 8: Library File Search Logic&quot;">​</a></h1><h2 id="preface" tabindex="-1">Preface <a class="header-anchor" href="#preface" aria-label="Permalink to &quot;Preface&quot;">​</a></h2><p>Now, we need to discuss how library files are located. Locating library files means—how does an executable that depends on other dynamic libraries find those libraries at runtime?</p><p>This is no trivial matter. If we think about it, we can hardly escape the use of library files in modern software engineering. For example, software we build or use integrates third-party libraries into the product, or in a package-management paradigm, we need to locate the correct library files at runtime for a given piece of software to run properly.</p><p>It is almost exactly that.</p><h2 id="naming-conventions" tabindex="-1">Naming Conventions <a class="header-anchor" href="#naming-conventions" aria-label="Permalink to &quot;Naming Conventions&quot;">​</a></h2><p>Dynamic libraries on Linux follow a naming convention. If you pay attention, you will notice that all static libraries follow <code>lib + &lt;library_name&gt; + .a</code>. In this case, we only need to tell the linker the <code>&lt;library_name&gt;</code> part, and the linker will automatically search for <code>lib&lt;library_name&gt;.a</code> according to other rules.</p><p>Dynamic libraries are slightly more complex. Because dynamic libraries support hot-swapping (meaning software can be released without recompiling from scratch), the naming rules are actually a bit more involved. Simply put:</p><p><code>lib + &lt;library_name&gt; + .so + &lt;library version information&gt;</code></p><p>As before, we only need to provide the <code>&lt;library_name&gt;</code> part, and the linker will automatically search for the rest.</p><p><code>&lt;library version information&gt;</code> deserves a separate discussion. Generally, a version number is sufficient: <code>&lt;M&gt;.&lt;m&gt;.&lt;p&gt;</code>, which represents the major version, minor version, and patch version. This is the specific name. There is also something called a soname, which is the dynamic library name retaining only the major version information. For example, the soname of <code>libz.so.1.2.3.4</code> is libz.so.1. This example comes from <em>Advanced C/C++ Compilation</em>.</p><h2 id="runtime-dynamic-library-search-rules" tabindex="-1">Runtime Dynamic Library Search Rules <a class="header-anchor" href="#runtime-dynamic-library-search-rules" aria-label="Permalink to &quot;Runtime Dynamic Library Search Rules&quot;">​</a></h2><p>Now we need to talk about the runtime search rules for dynamic library files. Specifically, you might be interested in the runtime dynamic library search rules on Linux. Here is the breakdown. When running a dynamically linked program on Linux, a component called the <strong>dynamic linker / loader</strong> (usually <code>ld-linux.so</code> / <code>ld.so</code>) is responsible for finding and loading the shared libraries (<code>.so</code>) required by the executable. The search rules for dynamic libraries may look complex, but they actually have clear priorities and a few common &quot;control points&quot;: <code>LD_PRELOAD</code>, the executable&#39;s embedded <code>RPATH</code>/<code>RUNPATH</code>, the environment variable <code>LD_LIBRARY_PATH</code>, system configuration (<code>/etc/ld.so.conf.d</code> + <code>ldconfig</code>), and system default paths (such as <code>/lib</code>, <code>/usr/lib</code>).</p><p>In the following sections, here is what you need to know: <strong>when the dynamic linker needs to resolve a dependency</strong> (i.e., the dependency name does not contain <code>/</code>), it usually searches in the following order (simplified):</p><ol><li>Libraries specified by <code>LD_PRELOAD</code> (loaded first, used for symbol overriding/injection).</li><li>If the executable contains <code>DT_RPATH</code> and does not have <code>DT_RUNPATH</code>, the <code>DT_RPATH</code> paths are used (note: <code>DT_RPATH</code> is deprecated but still supported).</li><li>The environment variable <code>LD_LIBRARY_PATH</code> (<strong>ignored for setuid/setgid executables</strong>).</li><li>If the executable contains <code>DT_RUNPATH</code>, <code>DT_RUNPATH</code> is used (and when <code>DT_RUNPATH</code> is present, <code>DT_RPATH</code> is generally ignored).</li><li>The cache <code>/etc/ld.so.cache</code> maintained by ldconfig, along with <code>/lib</code>, <code>/usr/lib</code> (and architecture-specific <code>/lib64</code>, <code>/usr/lib64</code>)—these are the &quot;trusted directories.&quot;</li><li>(If not found in any of the above) It ultimately fails and reports an error (such as <code>ld.so: cannot find ...</code>).</li></ol><blockquote><p>Note: The details of this order (especially the interaction between <code>RPATH</code> and <code>RUNPATH</code>) are influenced by the linker implementation and linker flags (such as <code>--enable-new-dtags</code>, which enables the -R or -rpath linker directive).</p></blockquote><hr><h2 id="detailed-explanation-expanding-on-each-item" tabindex="-1">Detailed Explanation (Expanding on Each Item) <a class="header-anchor" href="#detailed-explanation-expanding-on-each-item" aria-label="Permalink to &quot;Detailed Explanation (Expanding on Each Item)&quot;">​</a></h2><h4 id="ld-preload-injecting-or-overriding-symbols-on-demand" tabindex="-1">LD_PRELOAD (&quot;Injecting&quot; or Overriding Symbols on Demand) <a class="header-anchor" href="#ld-preload-injecting-or-overriding-symbols-on-demand" aria-label="Permalink to &quot;LD_PRELOAD (&quot;Injecting&quot; or Overriding Symbols on Demand)&quot;">​</a></h4><p><code>LD_PRELOAD</code> is an environment variable that can specify one or more shared libraries to be forcibly loaded into a process <strong>before the normal search</strong> begins. This allows for intercepting or replacing symbols (functions). However, this is rarely seen and generally not recommended unless you know exactly what you are doing 😃</p><hr><h4 id="dt-rpath-and-dt-runpath-i-e-rpath-runpath" tabindex="-1">DT_RPATH and DT_RUNPATH (i.e., &quot;rpath / runpath&quot;) <a class="header-anchor" href="#dt-rpath-and-dt-runpath-i-e-rpath-runpath" aria-label="Permalink to &quot;DT_RPATH and DT_RUNPATH (i.e., &quot;rpath / runpath&quot;)&quot;">​</a></h4><p>At link time, one or more runtime library search paths can be written into the dynamic segment (<code>.dynamic</code>) of an executable or shared library, corresponding to the ELF tags <code>DT_RPATH</code> and <code>DT_RUNPATH</code> respectively. Historically, <code>DT_RPATH</code> was introduced early on with the behavior of &quot;taking priority over environment variables.&quot; Later, <code>DT_RUNPATH</code> (new-dtags) was introduced. The implication of <code>DT_RUNPATH</code> is: <strong>it is searched after <code>LD_LIBRARY_PATH</code></strong>, meaning <code>LD_LIBRARY_PATH</code> can override the paths in RUNPATH; whereas <code>DT_RPATH</code>, in some implementations or historically, takes priority over <code>LD_LIBRARY_PATH</code> (making it harder to override).</p><p>Another important behavioral difference: <strong>DT_RPATH is effective for transitive dependencies</strong>, while <strong>DT_RUNPATH may not be used to find transitive dependencies</strong> (i.e., when executable -&gt; libA -&gt; libB, the behavior of RUNPATH in certain situations will not provide paths for finding libB, whereas RPATH will). This causes some combinations that worked with RPATH under older linkers to fail with a &quot;cannot find indirect dependency&quot; error after switching to RUNPATH (new-dtags).</p><p>In my current Linux experience, I rarely encounter this. Therefore, in more test environments, adopting the following approach is appropriate.</p><hr><h4 id="ld-library-path-this-is-an-environment-variable" tabindex="-1">LD_LIBRARY_PATH (This is an Environment Variable) <a class="header-anchor" href="#ld-library-path-this-is-an-environment-variable" aria-label="Permalink to &quot;LD_LIBRARY_PATH (This is an Environment Variable)&quot;">​</a></h4><p><code>LD_LIBRARY_PATH</code> is a list of runtime library search paths used by the dynamic linker at a specific stage (see the order above). It is very commonly used to temporarily override system paths or to test new library versions. <strong>Similarly</strong>, setuid/setgid executables ignore this variable (for security reasons).</p><p>The trouble with environment variables is that they easily interfere with all processes launched from a shell that has this variable set. We do not recommend relying on <code>LD_LIBRARY_PATH</code> long-term in production environments, because it affects all child processes started through that shell and is less maintainable than system configuration (ldconfig).</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LD_LIBRARY_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/opt/foo/lib:/home/you/sw/lib:$LD_LIBRARY_PATH</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./myapp</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><hr><h4 id="ldconfig-etc-ld-so-conf-d-and-ld-so-cache" tabindex="-1">ldconfig, /etc/ld.so.conf.d, and ld.so.cache <a class="header-anchor" href="#ldconfig-etc-ld-so-conf-d-and-ld-so-cache" aria-label="Permalink to &quot;ldconfig, /etc/ld.so.conf.d, and ld.so.cache&quot;">​</a></h4><p>System administrators typically tell <code>ldconfig</code> which directories should be trusted by the system dynamic linker by placing library directories in <code>/etc/ld.so.conf</code> or <code>/etc/ld.so.conf.d/*.conf</code>. <code>ldconfig</code> scans these directories and generates a binary cache <code>/etc/ld.so.cache</code> (to improve lookup speed), while also creating symbolic links (libXXX.so -&gt; libXXX.so.VERSION). The dynamic linker reads this cache to accelerate lookups.</p><p>Common operations:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 把新目录加入配置（以 root）</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;/opt/foo/lib&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/ld.so.conf.d/foo.conf</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 重建缓存</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ldconfig</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 查看缓存内容</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ldconfig</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> foo</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><hr><h4 id="system-default-directories-trusted-directories" tabindex="-1">System Default Directories (Trusted Directories) <a class="header-anchor" href="#system-default-directories-trusted-directories" aria-label="Permalink to &quot;System Default Directories (Trusted Directories)&quot;">​</a></h4><p>The dynamic linker usually searches <code>/lib</code>, <code>/usr/lib</code> (and on 64-bit systems, <code>/lib64</code>, <code>/usr/lib64</code>) by default. These directories are known as &quot;trusted directories.&quot; <code>ldconfig</code> also processes these directories. Even if a path is not written to <code>ld.so.conf</code>, placing a library in these directories usually allows it to be found (but pay attention to architecture bits, ABI, and version matching).</p><h2 id="what-about-windows" tabindex="-1">What About Windows? <a class="header-anchor" href="#what-about-windows" aria-label="Permalink to &quot;What About Windows?&quot;">​</a></h2><p>The Windows executable/loader and APIs (<code>LoadLibrary</code> / <code>LoadLibraryEx</code> / automatic loading via the import table) define a set of search orders and security improvements.</p><p>Generally, Windows has two approaches: implicit (import table) and explicit (runtime API).</p><p><strong>Implicit loading</strong> means the executable&#39;s Import Table is resolved by the system loader during process startup or module loading. The system attempts to find and map each <code>DLL</code> into the process address space. Developers specify dependencies during the linking phase (for example, <code>kernel32.dll</code>, <code>mydll.dll</code>), and loading is automatically completed by the system at process startup.</p><p><strong>Explicit loading</strong> means the code manually loads a DLL at runtime using APIs like <code>LoadLibrary</code> / <code>LoadLibraryEx</code>, and then obtains function pointers using <code>GetProcAddress</code>. Explicit loading allows you to control search behavior through parameters (for example, using flags like <code>LOAD_LIBRARY_SEARCH_USER_DIRS</code>).</p><h4 id="default-search-order-conceptual-order" tabindex="-1">Default Search Order (Conceptual Order) <a class="header-anchor" href="#default-search-order-conceptual-order" aria-label="Permalink to &quot;Default Search Order (Conceptual Order)&quot;">​</a></h4><blockquote><p>Note: The Windows search order has subtle differences across OS versions and configurations, and the system provides settings that affect this order (explained below). Here is a conceptual common order (understanding the priorities is sufficient):</p></blockquote><p>When a process requests loading a library named <code>foo.dll</code> (without specifying an absolute path), the system usually searches in the following order (conceptual order):</p><ol><li><strong>The full path explicitly specified by the caller</strong> (if calling <code>LoadLibrary(&quot;C:\\\\path\\\\foo.dll&quot;)</code>, it loads that path directly without searching).</li><li><strong>The loader first checks if it is an entry in &quot;KnownDLLs&quot;</strong> (KnownDLLs are a set of trusted system libraries registered in the system, prioritizing the existing system version).</li><li><strong>Application directory (Executable directory)</strong>: The directory where the executable (.exe) is located (usually prioritized over the system directory, specifically influenced by settings like SafeDllSearchMode).</li><li><strong>System directory</strong> (usually <code>%SystemRoot%\\System32</code>).</li><li><strong>Windows directory</strong> (usually <code>%SystemRoot%</code>).</li><li><strong>Current working directory</strong> (depends on SafeDllSearchMode; if &quot;safe search mode&quot; is enabled, the current directory&#39;s position is pushed further down).</li><li><strong>Directories listed in the PATH environment variable</strong> (in order).</li><li><strong>If application configuration or Side-by-side (SxS)/manifest features are enabled</strong>, it prioritizes resolving the binding version declared in the manifest or the side-by-side assembly from WinSxS.</li></ol><p>The key takeaway is: <strong>if you use an absolute path or a path relative to the executable, the system will not search the PATH</strong>; conversely, if you only provide a bare name like <code>foo.dll</code>, it will attempt the order above.</p>`,48)])])}const m=i(n,[["render",r]]);export{u as __pageData,m as default};
