import{_ as t,o as n,c as a,a2 as s}from"./chunks/framework.CZcFxxdw.js";const u=JSON.parse('{"title":"In-depth Deconstruction: MSVC Debugging Mechanisms and Visual Studio Debugger Principles","description":"","frontmatter":{"title":"In-depth Deconstruction: MSVC Debugging Mechanisms and Visual Studio Debugger Principles","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":1,"order":8},"headers":[],"relativePath":"en/vol7-engineering/msvc-debugging-internals.md","filePath":"en/vol7-engineering/msvc-debugging-internals.md","lastUpdated":null}'),o={name:"en/vol7-engineering/msvc-debugging-internals.md"};function i(r,e,l,h,g,d){return n(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="deep-dive-msvc-debugging-mechanisms-and-visual-studio-debugger-principles" tabindex="-1">Deep Dive: MSVC Debugging Mechanisms and Visual Studio Debugger Principles <a class="header-anchor" href="#deep-dive-msvc-debugging-mechanisms-and-visual-studio-debugger-principles" aria-label="Permalink to &quot;Deep Dive: MSVC Debugging Mechanisms and Visual Studio Debugger Principles&quot;">​</a></h1><p>I&#39;ve been working on some Windows projects at home recently. Given the large scale of these projects, I found myself dealing with MSVC debugging-related topics. I&#39;d like to share what I&#39;ve learned over the past few days, combined with MSVC&#39;s official documentation.</p><p>We sometimes have to admit that Visual Studio can be a bit clunky to use (especially when a project gets large—it can be quite painful, as VS is heavy), but its debugging capabilities are solid. I imagine many of you rely on debugging to solve issues in your own projects. That&#39;s the starting point of this blog post—taking a fresh look at debugging, specifically MSVC debugging.</p><h2 id="starting-with-what-debugging-is" tabindex="-1">Starting with What Debugging Is <a class="header-anchor" href="#starting-with-what-debugging-is" aria-label="Permalink to &quot;Starting with What Debugging Is&quot;">​</a></h2><p>I think it&#39;s important to agree on the fundamental concept of &quot;debugging&quot; first. We usually say a program has a bug and you need to debug it. Here, debugging means taking a snapshot to inspect the program&#39;s state at a given point in time. For example, when I was working on the IMX6ULL Desktop, I encountered an illegal sensor data crash that was discovered through remote debugging.</p><p>Formally speaking, debugging is a <strong>&quot;god-mode&quot; observation and control technique for running programs</strong>. It attempts to achieve three things:</p><ol><li><strong>Observation</strong>: Viewing memory, registers, variable values, thread states, and call stacks without altering the program logic.</li><li><strong>Control</strong>: Taking over CPU execution. This includes suspending, single-stepping, resuming, and modifying memory or variable values.</li><li><strong>Mapping</strong>: Translating obscure <strong>machine code</strong> and <strong>memory addresses</strong> back into human-readable <strong>source code</strong> in real time.</li></ol><p><strong>In a nutshell</strong>: Debugging is the process of using privileged interfaces provided by the operating system to forcefully intervene in a target process, making it run according to the developer&#39;s will and exposing its internal state.</p><hr><h2 id="the-participants" tabindex="-1">The Participants <a class="header-anchor" href="#the-participants" aria-label="Permalink to &quot;The Participants&quot;">​</a></h2><p>When we press F5 in Visual Studio, it&#39;s not just a single program at work, but a complex <strong>multi-process collaborative system</strong>. Let&#39;s take a look at who is involved in our debugging system.</p><p>As the active party, we are responsible for clicking the GUI interface provided by the Visual Studio IDE (The Shell) to issue commands. But I must state one thing clearly: VS does <strong>not</strong> handle the actual debugging logic; it only handles the <strong>display</strong>. It converts user click actions (like F10) into commands sent to the debug engine.</p><p>A crucial component is the Debug Engine (DE). It is responsible for parsing complex C++ expressions (like <code>vec[0].m_data</code>), reading PDB symbol files, and translating addresses <code>0x00401234</code> into <code>main.cpp:20</code> (somewhat similar to <code>addr2line</code> in the GNU toolchain).</p><p><code>msvsmon.exe</code> (Remote Debugging Monitor) is the executor, agent, and isolation layer. We know that during debugging, our IDE process spawns this debugging process. The role of <code>msvsmon</code> is to ensure that if the target program crashes or hangs, it doesn&#39;t cause the VS IDE to crash. At the same time, <code>msvsmon</code> is responsible for passing data between the IDE and the target process. It is the &quot;person&quot; that actually calls Windows APIs to control the target process.</p><p>We&#39;ll skip over the role of the Windows kernel here; it simply provides the debugging-related System APIs.</p><p>The PDB file (Program Database) is the static database connecting the &quot;binary world&quot; and the &quot;source code world.&quot; Without it, the debugger is &quot;blind&quot; and can only see assembly code. Therefore, we must have a PDB file to debug; otherwise, VS will tell you that no symbols have been loaded (for example, in Release mode).</p><hr><h2 id="how-does-msvc-debug-the-workflow" tabindex="-1">How Does MSVC Debug? (The Workflow) <a class="header-anchor" href="#how-does-msvc-debug-the-workflow" aria-label="Permalink to &quot;How Does MSVC Debug? (The Workflow)&quot;">​</a></h2><h4 id="phase-1-establishment" tabindex="-1">Phase 1: Establishment <a class="header-anchor" href="#phase-1-establishment" aria-label="Permalink to &quot;Phase 1: Establishment&quot;">​</a></h4><p>During remote debugging, everything begins with <strong>interaction between the debugger and the host system</strong>. Specifically, Visual Studio initiates a request through the Remote Debugging Monitor (<code>msvsmon.exe</code>), calling the crucial Win32 API — <code>CreateProcess</code>. During this call, a vital flag, <code>DEBUG_ONLY_THIS_PROCESS</code> (or <code>DEBUG_PROCESS</code>), is passed in. This flag is not just a startup instruction, but a &quot;declaration of takeover&quot; issued to the operating system, marking the target process as controlled from the moment of its creation.</p><p>Next, the process enters the <strong>kernel-level binding and handshake phase</strong>. When the Windows kernel receives a creation request with the debug flag, it doesn&#39;t merely spawn an independent process. Instead, it establishes a parent-child or debugging association between the target program (Debuggee) and the debugger process (<code>msvsmon</code>) within its kernel data structures. This deep binding ensures that all events generated by the target process—such as exceptions, thread creations, or module loads—are fed back to the debugger in real time through a specific debug channel, allowing the debugger to grasp the complete lifecycle of the target program.</p><p>Finally, there is the <strong>pre-execution suspension and takeover phase</strong>. To ensure developers don&#39;t miss a single line of code, the target process does not immediately jump to the <code>main</code> function or the user entry point to execute after initialization is complete. Instead, after the loader finishes its preliminary work, the operating system automatically places the main thread of the target process in a <strong>Suspend</strong> state. At this point, the target program is like a car with the engine running but the brakes firmly applied, quietly waiting for further instructions from the debugger. Only when the debugger has finished preparations like symbol loading and breakpoint setting, and issues a &quot;continue&quot; command, will the target program truly begin executing its business logic.</p><p>This section reveals the black-box mechanism through which the debugger truly &quot;takes control&quot; of the target process. I have organized these core logics into a more professional and logical narrative:</p><hr><h4 id="phase-2-the-debug-loop-—-the-core-scheduling-heart" tabindex="-1">Phase 2: The Debug Loop — The Core Scheduling Heart <a class="header-anchor" href="#phase-2-the-debug-loop-—-the-core-scheduling-heart" aria-label="Permalink to &quot;Phase 2: The Debug Loop — The Core Scheduling Heart&quot;">​</a></h4><p>The operation of a debugger is essentially an efficient and rigorous <strong>self-looping monitoring system</strong>. When the debugger enters its working state, it maintains a resident <code>While Loop</code>, whose central hub is the <code>WaitForDebugEvent</code> API. At this point, the debugger enters an &quot;efficiently blocked&quot; state, silently waiting for signals triggered by any disturbance in the target process.</p><p>Once the target process triggers a key event—whether it&#39;s a module load (DLL Load), thread creation, or the breakpoint trigger that developers care about most—the <strong>Windows kernel automatically intervenes</strong>. The kernel instantly freezes all threads in the target process, packages the live environment into structured event information, and passes it to the debugger. The debugger then &quot;wakes up&quot; and executes corresponding logic based on the event type: loading symbol files (PDB) to align with the source code, or handling the <code>EXCEPTION_BREAKPOINT</code> exception. Finally, when the developer finishes inspecting and commands to continue, the debugger calls <code>ContinueDebugEvent</code>, requesting the kernel to resume the threads and bringing the program back to &quot;life.&quot;</p><h4 id="phase-3-breakpoint-injection-and-instruction-level-control" tabindex="-1">Phase 3: Breakpoint Injection and Instruction-Level Control <a class="header-anchor" href="#phase-3-breakpoint-injection-and-instruction-level-control" aria-label="Permalink to &quot;Phase 3: Breakpoint Injection and Instruction-Level Control&quot;">​</a></h4><ul><li><strong>Software Breakpoints (INT 3):</strong> When you click the red dot on the left side of a line of code, the debugger is actually &quot;tampering&quot; with the corresponding address in the target memory. It replaces the first byte of the original instruction at that location with <code>0xCC</code> (the <code>INT 3</code> instruction). When the CPU executes to this point, it forcibly triggers an interrupt exception, handing control over to the debugger.</li><li><strong>Single Stepping:</strong> To achieve &quot;line-by-line execution,&quot; the debugger utilizes the CPU hardware-level <strong>Trap Flag (TF)</strong>. By setting the TF in the flags register to 1, the CPU enters single-step mode: after executing each machine instruction, it automatically generates a <code>SINGLE_STEP</code> exception and suspends. It is through this &quot;execute one beat, pause one beat&quot; rhythm that the debugger achieves microscopic observation of code execution details.</li></ul><h4 id="phase-4-termination" tabindex="-1">Phase 4: Termination <a class="header-anchor" href="#phase-4-termination" aria-label="Permalink to &quot;Phase 4: Termination&quot;">​</a></h4><p>When the debugging task ends, the debugger provides two graceful exit methods. The most common is <strong>complete termination</strong>, which cleanly ends the target process&#39;s lifecycle by calling <code>TerminateProcess</code>. The other is the <strong>Detach</strong> mode: by calling <code>DebugActiveProcessStop</code>, the debugger undoes all memory modifications (such as restoring the replaced <code>0xCC</code> byte) and releases the kernel binding. At this point, the target process shakes off its restraints, returns to an independent running state, and continues executing without disturbing the business logic.</p><h2 id="the-big-picture" tabindex="-1">The Big Picture <a class="header-anchor" href="#the-big-picture" aria-label="Permalink to &quot;The Big Picture&quot;">​</a></h2><p>To help blog readers understand, you can envision an architecture diagram like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span>[ 开发者 (User) ]</span></span>
<span class="line"><span>       | 交互 (F5, F10, 查看变量)</span></span>
<span class="line"><span>       v</span></span>
<span class="line"><span>[ Visual Studio IDE (UI层) ]</span></span>
<span class="line"><span>       | 发送指令</span></span>
<span class="line"><span>       v</span></span>
<span class="line"><span>[ 调试引擎 (Debug Engine) ] &lt;----读取----&gt; [ PDB 符号文件 ]</span></span>
<span class="line"><span>       | 跨进程通讯 (RPC)</span></span>
<span class="line"><span>       v</span></span>
<span class="line"><span>[ msvsmon.exe (调试监视器) ]</span></span>
<span class="line"><span>       | 调用 Win32 Debug API</span></span>
<span class="line"><span>       v</span></span>
<span class="line"><span>[ Windows Kernel (操作系统内核) ]</span></span>
<span class="line"><span>       | 控制 / 捕获异常</span></span>
<span class="line"><span>       v</span></span>
<span class="line"><span>[ 目标进程 (App.exe) ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><hr><h2 id="the-cornerstone-of-debugging-the-build-process-symbols" tabindex="-1">The Cornerstone of Debugging: The Build Process &amp; Symbols <a class="header-anchor" href="#the-cornerstone-of-debugging-the-build-process-symbols" aria-label="Permalink to &quot;The Cornerstone of Debugging: The Build Process &amp; Symbols&quot;">​</a></h2><p>Debugging doesn&#39;t start with F5; it starts with compilation. That&#39;s why we need to build in Debug mode for debugging—otherwise, the lack of debug symbols makes things very troublesome.</p><h4 id="the-map-and-guide-of-debugging-pdb-and-compiler-configuration" tabindex="-1">The &quot;Map&quot; and &quot;Guide&quot; of Debugging: PDB and Compiler Configuration <a class="header-anchor" href="#the-map-and-guide-of-debugging-pdb-and-compiler-configuration" aria-label="Permalink to &quot;The &quot;Map&quot; and &quot;Guide&quot; of Debugging: PDB and Compiler Configuration&quot;">​</a></h4><p>If the binary file is a maze, then the <strong>PDB (Program Database)</strong> is the map to that maze. It is not a simple auxiliary file, but a complex database that records the mapping between machine code addresses and source code line numbers, variable names, type definitions, and the FPO data required for backtraces.</p><p>When a program crashes at address <code>0x00401000</code>, the debugger doesn&#39;t know what happened there. It quickly searches the PDB file and, through the mapping table, discovers that this address corresponds to line 15 of <code>main.cpp</code>. It is precisely through this <strong>symbolication</strong> process that the debugger can translate raw register states into code contexts that developers can understand.</p><p>To ensure the accuracy of this map, <strong>compiler flags</strong> are crucial:</p><ul><li><strong><code>/Zi</code> or <code>/ZI</code></strong>: Forcibly generate PDB debug information, where <code>/ZI</code> specifically reserves extra padding space for &quot;Edit and Continue.&quot;</li><li><strong><code>/Od</code> (Disable Optimization)</strong>: This is the soul of Debug mode. When optimizing (<code>/O2</code>), the compiler will reorder instructions or inline functions for performance, causing the binary stream to become completely misaligned with source code line numbers. Disabling optimization ensures a &quot;what you see is what you get&quot; debugging experience.</li></ul><hr><h2 id="breakpoints-evaluation-and-hot-patching" tabindex="-1">Breakpoints, Evaluation, and Hot Patching <a class="header-anchor" href="#breakpoints-evaluation-and-hot-patching" aria-label="Permalink to &quot;Breakpoints, Evaluation, and Hot Patching&quot;">​</a></h2><h4 id="_1-breakpoint-implementation-software-vs-hardware" tabindex="-1">1. Breakpoint Implementation: Software vs. Hardware <a class="header-anchor" href="#_1-breakpoint-implementation-software-vs-hardware" aria-label="Permalink to &quot;1. Breakpoint Implementation: Software vs. Hardware&quot;">​</a></h4><ul><li><strong>Software Breakpoints (INT 3)</strong>: When you press F9, the debugger performs a &quot;bait and switch.&quot; It replaces the first byte of the instruction at the breakpoint with <code>0xCC</code>. When the CPU hits this byte, it triggers an interrupt and transfers control to the operating system, which then notifies the debugger.</li><li><strong>Hardware Breakpoints</strong>: Implemented through the CPU&#39;s dedicated <strong>debug registers (Dr0 - Dr7)</strong>. They don&#39;t require modifying memory and are typically used to monitor variable changes (data breakpoints).</li></ul><h4 id="_2-expression-evaluator-ee-a-miniature-compilation-system" tabindex="-1">2. Expression Evaluator (EE): A Miniature Compilation System <a class="header-anchor" href="#_2-expression-evaluator-ee-a-miniature-compilation-system" aria-label="Permalink to &quot;2. Expression Evaluator (EE): A Miniature Compilation System&quot;">​</a></h4><p>When you type <code>ptr-&gt;member</code> in the Watch window, the <strong>Expression Evaluator</strong> inside VS immediately springs into action. It combines type information from the PDB to calculate memory offsets, directly reads the target process&#39;s memory addresses, and formats the result into a human-readable structure.</p><h4 id="_3-edit-and-continue-hot-patching-technology" tabindex="-1">3. Edit and Continue: Hot Patching Technology <a class="header-anchor" href="#_3-edit-and-continue-hot-patching-technology" aria-label="Permalink to &quot;3. Edit and Continue: Hot Patching Technology&quot;">​</a></h4><p>This is a highly challenging feature. When you modify code, VS performs an <strong>incremental compilation</strong> in the background, generating new binary fragments. Through &quot;Hot Patching&quot; technology, it modifies the original function&#39;s entry point into a jump instruction (JMP) pointing to the newly generated memory address, thereby achieving code updates without restarting the program (I&#39;ve tried it and found that it doesn&#39;t always work well and can sometimes fail).</p><hr><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><p>Note that these are some common problems encountered during debugging, which I&#39;ve summarized and listed here:</p><ol><li><strong>&quot;Breakpoint will not currently be hit&quot; (Hollow circle breakpoint)</strong>: <ul><li><strong>Cause</strong>: The PDB does not match the source code, or the PDB is not loaded.</li><li><strong>Solution</strong>: Check the &quot;Modules&quot; window for symbol loading status; ensure the code hasn&#39;t been optimized away.</li></ul></li><li><strong>Variable displays &quot;Variable is optimized away&quot;</strong>: <ul><li><strong>Cause</strong>: In Release mode, the variable might be stored in a register for reuse, or directly eliminated by constant folding.</li></ul></li><li><strong>Stack Corruption</strong>: <ul><li>The debugger cannot backtrace the stack. This is usually because a buffer overflow has overwritten the return address.</li></ul></li></ol>`,54)])])}const p=t(o,[["render",i]]);export{u as __pageData,p as default};
