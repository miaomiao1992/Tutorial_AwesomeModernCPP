import{_ as s,o as i,c as t,a2 as a}from"./chunks/framework.CZcFxxdw.js";const u=JSON.parse('{"title":"Part 2: Project Structure — HAL Library Acquisition, Startup File Pitfalls, and Directory Setup","description":"","frontmatter":{"title":"Part 2: Project Structure — HAL Library Acquisition, Startup File Pitfalls, and Directory Setup","description":"","tags":["beginner","cpp-modern","stm32f1"],"difficulty":"beginner","platform":"stm32f1","chapter":14,"order":2},"headers":[],"relativePath":"en/vol8-domains/embedded/00-env-setup/02-project-structure.md","filePath":"en/vol8-domains/embedded/00-env-setup/02-project-structure.md","lastUpdated":null}'),n={name:"en/vol8-domains/embedded/00-env-setup/02-project-structure.md"};function l(r,e,o,h,d,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="part-2-project-structure-—-fetching-the-hal-library-startup-file-pitfalls-and-directory-setup" tabindex="-1">Part 2: Project Structure — Fetching the HAL Library, Startup File Pitfalls, and Directory Setup <a class="header-anchor" href="#part-2-project-structure-—-fetching-the-hal-library-startup-file-pitfalls-and-directory-setup" aria-label="Permalink to &quot;Part 2: Project Structure — Fetching the HAL Library, Startup File Pitfalls, and Directory Setup&quot;">​</a></h1><blockquote><p>In the previous part, we installed the toolchain. Now let&#39;s set up the project skeleton. This post documents my entire process of fetching the STM32 HAL library, including that baffling nested submodule issue, the hidden logic behind startup file naming conventions, and the hidden pitfalls in <code>stm32f1xx_hal_conf.h</code> that will crash your build halfway through.</p></blockquote><hr><h2 id="why-this-step-matters" tabindex="-1">Why This Step Matters <a class="header-anchor" href="#why-this-step-matters" aria-label="Permalink to &quot;Why This Step Matters&quot;">​</a></h2><p>You might ask, isn&#39;t it just a project structure? Can&#39;t we just create a few folders, toss the HAL library in, and call it a day? Not quite. The STM32 HAL library has its own &quot;ecosystem&quot; — the CMSIS core layer, the HAL driver layer, startup code, and linker scripts. These must be organized in a specific way, or the compiler won&#39;t know where to find the header files, and the linker won&#39;t know where to place the code in memory.</p><p>To make matters worse, ST&#39;s official HAL library is published via a Git repository, and it contains nested submodules. If you clone it the usual way, chances are you&#39;ll miss critical files. When your build fails halfway through complaining about a missing header file, tracing back to find the root cause is incredibly painful. I&#39;ve stumbled on this myself, so in this post, I&#39;ll flag all the pitfalls upfront so we can get the project skeleton right on the first try.</p><hr><h2 id="understanding-the-three-layer-architecture-of-the-hal-library" tabindex="-1">Understanding the Three-Layer Architecture of the HAL Library <a class="header-anchor" href="#understanding-the-three-layer-architecture-of-the-hal-library" aria-label="Permalink to &quot;Understanding the Three-Layer Architecture of the HAL Library&quot;">​</a></h2><p>Before we download any code, it&#39;s worth understanding how ST&#39;s HAL library is layered. This will help you understand why we need to create those directories and what each file does.</p><p>At the bottom is <strong>CMSIS-Core (Cortex Microcontroller Software Interface Standard)</strong>. This is a standard defined by ARM that specifies register access interfaces for the Cortex-M series cores. Simply put, CMSIS-Core tells you &quot;this chip has a register called SCB at address 0xE000ED00,&quot; so you can manipulate registers using <code>SCB-&gt;AIRCR = ...</code> instead of memorizing magic numbers. CMSIS-Core is maintained by ARM and is common to all Cortex-M chips.</p><p>The middle layer is <strong>CMSIS-Device</strong>. This is ST&#39;s specialization for the STM32F1 series. It defines what peripherals the specific F103C8T6 chip has, how many of each peripheral exist, and where their register addresses are. For example, the base address of <code>GPIOA</code> is <code>0x40010800</code>, and this information lives in the CMSIS-Device header files. You&#39;ll see a bunch of <code>stm32f103xb.h</code> files later — they belong to this layer.</p><p>The top layer is the <strong>HAL driver layer</strong>. This is a set of peripheral driver APIs written in C by ST, such as <code>HAL_GPIO_WritePin()</code> and <code>HAL_UART_Transmit()</code>. Their purpose is to abstract away low-level register operations, letting you interact with different STM32 series in a uniform way. In theory, code you write using HAL should require only minor configuration changes when porting to an STM32F4.</p><p>Above that is your application code. The application code calls HAL APIs, HAL calls CMSIS-Device definitions, and CMSIS-Device depends on the CMSIS-Core kernel interfaces. Once you understand this layering, you&#39;ll know why we need so many directories — each layer has its own dedicated folder.</p><hr><h2 id="fetching-the-hal-library-the-submodule-trap" tabindex="-1">Fetching the HAL Library: The Submodule Trap <a class="header-anchor" href="#fetching-the-hal-library-the-submodule-trap" aria-label="Permalink to &quot;Fetching the HAL Library: The Submodule Trap&quot;">​</a></h2><p>Alright, let&#39;s fetch the code. ST&#39;s official STM32F1 HAL library is hosted on GitHub at <code>https://github.com/STMicroelectronics/STM32CubeF1.git</code>. Your first instinct might be to simply <code>git clone</code> it, but there&#39;s a catch here. Let me walk you through it step by step.</p><p>First, let&#39;s create our project root directory. I like to keep all dependencies under a <code>vendor/</code> directory for a clean project structure:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> embedded-stm32/vendor</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> embedded-stm32</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Now let&#39;s clone the HAL library. Here&#39;s a mistake beginners make most often — doing a shallow clone with <code>--depth 1</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> submodule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --depth</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/STMicroelectronics/STM32CubeF1.git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command looks reasonable: it adds the library as a submodule, and <code>--depth 1</code> fetches only the latest version to save time. But the problem is that the STM32CubeF1 repository itself has internal submodules (the CMSIS library is brought in as a submodule), and <code>--depth 1</code> prevents nested submodules from being initialized correctly.</p><p>When you check the directory structure later, you&#39;ll notice a bizarre phenomenon:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1/Drivers/CMSIS/Device/ST/STM32F1xx/Source/Templates/gcc/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Normally, this directory should contain a bunch of startup files (like <code>startup_stm32f103xb.s</code>), but if you used a shallow clone, it will be empty. During compilation, you&#39;ll see an error like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fatal error: startup_stm32f103xb.s: No such file or directory</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>When you go back to investigate why the files are missing, you&#39;ll be completely baffled — the submodule was clearly added, so why are the files still missing?</p><p>The reason lies in Git&#39;s submodule mechanism. When you clone a repository containing submodules, Git only fetches the outer repository&#39;s content. The submodule directories inside are just &quot;pointers&quot; referencing a specific commit in another repository. You need to explicitly run <code>git submodule update --init --recursive</code> for Git to actually fetch those nested submodule contents. The <code>--depth 1</code> shallow clone breaks this mechanism because the history of nested submodules isn&#39;t fully fetched.</p><p>The correct approach is to do a full clone, then recursively initialize all submodules:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> submodule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/STMicroelectronics/STM32CubeF1.git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> submodule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --recursive</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>If you&#39;ve already added the submodule to your project but forgot to use <code>--recursive</code>, you can fix it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> submodule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --recursive</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>This command recursively fetches all nested submodules, ensuring the CMSIS Device directory files are complete. You can verify with the <code>ls</code> command we used earlier to check if the startup files have appeared:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1/Drivers/CMSIS/Device/ST/STM32F1xx/Source/Templates/gcc/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>You should see output like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>startup_stm32f100xb.s  startup_stm32f103xe.s  startup_stm32f105xc.s</span></span>
<span class="line"><span>startup_stm32f101xe.s  startup_stm32f103xg.s  startup_stm32f107xc.s</span></span>
<span class="line"><span>startup_stm32f101x6.s  startup_stm32f103xg.s  startup_stm32f107xc.s</span></span>
<span class="line"><span>startup_stm32f102x6.s  startup_stm32f103xb.s</span></span>
<span class="line"><span>startup_stm32f103x6.s  startup_stm32f103xd.s</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Seeing these <code>startup_stm32f103*.s</code> files means the submodule was fetched successfully. By the way, if you&#39;re on Arch Linux, your system might not have <code>git</code> preinstalled, so you&#39;ll need to run <code>sudo pacman -S git</code> first; Ubuntu users typically have git installed by default.</p><hr><h2 id="the-hidden-logic-behind-startup-file-naming" tabindex="-1">The Hidden Logic Behind Startup File Naming <a class="header-anchor" href="#the-hidden-logic-behind-startup-file-naming" aria-label="Permalink to &quot;The Hidden Logic Behind Startup File Naming&quot;">​</a></h2><p>Now we have the startup files, but a new problem arises — which one should we use?</p><p>Here&#39;s a detail that trips up countless beginners. Many online tutorials reference <code>startup_stm32f103c8.s</code>, but if you look closely at the <code>ls</code> output from earlier, you&#39;ll notice there is no such file! ST&#39;s official filename is <code>startup_stm32f103xb.s</code>.</p><p>Behind this discrepancy lies ST&#39;s chip naming convention. Let me explain: what does the &quot;C8&quot; in the F103C8T6 model number mean? C stands for Low-density, and 8 represents 64KB Flash. But ST&#39;s startup file naming convention isn&#39;t based on Flash size — it&#39;s based on &quot;density category&quot;:</p><ul><li><code>ld</code> = Low-density devices (16-32KB Flash)</li><li><code>md</code> = Medium-density devices (64-128KB Flash)</li><li><code>hd</code> = High-density devices (256-512KB Flash)</li><li><code>xl</code> = XL-density devices (768KB-1MB Flash)</li></ul><p>The F103C8T6 has 64KB Flash, which falls under medium-density, so the corresponding startup file is <code>startup_stm32f103xb.s</code>. The &quot;B&quot; here isn&#39;t the hexadecimal for 8, but rather an internal density code used by ST.</p><p>Correspondingly, for the compile-time macro definition, you need to pass <code>STM32F103xB</code> (note the uppercase B). Many tutorials incorrectly write <code>STM32F103xC8</code>, which causes the conditional compilation in the header files to select the wrong branch, resulting in code that doesn&#39;t match your hardware.</p><p>You might ask, why does ST use such a complex naming scheme? Historical reasons. The STM32F1 series was ST&#39;s first Cortex-M3 product line, and at the time, they divided it into several tiers based on Flash capacity. F103xB covers both the 64KB and 128KB versions, which are hardware-identical apart from Flash size, so they share the same startup file and header files.</p><p>So what does the startup file actually do? Simply put, it&#39;s the first piece of code executed after the chip resets. When the STM32 powers on or resets, the CPU reads the &quot;initial stack pointer&quot; from address 0x00000000, then reads the &quot;reset vector&quot; (Reset Handler) from 0x00000004 and jumps there to execute. The startup file defines this Vector Table, which contains the entry addresses for all interrupts and exceptions. It also handles initializing the <code>.data</code> section (copying initial values from Flash to RAM) and zeroing the <code>.bss</code> section, before finally jumping to your <code>main()</code> function. Without the startup file, the chip wouldn&#39;t know what to do after a reset, and the program couldn&#39;t run.</p><hr><h2 id="project-directory-structure" tabindex="-1">Project Directory Structure <a class="header-anchor" href="#project-directory-structure" aria-label="Permalink to &quot;Project Directory Structure&quot;">​</a></h2><p>Now that we have the HAL library and understand the startup files, let&#39;s set up a clean project structure. I recommend this layout:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>embedded-stm32/</span></span>
<span class="line"><span>├── CMakeLists.txt</span></span>
<span class="line"><span>├── src/</span></span>
<span class="line"><span>│   ├── main.c</span></span>
<span class="line"><span>│   ├── stm32f1xx_hal_conf.h</span></span>
<span class="line"><span>│   └── stm32f1xx_it.c</span></span>
<span class="line"><span>├── vendor/</span></span>
<span class="line"><span>│   └── STM32CubeF1/</span></span>
<span class="line"><span>├── build/</span></span>
<span class="line"><span>└── ld/</span></span>
<span class="line"><span>    └── STM32F103C8Tx_FLASH.ld</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>Let me explain the purpose of each directory:</p><p><code>vendor/STM32CubeF1/</code> is the HAL library we just cloned. You don&#39;t need to manually modify anything in this directory — just reference it. The CMSIS and HAL_Driver inside will be added to the compilation path through CMake&#39;s <code>target_include_directories</code>.</p><p><code>src/</code> holds your application code. <code>main.c</code> is the program entry point, <code>stm32f1xx_hal_conf.h</code> is the HAL library configuration file (we&#39;ll dive into its pitfalls below), and <code>stm32f1xx_it.c</code> is for interrupt service routines. Certain HAL peripherals (like UART) require user-defined interrupt handler functions, and these go in <code>stm32f1xx_it.c</code>.</p><p><code>build/</code> is the CMake output directory. We use an &quot;out-of-source&quot; build approach to avoid polluting the source directory with generated files. Build artifacts (<code>.o</code> files, <code>.elf</code>, <code>.hex</code>, <code>.bin</code>) will all end up here.</p><p><code>ld/</code> stores the linker script. We&#39;ll cover how to write this file in detail in the next post; for now, just know that it defines the memory layout.</p><p>You might notice I used <code>STM32F103C8Tx_FLASH.ld</code> as the linker script name. There&#39;s no hard rule for this naming, but I like to embed the chip model in the filename so I can tell at a glance which chip it&#39;s for. The only difference between the F103C8 and F103CB (128KB version) is the Flash size — you just change the <code>LENGTH</code> parameter in the linker script, and everything else stays the same.</p><hr><h2 id="stm32f1xx-hal-conf-h-the-hidden-pitfalls" tabindex="-1">stm32f1xx_hal_conf.h: The Hidden Pitfalls <a class="header-anchor" href="#stm32f1xx-hal-conf-h-the-hidden-pitfalls" aria-label="Permalink to &quot;stm32f1xx_hal_conf.h: The Hidden Pitfalls&quot;">​</a></h2><p>Now we arrive at the first &quot;minefield&quot; — the HAL configuration file. ST&#39;s official HAL library doesn&#39;t include a ready-to-use <code>stm32f1xx_hal_conf.h</code>; it only provides a <code>stm32f1xx_hal_conf_template.h</code> template. You need to copy the template into your project, rename it, and modify it.</p><p>Why not use CubeMX? If you use ST&#39;s STM32CubeMX graphical tool to generate a project, it automatically generates this file for you. But since we&#39;re taking the &quot;pure hand-written CMake&quot; route, we have to handle it manually.</p><p>First, copy the template over:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vendor/STM32CubeF1/Drivers/STM32F1xx_HAL_Driver/Inc/stm32f1xx_hal_conf_template.h</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/stm32f1xx_hal_conf.h</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then open the file in your editor and start modifying. The first pitfall is <strong>module selection</strong>. Near the top of the file, there&#39;s a huge block of <code>#define HAL_XXX_MODULE_ENABLED</code>, with all modules enabled by default. This causes all HAL drivers to be compiled in, bloating the firmware size significantly. For our LED blink program, we only need to enable these modules:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HAL_GPIO_MODULE_ENABLED</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HAL_RCC_MODULE_ENABLED</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HAL_CORTEX_MODULE_ENABLED</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Comment out the <code>#define</code> for all other modules. This way, the compiler only compiles the HAL functions you need, and the linker can do a better job of dead code elimination.</p><p>The second pitfall is <strong>clock macro definitions</strong>. Scroll down a few lines and you&#39;ll see a bunch of macros like <code>HSE_VALUE</code>, <code>HSI_VALUE</code>, and <code>LSI_VALUE</code>. These represent external/internal crystal frequencies, and the HAL library&#39;s RCC module needs to know these frequencies to calculate the system clock.</p><p>The most critical one is <code>HSE_VALUE</code>. In the template file, this macro is conditionally defined with <code>#if !defined(HSE_VALUE)</code>. If you don&#39;t define this macro, compiling certain HAL modules (like RTC or the watchdog) will throw errors:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>error: &#39;HSE_VALUE&#39; undeclared (first use in this function)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The solution is simple: make sure all clock macros are defined in <code>stm32f1xx_hal_conf.h</code>. The Blue Pill board typically uses an 8MHz external crystal (HSE), the internal high-speed oscillator (HSI) is 8MHz, the internal low-speed oscillator (LSI) is approximately 40kHz, and the external low-speed crystal (LSE) is usually 32.768kHz (if present on the board). Write them all out:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HSE_VALUE</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    8000000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">U</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HSI_VALUE</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    8000000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">U</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LSI_VALUE</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    40000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">U</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LSE_VALUE</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    32768</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">U</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Note that the unit is Hertz, and the uppercase <code>U</code> suffix denotes an &quot;unsigned integer.&quot; Getting these values right matters a lot — if <code>HSE_VALUE</code> is wrong, the system clock frequency calculated by RCC will be off, the UART baud rate will be wrong as well, and your serial output will be garbled.</p><p>The third pitfall is the <strong>assert_param macro</strong>. Near the end of the file, there&#39;s a macro definition like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  USE_FULL_ASSERT</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  #define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> assert_param</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">expr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) ((expr) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">U</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> assert_failed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__FILE__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__LINE__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> assert_failed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint32_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  #define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> assert_param</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">expr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) ((</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">U</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The HAL library uses <code>assert_param</code> everywhere to check whether function parameters are valid. For example, if you call <code>HAL_GPIO_WritePin()</code> with an invalid pin number, the assert will catch the error. If you define <code>USE_FULL_ASSERT</code>, a failed assert jumps to the <code>assert_failed()</code> function (which you need to implement yourself); otherwise, it does nothing (empty macro).</p><p>Many beginners forget to define <code>USE_FULL_ASSERT</code>, leading to compilation errors about an &quot;undefined macro.&quot; The fix: either add the code block above in <code>stm32f1xx_hal_conf.h</code> (it&#39;s already in the template, just make sure it&#39;s not commented out), or add <code>-DUSE_FULL_ASSERT</code> in CMake.</p><p>The fourth pitfall is the <strong>module callback macros</strong>. The second half of the file has a bunch of <code>HAL_XXX_REGISTER_CALLBACKS</code> definitions. These enable HAL&#39;s &quot;callback registration&quot; feature (a more flexible approach to interrupt handling). The default value is 0, and for simple applications, keeping it at 0 is fine. If you change it to 1, you&#39;ll need to implement callback functions for each peripheral, increasing code complexity.</p><p>One last detail: <code>stm32f1xx_hal_conf.h</code> must be findable by the HAL library header files. The usual approach is to place it in the <code>src/</code> directory, then add <code>src/</code> to the include path via CMake&#39;s <code>target_include_directories</code>. Alternatively, you can place it directly in the project root and specify it at compile time with <code>-I.</code>. The HAL library header files reference it via <code>#include &quot;stm32f1xx_hal_conf.h&quot;</code> (note the quotes, not angle brackets), so it must be in the search path.</p><hr><h2 id="the-template-file-pitfall-a-preview" tabindex="-1">The Template File Pitfall: A Preview <a class="header-anchor" href="#the-template-file-pitfall-a-preview" aria-label="Permalink to &quot;The Template File Pitfall: A Preview&quot;">​</a></h2><p>Before we wrap up, I want to give an early warning about a pitfall we&#39;ll encounter in the CMake post. If you feed the entire <code>Src/</code> directory of the HAL library directly to CMake for compilation, you&#39;ll get an error like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>multiple definition of &#39;SystemInit&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is because the HAL library contains several <code>template</code> files, such as <code>system_stm32f1xx.c</code>. These template files aren&#39;t meant to be compiled directly — they&#39;re meant for you to copy into your project and modify for your own implementation. If you compile them as-is, they&#39;ll conflict with your own implementation (both files define <code>SystemInit</code>).</p><p>The solution is to use CMake&#39;s <code>list(FILTER ... EXCLUDE ...)</code> to remove these template files from the source file list. I&#39;ll save the specific CMake syntax for the next post; for now, you just need to know: don&#39;t blindly add all <code>.c</code> files from the HAL library into compilation — the ones with the <code>template</code> suffix must be excluded.</p><hr><h2 id="where-we-are-now" tabindex="-1">Where We Are Now <a class="header-anchor" href="#where-we-are-now" aria-label="Permalink to &quot;Where We Are Now&quot;">​</a></h2><p>In this post, we finished setting up the project structure. You should now have:</p><ol><li>A correctly cloned HAL library (with all submodules initialized)</li><li>The knowledge that F103C8T6 requires the <code>startup_stm32f103xb.s</code> startup file and the <code>STM32F103xB</code> macro</li><li>A clean project directory layout</li><li>A properly configured <code>stm32f1xx_hal_conf.h</code> (clock macros and module selection are all set)</li></ol><p>But we&#39;re not done yet. In the next post, we&#39;ll cover the linker script and CMake configuration — that&#39;s what will actually get the code to compile. The linker script needs to tell the linker that the STM32F103C8T6&#39;s Flash starts at 0x08000000 with a size of 64KB, and RAM starts at 0x20000000 with a size of 20KB. If you get this file wrong, the program will compile but won&#39;t run, because the code will be placed at the wrong memory addresses.</p><p>Before that, you can go ahead and set up the project structure and copy and modify <code>stm32f1xx_hal_conf.h</code>. In the next article, we&#39;ll write the CMakeLists.txt and linker script, aiming to get you to compile your first <code>.elf</code> firmware file.</p>`,89)])])}const m=s(n,[["render",l]]);export{u as __pageData,m as default};
