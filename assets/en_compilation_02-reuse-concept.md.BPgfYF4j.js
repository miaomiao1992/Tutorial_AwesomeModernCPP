import{_ as i,o as s,c as a,a2 as t}from"./chunks/framework.BuBU3QrY.js";const n="/Tutorial_AwesomeModernCPP/assets/static_library.B7Dm5_Pr.png",r="/Tutorial_AwesomeModernCPP/assets/dynamic_library.BtGcOi9j.png",m=JSON.parse('{"title":"Deep Dive into C/C++ Compilation and Linking Techniques 2: Introduction to Shared and Static Libraries","description":"","frontmatter":{"title":"Deep Dive into C/C++ Compilation and Linking Techniques 2: Introduction to Shared and Static Libraries","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":13,"order":2,"translation":{"source":"documents/compilation/02-reuse-concept.md","source_hash":"c42b8e94395b08130fcd7932d2b44078655ee2f1875aa04d131816e753c571f9","translated_at":"2026-05-26T10:09:04.201407+00:00","engine":"anthropic","token_count":1658}},"headers":[],"relativePath":"en/compilation/02-reuse-concept.md","filePath":"en/compilation/02-reuse-concept.md","lastUpdated":null}'),l={name:"en/compilation/02-reuse-concept.md"};function o(d,e,h,c,p,k){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="deep-dive-into-c-c-compilation-and-linking-part-2-introduction-to-static-and-shared-libraries" tabindex="-1">Deep Dive into C/C++ Compilation and Linking Part 2: Introduction to Static and Shared Libraries <a class="header-anchor" href="#deep-dive-into-c-c-compilation-and-linking-part-2-introduction-to-static-and-shared-libraries" aria-label="Permalink to &quot;Deep Dive into C/C++ Compilation and Linking Part 2: Introduction to Static and Shared Libraries&quot;">​</a></h1><h2 id="what-is-reuse-and-how-does-it-relate-to-compilation-and-linking" tabindex="-1">What is reuse, and how does it relate to compilation and linking? <a class="header-anchor" href="#what-is-reuse-and-how-does-it-relate-to-compilation-and-linking" aria-label="Permalink to &quot;What is reuse, and how does it relate to compilation and linking?&quot;">​</a></h2><p>Reuse is everywhere, and I doubt anyone would disagree. The reuse we discuss here is the reutilization of code. We can already catch a glimpse of this in C++ programming:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AddType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AddType</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> a</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AddType</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">){</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 没有任何技巧的相加</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">trim_self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> waited_trim){</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // returns the copy of the trimmed string</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    size_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // left index</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> str.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)str[i]))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    size_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> j </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> str.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // right index</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (j </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)str[j </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  j</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> str.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(i, j);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> res </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // deduced as int</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    float</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> res2 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // deduced as floats</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>For example, the template code and function code above mean we don&#39;t have to copy code every time we call addition or compress whitespace in a string. Looking at it this way, code reuse has been around since the heyday of the C language. However, I would argue that this level of code reuse isn&#39;t very advanced—because it relies on source code distribution. In other words, to use our own past work or someone else&#39;s code masterpiece, we have to frantically dig up their source files, ensure all dependencies are in place, and add them to our project for compilation. I&#39;m sure you&#39;ve noticed the problem—in many cases, we simply cannot get the source code (trade secrets, those who know, know). In this situation, we naturally need to think about lower-level code reuse. That is binary-level distribution. This is the role of static and shared libraries, and it serves as the prerequisite for the next few sections dedicated to machine code distribution-level reuse mechanisms.</p><h2 id="what-is-a-static-library" tabindex="-1">What is a static library? <a class="header-anchor" href="#what-is-a-static-library" aria-label="Permalink to &quot;What is a static library?&quot;">​</a></h2><p>A static library might be much simpler than you think. We know that after the compiler finishes preprocessing and compiling source files, we get relocatable files. Previously, these relocatable files were directly combined into an executable. Now we can take a different approach: these common relocatable files can be assembled into a library on their own. The next time we look up symbols, we simply link against this library. This way, we hide the source code and can distribute it at the binary level. But this raises a question—how do we use it? We always need usable symbols to tell us the exact entry points. For instance, we know there is a function in the library that compresses whitespace in strings, but if we don&#39;t know what it&#39;s called, we can&#39;t use it. So it&#39;s obvious. Having just these binary files is completely insufficient; we need to meet another condition—exported header files for us to program against.</p><p>The following two diagrams illustrate the role of a static library quite well.</p><p><img src="`+n+'" alt="static_library"></p><p>However, this introduces a new problem. In reality, the code for <code>libfoo</code> is exactly the same, yet two copies exist. We don&#39;t always want this kind of hard copy. If <code>libfoo</code> is small, it&#39;s fine; hard drive capacity is relatively cheap these days, so we could call it a redundancy advantage. But in many other cases, if <code>libfoo</code> has an important security update and we want all software to reload it on the next startup, a static library seems powerless. Because it simply shifts distribution from the more difficult source code distribution to binary distribution. It doesn&#39;t solve the more important &quot;load when use&quot; problem at all. So it doesn&#39;t seem very elegant. In practice, static libraries aren&#39;t used all that widely (I personally rarely use them either).</p><h2 id="shared-libraries" tabindex="-1">Shared libraries <a class="header-anchor" href="#shared-libraries" aria-label="Permalink to &quot;Shared libraries&quot;">​</a></h2><p>So the problem lies in the fact that we perform a deep copy of all binary code, rather than a reference-level shallow copy. If we allow some symbols in the executable code to be lazily resolved at load time (which requires a loader that can dynamically load and modify the addresses of these undefined symbols to the actual shared symbol addresses), we naturally think—since we&#39;ve already reached the library level, let&#39;s take it a step further and turn this code into purely shareable code. When they are needed and available, we load them, and then all executable programs that need this library can smoothly use this shared code segment directly without clumsily copying their own version. This drastically saves our memory space. This sharing characteristic is also why we can say a shared library is a dynamic library (shared code inevitably requires dynamic loading to remap shared symbol addresses, so in this context, &quot;shared library&quot; and &quot;dynamic library&quot; are completely interchangeable—no one deliberately distinguishes between them today).</p><p>Of course, for the deeper characteristics of shared libraries—for example, to ensure that any executable needing this library can successfully load symbols from it, we compile all symbols using <code>-fPIC</code> (Position Independent Code). This makes relocation very convenient for the loader.</p><h2 id="overview-how-do-shared-libraries-actually-work" tabindex="-1">Overview: How do shared libraries actually work? <a class="header-anchor" href="#overview-how-do-shared-libraries-actually-work" aria-label="Permalink to &quot;Overview: How do shared libraries actually work?&quot;">​</a></h2><h3 id="building-a-shared-library-from-source-code-to-libfoo-so-versioned-libfoo-so-1-0" tabindex="-1">Building a shared library (from source code to <code>libfoo.so</code> / versioned <code>libfoo.so.1.0</code>) <a class="header-anchor" href="#building-a-shared-library-from-source-code-to-libfoo-so-versioned-libfoo-so-1-0" aria-label="Permalink to &quot;Building a shared library (from source code to `libfoo.so` / versioned `libfoo.so.1.0`)&quot;">​</a></h3><p>Goal: Generate a <code>.so</code> that can be dynamically loaded by clients and shared across multiple processes, with explicit ABI management (via SONAME/versioning).</p><p>The build process is factually almost identical to building an executable, except we don&#39;t add the startup headers. Beyond that, we need to ensure a few basic key points:</p><ul><li><strong>Must use Position Independent Code (PIC)</strong>: <code>-fPIC</code> (or <code>-fpic</code>) is used to generate code that can run at any address (function memory accesses use relative addresses or go through the GOT). Not using PIC will cause the linker/runtime to encounter relocation conflicts or non-relocatable segments.</li><li><strong>Use <code>-shared</code> to generate a shared object</strong>: The linker marks the type as a shared library (ELF type = DYN).</li><li><strong>Set the SONAME</strong>: Use the linker option <code>-Wl,-soname,libfoo.so.1</code> to specify the ABI name (clients record the SONAME in their DT_NEEDED). The actual file is usually <code>libfoo.so.1.0</code>, with symlinks <code>libfoo.so.1 -&gt; libfoo.so.1.0</code> and <code>libfoo.so -&gt; libfoo.so.1</code> provided (for convenience during development when using <code>-lfoo</code>).</li><li><strong>Control exported symbols (visibility / version script)</strong>: By default, all global symbols are exported. You can use GCC&#39;s <code>-fvisibility=hidden</code> + <code>__attribute__((visibility(&quot;default&quot;)))</code> to mark the interfaces that need exporting, or use a linker version script to control the symbol table, reducing API pollution and lowering the risk of symbol conflicts.</li><li><strong>Optional: Symbol versioning</strong>: Used to support different versions of symbols within the same SONAME, facilitating compatibility management (requires a linker version script).</li></ul><h3 id="building-the-client-executable-based-on-trusting-the-library-s-abi-soname" tabindex="-1">Building the client executable (based on &quot;trusting the library&#39;s ABI/SONAME&quot;) <a class="header-anchor" href="#building-the-client-executable-based-on-trusting-the-library-s-abi-soname" aria-label="Permalink to &quot;Building the client executable (based on &quot;trusting the library&#39;s ABI/SONAME&quot;)&quot;">​</a></h3><p>Here, &quot;trusting&quot; means that during the build process, the client trusts that the shared library&#39;s ABI/interface (header files, SONAME, symbol semantics) will not break its expectations. The relationship between the build phase and runtime, along with the generated ELF fields, is crucial.</p><h4 id="what-happens-at-link-time-building-the-client" tabindex="-1">What happens at link time (building the client) <a class="header-anchor" href="#what-happens-at-link-time-building-the-client" aria-label="Permalink to &quot;What happens at link time (building the client)&quot;">​</a></h4><ul><li>The client uses header file declarations (<code>foo.h</code>) and <code>-lfoo</code> to link against the corresponding shared library (or the library&#39;s development symlink <code>libfoo.so</code>).</li><li>The linker will: <ol><li>Merge the client&#39;s own code and object files into an executable (ELF type = EXEC or DYN (Position-Independent Executable)).</li><li><strong>Verify</strong>: Attempt to resolve undefined references (in the case of dynamic linking, the linker typically uses the dynamic symbol table of the specified shared library to satisfy these references; if not found, it throws an undefined reference error).</li><li><strong>Do not copy library code</strong>: Unlike static linking, the linker does not copy <code>.o</code> code into the executable. Instead, it records the dependency in <code>DT_NEEDED</code> (recording the library&#39;s SONAME) and generates the necessary relocations/PLT placeholders.</li></ol></li><li>Result: The executable contains dynamic section entries like <code>DT_NEEDED: libfoo.so.1</code>, but does not contain the library&#39;s implementation code.</li></ul><h3 id="runtime-loading-and-symbol-resolution-specific-behavior-of-the-dynamic-linker-loader" tabindex="-1">Runtime loading and symbol resolution (specific behavior of the dynamic linker / loader) <a class="header-anchor" href="#runtime-loading-and-symbol-resolution-specific-behavior-of-the-dynamic-linker-loader" aria-label="Permalink to &quot;Runtime loading and symbol resolution (specific behavior of the dynamic linker / loader)&quot;">​</a></h3><p>This is the most complex and critical part—the runtime <code>ld.so</code> (or the corresponding platform&#39;s loader) combines everything into a runnable process address space and resolves symbol references. Below is a detailed step-by-step and mechanism-based explanation.</p><h4 id="startup-phase-—-from-the-kernel-to-the-dynamic-linker" tabindex="-1">Startup phase — From the kernel to the dynamic linker <a class="header-anchor" href="#startup-phase-—-from-the-kernel-to-the-dynamic-linker" aria-label="Permalink to &quot;Startup phase — From the kernel to the dynamic linker&quot;">​</a></h4><ol><li><strong>Kernel loads the executable</strong>: The kernel reads the ELF header -&gt; if the <code>INTERP</code> segment exists in the ELF (which is the case for the vast majority of dynamic executables, with a value like <code>/lib64/ld-linux-x86-64.so.2</code>), the kernel first maps the dynamic linker into the process address space, then maps the executable&#39;s PT_LOAD segments, but does not directly run the executable&#39;s <code>_start</code>.</li><li><strong>Dynamic linker (ld.so) takes over</strong>: It is responsible for parsing <code>DT_NEEDED</code>, finding the actual library files, recursively loading dependencies and performing relocations, executing initializers (constructors), and finally handing control over to the executable&#39;s entry point (<code>_start</code> -&gt; <code>main</code>).</li></ol><h4 id="mapping-mmap-library-files" tabindex="-1">Mapping (mmap) library files <a class="header-anchor" href="#mapping-mmap-library-files" aria-label="Permalink to &quot;Mapping (mmap) library files&quot;">​</a></h4><ul><li>The loader reads the ELF Program Headers (PT_LOAD) of each dependent <code>.so</code>, mapping the executable segments (text) as read-execute and the data segments as read-write, etc. It also handles page alignment and segment protection (mmap + mprotect).</li><li>Each library is generally mapped only once (multiple processes can share the same physical pages, as long as the pages are read-only/shared).</li></ul><h4 id="relocations" tabindex="-1">Relocations <a class="header-anchor" href="#relocations" aria-label="Permalink to &quot;Relocations&quot;">​</a></h4><p>There are multiple types of relocations, falling into two important categories:</p><ul><li><strong>Relocations not requiring symbol lookup</strong> (e.g., RELATIVE type): These can be adjusted directly based on the base address (for position-independent code, the runtime adds the library&#39;s base address to the relative offset). They are usually processed in batches during the startup phase and are fast.</li><li><strong>Relocations requiring symbol lookup</strong> (e.g., R_X86_64_JUMP_SLOT / R_*_GLOB_DAT, etc.): These require searching for the corresponding definition location based on the symbol name (which might be in the executable or another library).</li></ul><h4 id="symbol-lookup-order-default-elf-search-rules-general-idea" tabindex="-1">Symbol lookup order (default ELF search rules, general idea) <a class="header-anchor" href="#symbol-lookup-order-default-elf-search-rules-general-idea" aria-label="Permalink to &quot;Symbol lookup order (default ELF search rules, general idea)&quot;">​</a></h4><p>To resolve a specific symbol (e.g., the function <code>foo</code>), the loader&#39;s lookup order is typically:</p><ol><li>The executable&#39;s global symbol table (executable overrides).</li><li>Traverse the dynamic symbol tables of each loaded library in DT_NEEDED list order, looking for the first matching global/weak symbol (note: actual rules are affected by ELF version, runtime flags, RTLD_LOCAL/RTLD_GLOBAL, symbol visibility, etc.).</li><li>If symbol versioning exists, the version tag must also match.</li><li>If loaded using <code>dlopen</code> with <code>RTLD_GLOBAL</code>, symbols from these libraries might participate in the resolution of subsequent libraries; <code>RTLD_LOCAL</code> does not participate in other subsequent resolutions.</li></ol><blockquote><p>Important: <strong>Symbols in the executable take precedence</strong> over those in shared libraries (this is known as symbol interposition), so the executable can &quot;override&quot; functions in the library (this is also the foundation for <code>LD_PRELOAD</code> to replace function implementations).</p></blockquote><p><img src="'+r+'" alt="dynamic_library"></p><p>The diagram above clearly illustrates the specific process.</p><h2 id="a-comparison" tabindex="-1">A comparison <a class="header-anchor" href="#a-comparison" aria-label="Permalink to &quot;A comparison&quot;">​</a></h2><p>I&#39;ve put together a comparison table for your reference:</p><table tabindex="0"><thead><tr><th>Comparison Item</th><th>Static Library (Static)</th><th>Shared Library (Shared / .so/.dll/.dylib)</th></tr></thead><tbody><tr><td>Binary file nature</td><td><code>.a</code> / <code>.lib</code>: An archive of several <code>.o</code> object files; at link time, the target code is copied into the executable.</td><td><code>.so</code> / <code>.dll</code> / <code>.dylib</code>: A shared object loadable at runtime, usually Position Independent Code (PIC), with SONAME/version info.</td></tr><tr><td>Executable integration (linking and runtime)</td><td>Resolves and copies the required target code into the executable at link time (static binding); at runtime, it no longer depends on the library file.</td><td>Records <code>DT_NEEDED</code> (or equivalent) at link time; at runtime, the dynamic linker maps it and relocates/resolves symbols in the process address space (dynamic binding, allowing real-time replacement/loading).</td></tr><tr><td>Impact on executable size</td><td>Increases executable size (contains actual copies of library code); multiple executables will redundantly include the same code.</td><td>Executable is smaller (only records dependencies); multiple processes share the same read-only/shared pages of the library; at runtime, extra memory is used for mapping and the GOT/PLT.</td></tr><tr><td>Portability</td><td>Simple deployment: the executable is usually self-contained (easier to port under the same architecture/ABI), but still affected by the OS/kernel/CRT.</td><td>Deployment depends on the runtime environment: requires appropriate shared library versions, a loader, and search paths (rpath/LD_LIBRARY_PATH/ldconfig); cross-distro/platform compatibility is more sensitive.</td></tr><tr><td>Ease of integration</td><td>Linking configuration is simple (directly <code>-l</code> / -L or merge .o files), no need to consider runtime loading; however, version upgrades require recompiling all clients.</td><td>Build and deployment are more complex (requires <code>-fPIC</code>, SONAME, rpath, symbol visibility, version scripts, etc.); but it supports runtime replacement, plugins, dlopen, and allows upgrading by replacing only the library file.</td></tr><tr><td>Ease of binary file processing/transformation</td><td>Packaging/inspecting/merging is straightforward (<code>ar</code>, <code>nm</code>, <code>objdump</code>); replacing or substituting local symbols is harder (requires relinking).</td><td>Generating and controlling exported symbols is more complex (symbol versioning, visibility); runtime relocation &amp; symbol resolution mechanisms are complex; however, runtime <code>dlopen/dlsym</code> provides flexible extension capabilities.</td></tr><tr><td>Suitability for development</td><td>Suitable for: small tools, embedded/single-file distribution, scenarios with no runtime dependencies; convenient for offline/restricted environment deployment.</td><td>Suitable for: large projects, modular design, plugin systems, scenarios requiring hot updates or reducing redundant memory/disk usage; beneficial for team collaboration and independent library releases.</td></tr><tr><td>Other points worth mentioning</td><td>- Security/Bug fixes require rebuilding and redistributing all executables. - Copyright/licenses (like GPL) may impose stricter obligations under static linking. - Usually no PLT overhead for runtime performance (calls).</td><td>- Can fix/replace the library independently (quick patching). - Risks of runtime hijacking (LD_PRELOAD, RPATH injection) and first-call latency (lazy binding). - Higher requirements for platform ABI/SONAME management and deployment workflows.</td></tr></tbody></table><h1 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h1><p>This is primarily based on the book: <em>Advanced C/C++ Compilation Technology</em></p>',42)])])}const y=i(l,[["render",o]]);export{m as __pageData,y as default};
