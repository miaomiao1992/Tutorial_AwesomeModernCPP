import{_ as t,o as a,c as n,a2 as i}from"./chunks/framework.BuBU3QrY.js";const u=JSON.parse('{"title":"Part 6: Starting by Lighting the First LED — Why We Use Modern C++ for STM32","description":"","frontmatter":{"title":"Part 6: Starting by Lighting the First LED — Why We Use Modern C++ for STM32","description":"","tags":["beginner","cpp-modern","stm32f1"],"difficulty":"beginner","platform":"stm32f1","chapter":15,"order":1,"translation":{"source":"documents/vol8-domains/embedded/01-led/01-motivation-and-overview.md","source_hash":"893dbb72425c37cfd9fd38832830d09b5e374ae7eb44c4ca124ac050999ae2ca","translated_at":"2026-05-26T12:03:11.907957+00:00","engine":"anthropic","token_count":2498}},"headers":[],"relativePath":"en/vol8-domains/embedded/01-led/01-motivation-and-overview.md","filePath":"en/vol8-domains/embedded/01-led/01-motivation-and-overview.md","lastUpdated":null}'),s={name:"en/vol8-domains/embedded/01-led/01-motivation-and-overview.md"};function o(r,e,l,h,d,c){return a(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="part-6-lighting-the-first-led-—-why-we-use-modern-c-for-stm32" tabindex="-1">Part 6: Lighting the First LED — Why We Use Modern C++ for STM32 <a class="header-anchor" href="#part-6-lighting-the-first-led-—-why-we-use-modern-c-for-stm32" aria-label="Permalink to &quot;Part 6: Lighting the First LED — Why We Use Modern C++ for STM32&quot;">​</a></h1><blockquote><p>For everyone who just finished setting up their toolchain and can&#39;t wait to make the board do something. This is where we truly start writing hardware control code. We won&#39;t rush into code just yet — let&#39;s thoroughly discuss the &quot;why&quot; first.</p></blockquote><hr><h2 id="starting-with-a-single-led" tabindex="-1">Starting with a Single LED <a class="header-anchor" href="#starting-with-a-single-led" aria-label="Permalink to &quot;Starting with a Single LED&quot;">​</a></h2><p>Every embedded developer&#39;s journey begins with the exact same thing — lighting an LED. This isn&#39;t some trivial matter; it&#39;s the embedded world&#39;s &quot;Hello World,&quot; your first successful conversation with a silent chip. Whether you later use STM32 for motor control, USB communication, or running an RTOS, GPIO (General-Purpose I/O) operations are always the foundation. Just as learning programming starts with <code>printf</code>, learning embedded development starts with driving a pin high and low — it&#39;s an unavoidable first step.</p><p>I remember the first time I made the LED on the Blue Pill light up. That feeling is hard to describe. It&#39;s just a tiny light, after all, but you realize that your code — through compilation, linking, format conversion, and SWD protocol transmission — ultimately becomes an electrical signal that physically changes the voltage on a pin, and then the LED turns on. This experience of &quot;code becoming a physical phenomenon&quot; is something pure software development can never give you. In that moment, you feel that the weekend you spent wrestling with the toolchain was worth it.</p><p>Speaking of the toolchain, I must admit that writing this piece puts me in a rather complicated mood. The previous five env_setup tutorials — from installing arm-none-eabi-gcc to configuring CMake, from wrestling with WSL2 USB passthrough to getting the OpenOCD debugger working — every step was a trail of tears. Especially that time trying to get the ST-Link recognized under WSL2, I almost gave up entirely. But when we could finally type <code>arm-none-eabi-g++</code> in the terminal, then <code>openocd -f ...</code>, and see nothing happen on the board — because we hadn&#39;t written the right code yet — that feeling was actually reassuring. The environment was fine, the toolchain was connected, flashing worked, and now all we needed was a single line of code to actually control the hardware.</p><p>Now we&#39;ve finally reached this step. No more battling the compiler in the terminal, no more hunting for typos in config files — it&#39;s time to write real code that makes a chip work for us.</p><h2 id="just-how-painful-is-traditional-embedded-development" tabindex="-1">Just How Painful Is Traditional Embedded Development? <a class="header-anchor" href="#just-how-painful-is-traditional-embedded-development" aria-label="Permalink to &quot;Just How Painful Is Traditional Embedded Development?&quot;">​</a></h2><p>But before we get to that, I want to talk about why this isn&#39;t inherently simple, and why we chose a slightly different path.</p><p>What does traditional STM32 development look like? If you&#39;ve used Keil MDK or IAR, you&#39;re surely familiar with that experience — a bloated IDE taking up several gigabytes, an editor with features stuck in the last century, code completion that basically relies on guessing, and an ugly debug interface that&#39;s just frustrating. What&#39;s worse is that it locks you firmly to the Windows platform. Want to develop on Linux? Sorry, either use Wine to emulate it (and face all sorts of inexplicable crashes) or dutifully fire up a virtual machine. Moreover, Keil&#39;s compiler is closed-source, its optimization behavior is opaque, and when something goes wrong, you have no idea how it optimized your code.</p><p>Of course, these are just surface-level inconveniences. What truly made me decide to abandon traditional development methods was the bad practices that the C language has accumulated over decades in the embedded field. Look at what a typical STM32 project looks like: <code>#define</code> macros everywhere, things like <code>GPIO_PIN_5</code>, <code>GPIOA_BASE</code> — these preprocessor symbols have no types, no scope, their real values are invisible during debugging, and the compiler&#39;s type checking completely fails on them. Then there are those HAL library callback functions, passing function pointers and <code>void*</code> userdata back and forth, making type safety a mere illusion. Add layer upon layer of conditional compilation <code>#ifdef</code>, <code>#ifndef</code>, and cross-platform adaptation turns the code into spaghetti.</p><p>The most fatal issue is code reusability. You write an LED driver for the Blue Pill, hardcoding <code>GPIOC</code> and <code>GPIO_PIN_13</code>. Next time you switch to an STM32F407 board where the LED is on PD12, what do you do? Copy, paste, and change the parameters? What if the project has ten pins to control? Twenty? C macros and structs can solve part of the problem, but ultimately you&#39;ll still end up buried in runtime checks and switch-case statements — neither elegant nor efficient.</p><p>This isn&#39;t about trashing C — C is a great language, and there&#39;s a reason it has dominated the embedded field for decades. But times are changing, compilers are improving, and can&#39;t we pursue better abstractions without paying a runtime cost?</p><h2 id="why-c-23" tabindex="-1">Why C++23? <a class="header-anchor" href="#why-c-23" aria-label="Permalink to &quot;Why C++23?&quot;">​</a></h2><p>This is where modern C++ enters the picture. Note that I said &quot;modern C++,&quot; not the &quot;C with classes&quot; from the 90s. The features brought by the C++23 standard are exactly what embedded development has been dreaming of.</p><p>Zero-overhead abstraction is C++&#39;s most core design philosophy — you don&#39;t pay for what you don&#39;t use. Templates are expanded at compile time, <code>constexpr</code> functions are evaluated at compile time, and <code>if constexpr</code> makes branch selections at compile time. These mechanisms give your code beautiful abstraction layers at the source level, but the compiled machine code is just as efficient as hand-written C. Your LED template class looks like an elegant type system, but the few <code>LDR</code> and <code>STR</code> instructions the compiler ultimately generates are exactly the same as if you had directly manipulated the registers. No virtual function overhead, no RTTI overhead, no exception handling overhead — because we explicitly disabled these features at compile time.</p><p>Compile-time type safety is another killer advantage. In C, if you pass <code>13</code> to a function expecting a port number, the compiler won&#39;t make a peep, because they&#39;re both just integers. But in our C++ template system, <code>Port::C</code> is an independent type. You encode the port and pin information into the type system at compile time, so any wrong parameter is exposed during compilation, rather than you scratching your head staring at the code only after the LED on the board fails to light up.</p><p>The code reuse brought by templates goes without saying. Our GPIO template accepts the port and pin as non-type template parameters, which means <code>Gpio&lt;Port::A, 5&gt;</code> and <code>Gpio&lt;Port::C, 13&gt;</code> are two different classes, each with its own <code>set()</code> and <code>clear()</code> methods. The clock enable branching is done at compile time using <code>if constexpr</code> — if it&#39;s port A, enable the GPIOA clock; if it&#39;s port B, enable the GPIOB clock — all of this happens at compile time, with zero runtime overhead. You never have to write that kind of runtime table-lookup code to find port numbers again.</p><p>Then there are those C++23 sweeteners: the <code>[[nodiscard]]</code> attribute makes the compiler warn you when you ignore a return value — this is incredibly important in embedded; the clock configuration failed and you didn&#39;t check? The system runs away immediately. <code>enum class</code> wraps bare integers into strongly-typed enumerations, putting an end to implicit conversions between different enum values. <code>constexpr</code> makes port address conversion a compile-time constant. Individually, these features seem unremarkable, but combined, they can take the safety and maintainability of embedded code up a major notch.</p><p>So we chose C++23 not to be trendy, but because it genuinely solves real problems in embedded development. Later on, we&#39;ll use plenty of code to prove this point.</p><hr><h2 id="what-you-need-to-prepare" tabindex="-1">What You Need to Prepare <a class="header-anchor" href="#what-you-need-to-prepare" aria-label="Permalink to &quot;What You Need to Prepare&quot;">​</a></h2><p>Before we officially begin, there are a few things we need to confirm are in place.</p><p>First are those five env_setup tutorials. If you&#39;ve been skipping around, I strongly recommend going back and reading parts 01 through 05 in their entirety: toolchain installation, project structure, CMake configuration, USB flashing, and debugger configuration. Every line of code in this part is built on the environment set up in those five parts. Your <code>arm-none-eabi-g++</code> must compile normally, CMake must build successfully, and OpenOCD must be able to flash firmware to the board. If you haven&#39;t got these sorted out yet, stop now and get them done — half an hour won&#39;t make a difference.</p><p>Then there&#39;s basic programming knowledge. I won&#39;t start from &quot;what is a variable,&quot; but I also won&#39;t assume you&#39;re a C++ template metaprogramming expert. You need to be familiar with basic C or C++ syntax: variable declarations, function definitions, basic concepts of structs and classes, and the purpose of <code>#include</code> header files. If you&#39;ve written code in any programming language and understand what &quot;function call&quot; and &quot;return value&quot; mean, then you have a sufficient starting point. Advanced features like templates, CRTP (Curiously Recurring Template Pattern), and <code>constexpr</code> will be gradually introduced and explained as we use them.</p><p>On the hardware side, you only need three things for this entire article: an STM32F103C8T6 Blue Pill development board, an ST-Link V2 debug probe, and a USB cable. Blue Pills can be bought for under ten RMB on Taobao, and ST-Link V2s are even cheaper, just a few RMB. All three together might cost less than a cup of milk tea, but they can take you through the entire journey from lighting an LED to understanding the modern embedded development paradigm. The ST-Link connects to the Blue Pill via three wires: SWDIO, SWCLK, and GND, plus 3.3V to power the board. We covered the specific wiring in detail in the USB section of env_setup, so we won&#39;t repeat it here.</p><p>The software environment is the same set we configured in env_setup: the <code>arm-none-eabi-gcc</code> toolchain, OpenOCD, and CMake 3.22 or higher. Use whatever editor you like; VSCode with the clangd plugin gives a decent code completion experience, but it doesn&#39;t matter if you use Vim, Neovim, or even just <code>cat</code> — we use CMake for building anyway, so it&#39;s editor-agnostic.</p><p>⚠️ If you&#39;re developing under WSL2, make absolutely sure that USB/IP passthrough is configured and that <code>lsusb</code> can see the ST-Link device. This is a prerequisite for flashing; if it&#39;s not set up, the subsequent <code>openocd</code> commands will definitely fail.</p><hr><h2 id="the-road-ahead" tabindex="-1">The Road Ahead <a class="header-anchor" href="#the-road-ahead" aria-label="Permalink to &quot;The Road Ahead&quot;">​</a></h2><p>Now that the tools and mindset are ready, I want to map out the entire road ahead so you have a mental map. The LED control tutorial series isn&#39;t a single article, but a complete learning path from &quot;understanding hardware&quot; to &quot;mastering the API&quot; to &quot;redesigning with modern C++,&quot; totaling 13 parts. Why do we need so many parts just to light an LED? Because our goal isn&#39;t &quot;just make it light up and call it done,&quot; but to understand the principles behind every line of code and the trade-offs in every design decision.</p><p>We&#39;ll start with the hardware principles of GPIO, which is the most foundational layer. GPIO sounds like just five characters for &quot;general-purpose input/output,&quot; but the circuit structure behind it — push-pull output, open-drain output, pull-up resistors, pull-down resistors, Schmitt triggers — each directly affects how you should configure pins and choose operating modes. Without understanding these, writing code is just memorizing incantations; change the scenario and you&#39;re lost. We&#39;ve allocated three parts for hardware principles, starting from the GPIO internal structure block diagram, to circuit analysis of the four operating modes, and then to the register organization of the STM32F103. Don&#39;t be afraid of hardware — these things are actually quite easy to understand when drawn as diagrams.</p><p>Then the question arises — knowing the hardware principles of GPIO, how do we control it with software? The official ST-provided HAL library is exactly this bridge. HAL stands for Hardware Abstraction Layer, and it wraps low-level register operations into function calls like <code>HAL_GPIO_Init</code> and <code>HAL_GPIO_WritePin</code>. We&#39;ll use three tutorials to break down HAL&#39;s GPIO interface: initialization configuration, read/write operations, and clock management. This part will use C language style directly, because HAL itself is a C interface, and we need to learn the &quot;fundamentalist&quot; usage first before we can talk about building better abstractions on top of it.</p><p>Then comes one part on traditional C language implementation. Here we&#39;ll connect the knowledge from the previous two parts: determine configuration parameters based on hardware principles, and use the HAL API to write a working LED blink program. But this C language version of the code will expose the problems we mentioned earlier — hardcoded macros, lack of type safety, and poor reusability. The purpose of this part is to let you see the pain points with your own eyes, laying the motivational groundwork for the C++ refactoring that follows.</p><p>But we&#39;re not done yet. Once we recognize the pain points, we enter the most core C++ refactoring stage, spanning four tutorials. The first introduces the CRTP singleton pattern and clock configuration encapsulation; the second dives deep into GPIO template design, explaining non-type template parameters, <code>if constexpr</code> branching, and the safe use of <code>reinterpret_cast</code>; the third builds an LED template on top of the GPIO template, demonstrating the practical effects of zero-overhead abstraction; the fourth compares the compiled output of the C and C++ versions, using disassembly to prove that C++ templates truly introduce no extra overhead. These four parts are the main event of this series, and they represent the core value of our tutorial.</p><p>After that, there&#39;s one part dedicated to C++23 features, systematically organizing the modern features we use in our code: <code>constexpr</code>, <code>enum class</code>, <code>[[nodiscard]]</code>, <code>if constexpr</code>, and so on. Finally, there&#39;s one part on pitfall exercises, compiling all the weird issues we encountered during development — forgetting to enable the clock causing pins not to work, the special limitations of PC13, choosing push-pull vs. open-drain incorrectly causing wrong signals — to help you clear the mines in advance.</p><p>The design logic of this entire path is very clear: understand the hardware first to configure parameters correctly, learn the API first to operate the hardware, and experience C&#39;s pain points first to understand the value of C++ refactoring. This isn&#39;t a tutorial that &quot;throws the final code at you right away,&quot; but one that walks you through the complete cognitive process from bottom to top. After completing it, you won&#39;t just know &quot;how to write it,&quot; but also &quot;why it&#39;s written this way.&quot;</p><hr><h2 id="the-board-in-your-hands" tabindex="-1">The Board in Your Hands <a class="header-anchor" href="#the-board-in-your-hands" aria-label="Permalink to &quot;The Board in Your Hands&quot;">​</a></h2><p>Before we write any code, let&#39;s take a clear look at the Blue Pill development board in front of us.</p><p>Blue Pill is the common name for the STM32F103C8T6 minimum system board, named because the board&#39;s shape resembles a blue pill (although the origin of this name is a bit hard to explain). The STM32F103C8T6 chip it carries is a microcontroller based on the ARM Cortex-M3 core, with a maximum clock frequency of 72MHz, 64KB Flash, and 20KB RAM. In 2026, this spec looks downright pitiful — your phone easily has 12GB RAM and 256GB storage, and this chip doesn&#39;t even have enough memory for a single icon on your phone screen. But don&#39;t forget that this chip&#39;s design goal is real-time control and low power consumption, not running Android. A 72MHz Cortex-M3 is more than enough to drive motors, sample sensors, run communication protocols, and even run a lightweight RTOS.</p><p>What we care about most is the LED on the board. Blue Pills typically have an onboard LED connected to the PC13 pin, wired through a current-limiting resistor to VCC3.3V. Pay attention to this wiring — the LED&#39;s anode goes through the resistor to VCC, and the cathode connects to PC13. This means that when PC13 outputs a low level, current flows from VCC through the resistor and LED into PC13, and the LED lights up; when PC13 outputs a high level (3.3V), the voltage difference across both ends is zero, no current flows, and the LED turns off. So this is an &quot;active-low&quot; LED, which will be reflected in the code later as <code>ActiveLow</code>. In the next part, we&#39;ll draw the LED&#39;s circuit diagram in detail for analysis; for now, you just need to remember &quot;PC13, lights up on low.&quot;</p><p>⚠️ The PC13 pin has some special limitations on the STM32F103 — it&#39;s connected to the RTC domain, the maximum output current is only 3mA, and its drive speed is limited. So you wouldn&#39;t use it to drive high-current loads, but lighting an onboard LED is more than enough. This limitation doesn&#39;t need special handling in our C++ template, because the LED template only needs to correctly output high and low levels, and doesn&#39;t involve high-current scenarios.</p><p>On the debugger side, the ST-Link V2 communicates with the Blue Pill through the SWD (Serial Wire Debug) interface. SWD only needs two signal lines: SWDIO (data line, bidirectional) and SWCLK (clock line, host output). Add the ground line GND, and just three wires can complete all debugging and flashing operations. The Blue Pill board has a four-pin SWD interface on the right side (labeled SWDIO, SWCLK, GND, 3.3V); just connect the corresponding ST-Link pins to it. If this interface is hard to wire to, you can also use the pin headers on the left side of the board — PA13 is SWDIO, PA14 is SWCLK, and these two pins have alternate function mappings in SWD mode.</p><p>The STM32F103C8T6 has three main GPIO port groups: GPIOA, GPIOB, and GPIOC, each with 16 pins (PA0-PA15, PB0-PB15, PC0-PC15), for a total of 48 programmable GPIO pins. GPIOA and GPIOB have relatively complete functionality, and most of their pins can be freely configured as input, output, alternate function, or analog mode. PC13 through PC15 on GPIOC have the RTC domain limitations mentioned above, while PC0 through PC12 don&#39;t have these constraints. In our later exercises, the pins you&#39;ll use are basically concentrated on GPIOA and GPIOC, with GPIOB being used relatively less.</p><hr><h2 id="what-our-project-looks-like" tabindex="-1">What Our Project Looks Like <a class="header-anchor" href="#what-our-project-looks-like" aria-label="Permalink to &quot;What Our Project Looks Like&quot;">​</a></h2><p>Alright, we&#39;ve talked enough about hardware; now let&#39;s look at the software. The code for the entire LED control project is in the <code>led_blink</code> directory, structured as follows:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1_led_control/</span></span>
<span class="line"><span>├── device/</span></span>
<span class="line"><span>│   ├── gpio/</span></span>
<span class="line"><span>│   │   └── gpio.hpp        # GPIO泛化模板（本系列核心中的核心）</span></span>
<span class="line"><span>│   └── led.hpp             # LED专用模板</span></span>
<span class="line"><span>├── base/</span></span>
<span class="line"><span>│   └── simple_singleton.hpp # CRTP单例基类</span></span>
<span class="line"><span>├── system/</span></span>
<span class="line"><span>│   ├── clock.h             # 系统时钟配置（头文件）</span></span>
<span class="line"><span>│   ├── clock.cpp           # 系统时钟配置（实现）</span></span>
<span class="line"><span>│   ├── dead.hpp            # 错误处理：死循环挂起</span></span>
<span class="line"><span>│   ├── hal_mock.c          # HAL中断桥接</span></span>
<span class="line"><span>│   └── syscall.c           # C运行时最小实现</span></span>
<span class="line"><span>├── main.cpp                # 程序入口：LED闪烁</span></span>
<span class="line"><span>├── CMakeLists.txt          # CMake构建配置</span></span>
<span class="line"><span>├── STM32F103C8TX_FLASH.ld  # 链接脚本</span></span>
<span class="line"><span>└── stm32f1xx_hal_conf.h    # HAL配置头文件</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p>Let&#39;s quickly go through each file&#39;s responsibilities from top to bottom to build an overall impression first; we&#39;ll dive into each one in subsequent tutorials.</p><p><code>main.cpp</code> is the entry point for the entire program, currently with less than 20 lines of code. It calls <code>HAL_Init()</code> to initialize the HAL library, configures the system clock to 64MHz, then constructs an LED object and enters an infinite blink loop. It&#39;s just that simple — but behind this simplicity, the template classes in the <code>drivers</code> directory are doing a lot of heavy lifting.</p><p><code>gpio.hpp</code> is the absolute core of this series. It defines a <code>Gpio</code> class template that accepts two non-type template parameters: the port (a <code>Port</code> enum, with values like <code>GPIOA_BASE</code>, <code>GPIOB_BASE</code> — these hardware base addresses) and the pin number (<code>uint8_t</code>, with values from <code>0</code> to <code>15</code>). Inside the template, the port address is converted to a <code>GPIO_TypeDef*</code> pointer, encapsulating initialization, read/write, and toggle operations. It also uses a nested class <code>ClockEnable</code> with <code>if constexpr</code> to implement compile-time clock enable branching. The entire template has no virtual functions and no dynamic memory allocation; the compiled code is identical to hand-written C directly calling HAL.</p><p><code>led.hpp</code> builds an LED-specific template on top of the GPIO template. It inherits from <code>Gpio&lt;Port, Pin&gt;</code> and adds an <code>ActiveLevel</code> template parameter to indicate whether the LED is active-high or active-low. The constructor automatically calls <code>set_mode(Mode::PushPull)</code> to configure push-pull output mode. The <code>on()</code> and <code>off()</code> methods decide whether to write high or low based on the value of <code>ActiveLevel</code>. <code>toggle()</code> directly delegates to the underlying <code>Gpio::toggle()</code>. This is a textbook example of zero-overhead abstraction — the LED template provides a semantically clear interface at the source level, but after the compiler inlines it, <code>led.on()</code> is just a single <code>HAL_GPIO_WritePin</code> call.</p><p><code>singleton.hpp</code> is a CRTP (Curiously Recurring Template Pattern) singleton base class. It uses template inheritance to give any subclass automatic singleton semantics — <code>instance()</code> returns a reference to a static local variable, guaranteeing thread-safe lazy initialization while avoiding global variable initialization order issues. Copy and move constructors are explicitly deleted. Currently, only <code>SystemClock</code> uses this base class, but more hardware abstraction classes will inherit from it later.</p><p>The files in the <code>system</code> directory are all system-level infrastructure. <code>system_clock.hpp</code> and <code>system_clock.cpp</code> encapsulate RCC clock configuration: first using the HSI internal oscillator to multiply up to 64MHz (HSI 8MHz ÷ 2 × 16 = 64MHz), then configuring the AHB (Advanced High-performance Bus)/APB1/APB2 dividers. If the clock configuration fails, it calls the <code>halt()</code> function in <code>error_handler.hpp</code> to put the system into an infinite loop — in a bare-metal environment without exception handling mechanisms, &quot;stopping&quot; is the safest error response. <code>stm32f1xx_it.cpp</code> does only one thing: provide the <code>SysTick</code> interrupt service routine (ISR) to drive HAL&#39;s timebase. <code>syscalls.cpp</code> provides an empty <code>_sbrk</code> function to satisfy the C++ runtime&#39;s linking requirements — in an environment without an operating system, these initialization stub functions must be provided by us.</p><p><code>CMakeLists.txt</code> is the build configuration we dissected in detail in the env_setup series. It sets up the cross-compile toolchain, brings in HAL driver source code, configures compiler flags (<code>-Wall -Wextra</code>), disables exceptions and RTTI (<code>-fno-exceptions -fno-rtti</code>), and defines CMake custom targets for flashing and erasing. The C++23 standard is enabled here through <code>-std=c++23</code>, which is the prerequisite for the entire project to use modern C++ features.</p><p>For now, let&#39;s not look at the specific implementation, but only at the final result. Here is the complete code for our <code>main.cpp</code>:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;device/led.hpp&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;system/clock.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;C&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;stm32f1xx_hal.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    HAL_Init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    clock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ClockConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">instance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setup_system_clock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* led setups! */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    device</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::LED</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">device</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gpio</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GpioPort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::C, GPIO_PIN_13</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> led;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        HAL_Delay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        led.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        HAL_Delay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        led.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">off</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>Look closely at this code. <code>HAL_Init()</code> and <code>SystemClock::instance().init()</code> are system initialization that every STM32 project must do; there&#39;s nothing special about this part. The exciting part is the third line — <code>Led&lt;Port::C, 13, ActiveLow&gt; led{};</code>. This single line accomplishes three things simultaneously: it tells the compiler we&#39;re using pin 13 of the GPIOC port, it automatically calls the constructor&#39;s <code>set_mode()</code> to configure the pin as push-pull output, and it automatically enables the GPIOC peripheral clock. And as the caller, you only need to declare a variable with the correct type, leaving everything else to the template to handle at compile time.</p><p>The blink loop that follows is so straightforward it needs no explanation: delay 500 milliseconds, turn on, delay 500 milliseconds, turn off. The method names <code>led.on()</code> and <code>led.off()</code> are self-documenting — you can tell what the code is doing without reading any comments. Compare this with the traditional C approach of <code>HAL_GPIO_WritePin(GPIOC, GPIO_PIN_13, GPIO_PIN_RESET)</code>, and it&#39;s obvious at a glance which is easier to understand.</p><p>Of course, I&#39;m just showing the final result right now. This &quot;simplicity&quot; is built on the carefully designed templates in <code>gpio.hpp</code> and <code>led.hpp</code>. Our goal is to ensure that everyone who reads this will eventually fully understand the design motivation, implementation details, and underlying C++23 features of these templates. By that time, you&#39;ll be able to design similar hardware abstraction templates yourself and extend this approach to any peripheral like UART, SPI, or I2C.</p><hr><h2 id="where-to-next" tabindex="-1">Where to Next <a class="header-anchor" href="#where-to-next" aria-label="Permalink to &quot;Where to Next&quot;">​</a></h2><p>Both hardware and software are ready, the learning path is mapped out, and we&#39;ve gone through the project structure. Starting from the next part, we&#39;re going to dive headfirst into the hardware principles of GPIO.</p><p>In the next part, we&#39;ll answer a question that seems simple but actually has quite a bit of depth: what exactly is GPIO? It&#39;s not just a wire. Inside a GPIO pin, there&#39;s an input data register, an output data register, a push-pull driver, an open-drain driver, a pull-up resistor, a pull-down resistor, a Schmitt trigger, and an alternate function selector — all of these together form a rather exquisite circuit structure. The STM32F103&#39;s GPIO supports four operating modes: general-purpose input, general-purpose output, alternate function, and analog mode. Understanding these internal structures is a prerequisite for correctly configuring and using GPIO. We&#39;ll start from the GPIO internal structure block diagram in the next part, clearly explaining the difference between push-pull and open-drain output, the role of pull-up and pull-down resistors, and the meaning of pin speed settings.</p><p>The craftsman who wishes to do good work must first sharpen his tools. Once we thoroughly understand the hardware, writing code becomes a breeze.</p>`,67)])])}const m=t(s,[["render",o]]);export{u as __pageData,m as default};
