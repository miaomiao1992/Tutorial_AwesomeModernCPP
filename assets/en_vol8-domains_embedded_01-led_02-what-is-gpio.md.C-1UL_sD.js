import{_ as t,o as n,c as i,a2 as s}from"./chunks/framework.CZcFxxdw.js";const u=JSON.parse('{"title":"Part 7: What Exactly is GPIO — The Past and Present of General-Purpose Input/Output","description":"","frontmatter":{"title":"Part 7: What Exactly is GPIO — The Past and Present of General-Purpose Input/Output","description":"","tags":["beginner","cpp-modern","stm32f1"],"difficulty":"beginner","platform":"stm32f1","chapter":15,"order":2},"headers":[],"relativePath":"en/vol8-domains/embedded/01-led/02-what-is-gpio.md","filePath":"en/vol8-domains/embedded/01-led/02-what-is-gpio.md","lastUpdated":null}'),a={name:"en/vol8-domains/embedded/01-led/02-what-is-gpio.md"};function o(r,e,l,p,h,c){return n(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="part-7-what-exactly-is-gpio-—-the-past-and-present-of-general-purpose-i-o" tabindex="-1">Part 7: What Exactly is GPIO — The Past and Present of General-Purpose I/O <a class="header-anchor" href="#part-7-what-exactly-is-gpio-—-the-past-and-present-of-general-purpose-i-o" aria-label="Permalink to &quot;Part 7: What Exactly is GPIO — The Past and Present of General-Purpose I/O&quot;">​</a></h1><h2 id="preface-from-environment-setup-to-questioning-the-essence" tabindex="-1">Preface: From Environment Setup to Questioning the Essence <a class="header-anchor" href="#preface-from-environment-setup-to-questioning-the-essence" aria-label="Permalink to &quot;Preface: From Environment Setup to Questioning the Essence&quot;">​</a></h2><p>In the previous article, we discussed why we use modern C++ to program STM32 — the pain points of traditional development where C macros run rampant, and the changes that zero-overhead abstraction in modern C++ can bring. We also briefly surveyed the project&#39;s code structure and saw that the final <code>main.cpp</code> only needs a few lines of code to make an LED blink. But if you stop and think about it — we write C++ code, the code runs on a piece of silicon, and an LED is a physical device. What connects them in between? The answer is pins, or more precisely, GPIO pins.</p><p>GPIO stands for General-Purpose Input/Output. The name itself is very straightforward — it is general-purpose, not dedicated to any specific function, and it can both input and output. But the word &quot;general-purpose&quot; might create an illusion that it is simple, primitive, or even unimportant. The truth is exactly the opposite. GPIO is the most fundamental and direct channel for an MCU (Microcontroller Unit) to interact with the outside world. Almost every peripheral you will use later — serial communication, SPI bus, I2C bus, PWM motor control — their physical signals are ultimately output or input through GPIO pins. Understanding GPIO means understanding how the MCU &quot;reaches out to touch the world.&quot;</p><p>You can think of GPIO as countless invisible hands reaching out from the MCU. These hands only do the simplest things — grab a high level, or let go to a low level. But when these hands act in specific timing sequences and combinations, they can accomplish extremely complex tasks like communication, control, and data acquisition. And everything starts with understanding how a single hand grabs and lets go.</p><p>What we need to do now is dive into the internal structure of this &quot;hand&quot; and see exactly how it works. Don&#39;t rush to look at the code just yet; let&#39;s start from the most fundamental physical questions.</p><h2 id="from-led-circuits-to-the-programming-model" tabindex="-1">From LED Circuits to the Programming Model <a class="header-anchor" href="#from-led-circuits-to-the-programming-model" aria-label="Permalink to &quot;From LED Circuits to the Programming Model&quot;">​</a></h2><p>Let&#39;s go back to the most fundamental physical question first: why does an LED light up?</p><p>The physical condition for an LED (Light Emitting Diode) to light up is actually very simple — as long as current flows from its positive terminal (anode) to its negative terminal (cathode), and the current is large enough (usually a few milliamps is sufficient for visibility), it will emit light. In a classic LED driver circuit, we connect VCC (positive power supply) through a current-limiting resistor to the LED&#39;s anode, and connect the LED&#39;s cathode to GND (ground). Current flows from VCC, through the resistor, through the LED, and back to GND, forming a complete circuit. The resistor&#39;s job is to limit the current to prevent the LED from burning out due to overcurrent.</p><p>This is a purely passive circuit. As long as the power is connected, the LED stays on, and you have no means of control.</p><p>Now, let&#39;s replace VCC with a pin on the MCU. When this pin outputs a high level (for STM32, that&#39;s a voltage close to 3.3V), a current path exists, and the LED turns on. When the pin outputs a low level (close to 0V), there is almost no voltage difference across the LED, no current flows, and the LED turns off. Just like that, we achieved control over the LED&#39;s on/off state by controlling the pin&#39;s level. Of course, you can also wire it the other way around — anode to pin, cathode to ground — in which case the LED only lights up when the pin outputs a high level. Both approaches are common in real projects, and the on-board LED on the STM32F103C8T6 minimum system board uses the active-low wiring, connected to the PC13 pin.</p><p>The next question is: how does the MCU pin &quot;output&quot; a high or low level? A pin is not a wire; it cannot generate voltage out of thin air. Behind the pin is an entire digital circuit — MOSFETs (Metal-Oxide-Semiconductor Field-Effect Transistors), registers, and multiplexers. The code we write simply writes a value to a specific memory address, and this value is translated by the hardware circuit into MOSFET conduction or cutoff. The MOSFET&#39;s conduction state determines whether the pin is at VDD (high level) or VSS (low level).</p><p>This is the GPIO programming model. We write code to tell the GPIO controller &quot;I want this pin to output a high level,&quot; the GPIO controller operates the internal MOSFET, and the MOSFET changes the pin&#39;s physical voltage. From software to hardware, the signal goes through three layers of translation: registers, buses, and transistors. You will find that this programming model applies not only to LED control but to all digital signal interactions through GPIO. Button detection is the reverse process — an external signal changes the pin&#39;s voltage, and after sampling, GPIO tells the CPU. We will expand on this in detail shortly.</p><blockquote><p>⚠️ Here is a pitfall that beginners are especially prone to falling into: many people assume that pins are in output mode by default and can directly control an LED right after power-on. But in reality, STM32 pins default to a floating input state after reset. If you forget to configure the pin as output before trying to control the LED, the pin will not output the level you expect, and the LED naturally won&#39;t light up. This is also why in our <code>led.hpp</code>, the LED constructor must first call <code>Base::setup(Base::Mode::OutputPP, ...)</code> to initialize the pin.</p></blockquote><h2 id="pin-grouping-on-the-stm32f103c8t6" tabindex="-1">Pin Grouping on the STM32F103C8T6 <a class="header-anchor" href="#pin-grouping-on-the-stm32f103c8t6" aria-label="Permalink to &quot;Pin Grouping on the STM32F103C8T6&quot;">​</a></h2><p>The STM32F103C8T6 chip uses an LQFP48 package, meaning it has 48 physical pins distributed around the chip&#39;s perimeter. But if you look closely at the datasheet, you&#39;ll find that not all 48 pins can serve as GPIO. Among them are dedicated pins like VDD (power), VSS (ground), VBAT (backup battery), NRST (reset), and BOOT0 (boot mode selection), leaving about 37 pins that can be used as GPIO.</p><p>These 37 GPIO pins are divided into five groups, named GPIOA, GPIOB, GPIOC, GPIOD, and GPIOE. Each group can contain up to 16 pins, numbered from 0 to 15. The STM32 designers chose the number 16 for a reason — 16 is exactly the width of a 16-bit register, which means a single 16-bit register can fully describe the state of every bit in one GPIO group, making the hardware design very clean.</p><p>The pin naming convention is &quot;group name + number.&quot; For example, PA0 is pin number 0 of the GPIOA group, and PC13 is pin number 13 of the GPIOC group. The <code>GPIO_PIN_13</code> we use in our code is essentially a bit mask — <code>1 &lt;&lt; 13</code>, which is <code>0x2000</code>. The HAL library uses this mask to identify the specific pin, allowing a single operation to affect multiple pins simultaneously.</p><p>In our project code, the <code>GpioPort</code> enum in <code>device/gpio/gpio.hpp</code> maps each GPIO group to its base address in memory:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GpioPort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uintptr_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GPIOA_BASE,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 0x40010800</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    B</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GPIOB_BASE,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 0x40010C00</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    C</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GPIOC_BASE,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 0x40011000</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    D</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GPIOD_BASE,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 0x40011400</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    E</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GPIOE_BASE,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 0x40011800</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>You&#39;ll notice that the interval between these base addresses is <code>0x400</code> (1024 bytes), indicating that each GPIO group occupies 1KB of address space in memory. Within this 1KB space, seven registers are arranged that control the entire behavior of the 16 pins in that group. The two most critical configuration registers are CRL and CRH — CRL (Configuration Register Low) handles Pin0 through Pin7 (the lower 8 pins), and CRH (Configuration Register High) handles Pin8 through Pin15 (the upper 8 pins). Each pin occupies 4 bits in the configuration register (2 CNF configuration bits + 2 MODE mode bits), and 16 pins exactly consume two 32-bit registers.</p><p>Great, now we know the pin grouping and naming conventions. But what exactly can a pin do? That brings us to the four operating modes of GPIO.</p><blockquote><p>⚠️ A common source of confusion: the chip is called STM32F103C8T6, so why is it sometimes written as STM32F103C8 and sometimes with the T6 suffix? Actually, C8 is the part number code, indicating 64KB of flash memory; T6 is the package code, indicating the LQFP48 package. The same part number with a different package (such as LQFP64 or LQFP100) will have a different number of available GPIO pins. So when you look up pin assignments, always confirm the package type.</p></blockquote><h2 id="the-four-operating-modes-of-gpio" tabindex="-1">The Four Operating Modes of GPIO <a class="header-anchor" href="#the-four-operating-modes-of-gpio" aria-label="Permalink to &quot;The Four Operating Modes of GPIO&quot;">​</a></h2><p>Although GPIO stands for &quot;General-Purpose Input/Output,&quot; its versatility goes far beyond simply &quot;being able to output high/low levels and read high/low levels.&quot; The STM32F1 series GPIO supports four main operating modes: input, output, alternate function, and analog. Each mode exists out of necessity, corresponding to four fundamental needs of the MCU interacting with the outside world.</p><p>First, let&#39;s discuss Input mode. The core problem that input mode solves is &quot;what is the outside world telling the MCU?&quot; When a pin is configured as input, external signals enter the chip through the pin. The voltage on the pin first passes through a Schmitt Trigger for shaping — the Schmitt Trigger&#39;s job is to convert a potentially noisy analog signal (such as a slow rising edge with noise) into a clean digital signal, either a definitive 0 or a definitive 1, with no intermediate state. The shaped signal is then sampled into the Input Data Register (IDR). Our program can read the IDR to know whether the pin is currently at a high or low level. In input mode, you can also optionally enable the internal pull-up resistor or pull-down resistor: a pull-up resistor weakly connects the pin to VDD, making it default to a high level when floating; a pull-down resistor weakly connects the pin to VSS, making it default to a low level when floating; with neither pull-up nor pull-down enabled, the floating pin&#39;s level is indeterminate. This is crucial in button detection — if one end of your button is connected to the pin and the other end to ground, you need to enable the internal pull-up resistor, so that when the button is not pressed you read a high level, and when pressed you read a low level, yielding a clear and reliable state. Why does input mode need to exist? Because an MCU cannot always &quot;talk to itself&quot; by outputting signals; it must be able to sense state changes in the outside world — whether a button has been pressed, whether a sensor has issued an alarm, whether another chip has sent a ready signal — these are all use cases for input mode.</p><p>Next is Output mode. The core problem that output mode solves is &quot;what is the MCU telling the outside world?&quot; When a pin is configured as output, the chip actively drives the pin to a high or low level. Output mode has two subtypes: push-pull and open-drain. Push-pull mode uses two MOSFETs — a P-MOS upper transistor connected to VDD and an N-MOS lower transistor connected to VSS — to actively drive in both directions. When outputting a high level, the upper transistor conducts and the lower transistor cuts off, pulling the pin to VDD; when outputting a low level, the upper transistor cuts off and the lower transistor conducts, pulling the pin to VSS. The two transistors alternate their operation like pushing and pulling, hence the name &quot;push-pull.&quot; Push-pull mode has strong driving capability and can source and sink relatively large currents. Open-drain mode, on the other hand, only has the N-MOS lower transistor working. When outputting a low level, the lower transistor conducts and pulls the pin to VSS, but when outputting a high level, the lower transistor also cuts off, leaving the pin in a high-impedance state (floating), unable to actively pull high. To output a high level, an external pull-up resistor must be connected. A typical application scenario for open-drain output is the I2C bus — multiple devices share the same signal line, and any device can pull the line low, but no device actively pushes the line high (to avoid bus conflicts), with the high level provided by an external pull-up resistor. LED control typically uses push-pull output, which is why we chose <code>Mode::OutputPP</code> in <code>led.hpp</code>. Why does output mode need to exist? Because the MCU must be able to actively change the state of external circuits — lighting up LEDs, driving relays, generating clock signals — all of these require the pin to have the ability to actively output a definite level.</p><p>Then there is Alternate Function mode. This mode exists because STM32 integrates a large number of on-chip peripherals — USART serial ports, SPI buses, I2C buses, timer PWM outputs, and so on — and these peripherals need physical pins to send and receive signals, but the chip&#39;s pin count is limited. The solution is pin multiplexing: the same physical pin can assume different roles at different times. When a pin is configured as alternate function mode, it is no longer directly controlled by the GPIO controller but is handed over to the corresponding on-chip peripheral to drive. For example, PA9 and PA10 can be configured as the TX (transmit) and RX (receive) pins of USART1, at which point they are no longer regular GPIO but serial communication signal lines. Once configured, the code operates on the USART peripheral&#39;s registers rather than the GPIO registers, and the pin&#39;s signals are automatically generated by the USART hardware. In <code>gpio.hpp</code>, this corresponds to <code>Mode::AfPP</code> (alternate function push-pull) and <code>Mode::AfOD</code> (alternate function open-drain). Why does alternate function mode need to exist? Because pins are a scarce resource. A 48-pin chip only has a little over 30 pins available as GPIO, but the on-chip peripherals combined might need 50 to 60 signal lines. Without multiplexing, the chip&#39;s pin count would bloat to an unacceptable level.</p><p>Finally, there is Analog mode. Analog mode is used for connecting to on-chip ADC (Analog-to-Digital Converter) or DAC (Digital-to-Analog Converter). In analog mode, the pin&#39;s digital functions are completely disabled — the Schmitt Trigger is disabled, the Input Data Register (IDR) does not update, and the analog signal on the pin goes directly through an internal path to the ADC for sampling. Why does analog mode need to exist? Because the presence of the Schmitt Trigger introduces additional current consumption and signal distortion. When you need to read precise analog voltages (such as millivolt-level signals from a temperature sensor), these digital circuits become sources of interference instead. So analog mode is essentially &quot;turning off all digital logic and letting the pin return to its purest analog state.&quot; In <code>gpio.hpp</code>, this corresponds to <code>Mode::Analog</code>.</p><blockquote><p>⚠️ Pitfall warning: Many beginners find that pin behavior is incorrect after configuring GPIO, only to discover that the mode was configured wrong. The most common mistake is configuring a pin that should be in alternate function mode as a regular output mode — for example, wanting to use PA9 as USART1_TX but configuring it as <code>GPIO_MODE_OUTPUT_PP</code>, resulting in the serial port being unable to send data. Alternate functions must use <code>GPIO_MODE_AF_PP</code> or <code>GPIO_MODE_AF_OD</code>, which tells the multiplexer to hand the pin over to the peripheral.</p></blockquote><h2 id="gpio-internal-block-diagram" tabindex="-1">GPIO Internal Block Diagram <a class="header-anchor" href="#gpio-internal-block-diagram" aria-label="Permalink to &quot;GPIO Internal Block Diagram&quot;">​</a></h2><p>We&#39;ve described the four modes in text, but to truly understand how GPIO works, an internal block diagram is worth a thousand words. Below is an ASCII-art diagram of the STM32F1 series GPIO pin internal structure. Please note that this is a simplified conceptual diagram that omits some details (such as output speed control), but the core signal paths are accurate.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                         VDD (3.3V)</span></span>
<span class="line"><span>                           |</span></span>
<span class="line"><span>                       [上拉电阻]</span></span>
<span class="line"><span>                           |      (可配置开关)</span></span>
<span class="line"><span>            ┌──────────────┤</span></span>
<span class="line"><span>            |              |</span></span>
<span class="line"><span>            |          +---+---+</span></span>
<span class="line"><span>            |          |       |</span></span>
<span class="line"><span> 引脚 Pin ──┤────[保护二极管]──┤</span></span>
<span class="line"><span>            |          |       |</span></span>
<span class="line"><span>            |          | [P-MOS 上管]</span></span>
<span class="line"><span>            |          |       |</span></span>
<span class="line"><span>            |          +---+---+</span></span>
<span class="line"><span>            |              |         ┌──────────┐</span></span>
<span class="line"><span>            |              +─────────┤ 输出     ├─── ODR (输出数据寄存器)</span></span>
<span class="line"><span>            |              |         │ 驱动器   │        ↑</span></span>
<span class="line"><span>            |          +---+---+     └──────────┘        |</span></span>
<span class="line"><span>            |          |       |              ↑    [多路选择器 MUX]</span></span>
<span class="line"><span>            |          | [N-MOS 下管]         |         ↑</span></span>
<span class="line"><span>            |          |       |        ┌─────┴─────────┤</span></span>
<span class="line"><span>            |          +---+---+        │               │</span></span>
<span class="line"><span>            |              |      [CRL/CRH       复用功能输入</span></span>
<span class="line"><span>            |          [下拉电阻]     配置寄存器]   ←── 片上外设</span></span>
<span class="line"><span>            |              |</span></span>
<span class="line"><span>            |             VSS (0V)</span></span>
<span class="line"><span>            |</span></span>
<span class="line"><span>            |         ┌────+────┐</span></span>
<span class="line"><span>            |         | 施密特   |</span></span>
<span class="line"><span>            +─────────┤ 触发器   |</span></span>
<span class="line"><span>                      └────+────┘</span></span>
<span class="line"><span>                           |</span></span>
<span class="line"><span>                           ↓</span></span>
<span class="line"><span>                      IDR (输入数据寄存器)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br><span class="line-number">29</span><br><span class="line-number">30</span><br><span class="line-number">31</span><br><span class="line-number">32</span><br><span class="line-number">33</span><br></div></div><p>Don&#39;t let this diagram intimidate you; let&#39;s break it down block by block.</p><p><strong>Protection diodes</strong> are the pin&#39;s first line of defense, and also the most easily overlooked part. They are connected between the pin and VDD/VSS, forming a clamping circuit. Under normal operating conditions, the pin voltage is between 0V and 3.3V, and neither protection diode conducts, having no effect on the circuit. But if an abnormality occurs in the external circuit — for example, if 5V is applied to the pin — the upper protection diode will conduct, shunting the excess energy to the VDD power rail and preventing the internal circuitry from being damaged by overvoltage. Similarly, if the pin is pulled to a negative voltage, the lower protection diode will conduct, clamping the pin to VSS. This is a very simple but highly effective protection mechanism. However, the current that protection diodes can withstand is limited, typically specified as injection current in the datasheet, and sustained high current could burn out the diodes. The correct approach is to use a level-shifting chip or a current-limiting resistor for isolation.</p><p><strong>Pull-up and pull-down resistors</strong> are two configurable internal resistors. Note that they are not always connected — whether they are enabled is determined by the configuration bits in the CRL/CRH registers. When a pin is configured in &quot;input pull-up&quot; mode, the switch for the pull-up resistor between VDD and the pin is closed, and the pin is connected to VDD through an internal resistor of approximately 40K ohms. This means the pin will be weakly pulled to a high level when floating. Similarly, in &quot;input pull-down&quot; mode, the pin is connected to VSS through a similar resistor. The resistance of these two resistors is relatively large (in the 30K-50K range), so the pulling force is weak — if there is a stronger external driver (such as a button press directly connecting to GND), the external drive will easily override the effect of the internal pull-up.</p><p><strong>The Schmitt Trigger</strong> is located on the input signal path. Its role is crucial. Signals from the outside world are rarely perfect square waves — they may rise slowly, have glitches, or oscillate near the threshold. If such signals were used to directly trigger digital circuits, it would lead to serious misjudgments. The Schmitt Trigger solves this problem by introducing hysteresis: its rising threshold (for example, 1.7V) and falling threshold (for example, 0.9V) are different. A signal going from low to high must exceed 1.7V to be considered &quot;high,&quot; and going from high to low must fall below 0.9V to be considered &quot;low.&quot; The region between 0.9V and 1.7V is the &quot;uncertain zone,&quot; where the output holds its last determined state unchanged. This design greatly improves noise margin. In analog mode, the Schmitt Trigger is turned off, and the analog signal connects directly to the ADC without being digitized.</p><p><strong>The output driver</strong> is the core of push-pull output. It consists of a P-MOS upper transistor and an N-MOS lower transistor, with the gates of both transistors controlled by the corresponding bit of the Output Data Register (ODR) (after passing through the multiplexer). When a certain bit in the ODR is written as 1, the upper transistor conducts and the lower transistor cuts off, driving the pin to VDD (high level). When a certain bit in the ODR is written as 0, the upper transistor cuts off and the lower transistor conducts, driving the pin to VSS (low level). In open-drain output mode, the P-MOS upper transistor is permanently cut off, and only the N-MOS lower transistor works. The output speed control (MODE bits) actually controls the slew rate of the output driver — the faster the speed, the more rapidly the MOSFETs switch, the steeper the signal edges, but this also generates greater EMI (Electromagnetic Interference) and power supply noise. This is also why we chose <code>Speed::Low</code> in <code>led.hpp</code> — LED blinking doesn&#39;t need high-speed toggling, and a low speed also reduces unnecessary electromagnetic emissions.</p><p><strong>The multiplexer (MUX)</strong> is the &quot;traffic cop&quot; for pin control authority. It decides where the pin&#39;s output drive signal comes from: from the GPIO controller&#39;s ODR register (regular GPIO output), or from an on-chip peripheral (alternate function output). This selection is determined by the CNF bits in the CRL/CRH registers. When CNF is configured for alternate function, the MUX connects the peripheral&#39;s output signal to the driver, and the ODR&#39;s control is bypassed. This is why, after configuring an alternate function, you no longer need to manually manipulate the ODR — the peripheral hardware automatically controls the pin&#39;s signal.</p><p><strong>The CRL/CRH configuration registers</strong> are the &quot;control center&quot; of the entire GPIO. Every 4 bits control one pin&#39;s MODE (speed/output enable) and CNF (specific mode configuration). We will analyze the bit field meanings of these registers in detail shortly.</p><h2 id="the-relationship-between-pins-and-registers" tabindex="-1">The Relationship Between Pins and Registers <a class="header-anchor" href="#the-relationship-between-pins-and-registers" aria-label="Permalink to &quot;The Relationship Between Pins and Registers&quot;">​</a></h2><p>After understanding the internal structure of GPIO, let&#39;s now turn our attention to the registers that are actually manipulated by the program. Each GPIO group (GPIOA through GPIOE) has seven 32-bit registers in the memory address space, arranged at fixed offsets. Let&#39;s use GPIOC as an example — because our LED is connected to PC13.</p><p>The base address of GPIOC is <code>0x40011000</code>. This address is not arbitrarily assigned — it lies within the STM32&#39;s APB2 (Advanced Peripheral Bus) address space, and all GPIO peripherals are attached to the APB2 bus. Starting from the base address, the seven registers are arranged as follows.</p><p><strong>The CRL register (offset 0x00, full address 0x40011000)</strong> is responsible for configuring Pin0 through Pin7, the eight lower-numbered pins. This is a 32-bit register where every 4 bits control one pin, corresponding to Pin0, Pin1, ..., Pin7 from least significant bit to most significant bit. Within each 4-bit group, the lower 2 bits are called MODE, and the upper 2 bits are called CNF. The MODE bits determine the pin&#39;s output speed (in output mode) or input mode flag (in input mode, MODE=00). The CNF bits determine the specific sub-mode — for example, in input mode whether it is floating input or pull-up input, and in output mode whether it is push-pull or open-drain.</p><p><strong>The CRH register (offset 0x04, full address 0x40011004)</strong> is completely symmetrical to CRL, except that it handles Pin8 through Pin15, the eight higher-numbered pins. The structure is identical — every 4 bits control one pin, corresponding to Pin8, Pin9, ..., Pin15 from least significant bit to most significant bit.</p><p>Let&#39;s do the math using our PC13 as an example. PC13 is pin number 13 of the GPIOC group, and since 13 &gt;= 8, it is controlled by the CRH register. In CRH, Pin8 occupies bits [3:0], Pin9 occupies bits [7:4], and so on. PC13 corresponds to the (13-8)=5th group of 4 bits, which is bits [23:20] of CRH. If we want to configure PC13 as push-pull output at 2MHz, the MODE bits should be <code>10</code> (2MHz), and the CNF bits should be <code>00</code> (general-purpose push-pull output), combined as <code>0010</code>, written to bits [23:20] of CRH. The <code>HAL_GPIO_Init()</code> function in the HAL library is essentially doing these bit-field operations for us under the hood. The <code>Base::setup(Base::Mode::OutputPP, Base::PullPush::NoPull, Base::Speed::Low)</code> we call in <code>gpio.hpp</code> ultimately writes these values to bits [23:20] of CRH through the HAL library.</p><p><strong>The IDR register (offset 0x08, full address 0x40011008)</strong> is the Input Data Register, a read-only register. Its lower 16 bits correspond to the current level state of Pin0 through Pin15, respectively. If Pin13 is currently at a high level, bit 13 of the IDR is 1; if it is at a low level, bit 13 is 0. When you read a button state in input mode, the underlying operation is reading this register. Regardless of what mode the pin is configured in (except analog mode), the IDR continuously reflects the actual level state on the pin.</p><p><strong>The ODR register (offset 0x0C, full address 0x4001100C)</strong> is the Output Data Register, which is both readable and writable. In GPIO output mode, each bit of the ODR directly controls the level of the corresponding pin. Writing 1 outputs a high level, and writing 0 outputs a low level. But directly modifying the ODR has a hidden danger — a read-modify-write operation on the ODR is not atomic. If your program is interrupted while modifying Pin13, and the interrupt handler modifies another pin in the same group (such as Pin12), then Pin12&#39;s modification may be overwritten when the interrupt returns. To solve this problem, STM32 designed the BSRR and BRR registers.</p><p><strong>The BSRR register (offset 0x10, full address 0x40011010)</strong> is the Port Bit Set/Reset Register, which provides an atomic way to modify the ODR. The lower 16 bits (bit0 to bit15) of BSRR are &quot;set bits&quot; — writing a 1 to a certain bit sets the corresponding ODR bit to 1 (pin outputs high level), while writing 0 has no effect. The upper 16 bits (bit16 to bit31) of BSRR are &quot;reset bits&quot; — writing a 1 to a certain bit clears the corresponding ODR bit to 0 (pin outputs low level), while writing 0 has no effect. The key point is that this operation is atomic — no read-modify-write is needed, and a single write can precisely control the specified bits without affecting other bits.</p><p>For example, to make PC13 output a high level, we can write <code>0x2000</code> to BSRR (setting bit 13 to 1), and to output a low level we write <code>0x20000000</code> (setting bit 29, which is 13+16, to 1). This is the underlying implementation logic of <code>HAL_GPIO_WritePin()</code>, and also the hardware operation ultimately called by the <code>set_gpio_pin_state()</code> method in our <code>gpio.hpp</code>.</p><p><strong>The BRR register (offset 0x14, full address 0x40011014)</strong> is the Port Bit Reset Register, functionally equivalent to taking the upper 16 bits of BSRR out separately — writing a 1 to the lower 16 bits clears the corresponding ODR bit. It was commonly used in early firmware libraries, but with BSRR available, BRR became redundant because BSRR already covers both set and reset operations.</p><p><strong>The LCKR register (offset 0x18, full address 0x40011018)</strong> is the Configuration Lock Register. Its purpose is to lock the GPIO configuration — once locked, the corresponding CRL/CRH bits cannot be modified again until the next system reset. This is very useful in production-level code: after initialization is complete, lock the configuration to prevent the program from accidentally modifying GPIO settings and causing hardware damage if it runs astray. The locking operation requires following a specific write sequence, which is a kind of anti-misoperation protection mechanism in the hardware design.</p><blockquote><p>⚠️ Pitfall warning: When using the BSRR register, remember the rule &quot;writing 1 takes effect, writing 0 has no effect.&quot; This means you can safely write any value to BSRR without worrying about accidentally affecting other pins. But if you directly manipulate the ODR register, you must use a read-modify-write approach, which is unsafe in multithreaded or interrupt environments. Therefore, a good habit in embedded development is to prefer using BSRR to control output pins.</p></blockquote><h2 id="wrapping-up-and-a-preview" tabindex="-1">Wrapping Up and a Preview <a class="header-anchor" href="#wrapping-up-and-a-preview" aria-label="Permalink to &quot;Wrapping Up and a Preview&quot;">​</a></h2><p>At this point, we have traversed the complete chain of GPIO, from physical circuits to the programming interface. We know that GPIO has four operating modes — input, output, alternate function, and analog — and each mode corresponds to a specific hardware signal path and register configuration, with each mode&#39;s existence serving an irreplaceable purpose. Through the internal block diagram, we saw how hardware units like protection diodes, the Schmitt Trigger, the push-pull driver, and the multiplexer work together. We also went through the addresses, offsets, and functions of the seven key registers (CRL, CRH, IDR, ODR, BSRR, BRR, LCKR) one by one, and using PC13 as a concrete example, we traced the complete path from C++ code to the underlying registers — from the <code>0x2000</code> bit mask of <code>GPIO_PIN_13</code>, to bits [23:20] of CRH, to the atomic operations of BSRR, every step corresponding to actual hardware behavior.</p><p>GPIO is the foundation of embedded development. The serial communication, SPI bus, I2C protocol, PWM control, and ADC sampling that we will cover later are all built on top of GPIO. Alternate function mode allows pins to &quot;transform&quot; into channels for various peripherals, and analog mode allows pins to handle continuous voltage signals, but regardless of the mode, the pin&#39;s physical structure, protection mechanisms, and configuration methods are all interconnected. Once you understand GPIO, you hold the key to understanding the entire STM32 peripheral system.</p><p>In the next article, we will focus on the specific scenario of LED control. We will dive deep into the working details of push-pull output mode — how the P-MOS and N-MOS alternately conduct, what the output speed setting means, and why <code>Speed::Low</code> is sufficient for LED control. More importantly, we will look at the special circuit design of PC13 on the Blue Pill development board — why is the on-board LED active-low instead of active-high? What kind of circuit considerations lie behind this seemingly counterintuitive design? Once you understand these, you will see why we need the <code>ActiveLevel::Low</code> template parameter in our <code>led.hpp</code>, and how it cleverly encapsulates hardware differences.</p>`,57)])])}const g=t(a,[["render",o]]);export{u as __pageData,g as default};
