import{_ as r,C as l,o,c as h,j as i,a as s,E as t,a2 as n}from"./chunks/framework.CZcFxxdw.js";const y=JSON.parse('{"title":"Reading Assembly and Register ABI","description":"CppCon 2025 Talk Notes — C++: Some Assembly Required by Matt Godbolt","frontmatter":{"title":"Reading Assembly and Register ABI","description":"CppCon 2025 Talk Notes — C++: Some Assembly Required by Matt Godbolt","conference":"cppcon","conference_year":2025,"talk_title":"C++: Some Assembly Required","speaker":"Matt Godbolt","video_bilibili":"https://www.bilibili.com/video/BV1ptCCBKEwW?p=2","video_youtube":"https://www.youtube.com/watch?v=zoYT7R94S3c","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","cpp_standard":[17,20],"chapter":2,"order":2,"translation":{"source":"documents/vol10-open-lecture-notes/cppcon/2025/02-some-assembly-required/02-reading-assembly-and-registers-abi.md","source_hash":"f72bb0aec38545fc3b89c84775652133ee2e5d0e2e401a61e5d984514ad87f16","translated_at":"2026-05-20T04:37:22.047171+00:00","engine":"anthropic","token_count":5510}},"headers":[],"relativePath":"en/vol10-open-lecture-notes/cppcon/2025/02-some-assembly-required/02-reading-assembly-and-registers-abi.md","filePath":"en/vol10-open-lecture-notes/cppcon/2025/02-some-assembly-required/02-reading-assembly-and-registers-abi.md","lastUpdated":null}'),p={name:"en/vol10-open-lecture-notes/cppcon/2025/02-some-assembly-required/02-reading-assembly-and-registers-abi.md"};function d(c,e,u,k,g,m){const a=l("RefLink");return o(),h("div",null,[e[26]||(e[26]=i("h1",{id:"reading-assembly-building-intuition-from-scratch",tabindex:"-1"},[s("Reading Assembly: Building Intuition from Scratch "),i("a",{class:"header-anchor",href:"#reading-assembly-building-intuition-from-scratch","aria-label":'Permalink to "Reading Assembly: Building Intuition from Scratch"'},"​")],-1)),i("p",null,[e[0]||(e[0]=s("Faced with a screen full of ",-1)),e[1]||(e[1]=i("code",null,"mov",-1)),e[2]||(e[2]=s(", ",-1)),e[3]||(e[3]=i("code",null,"add",-1)),e[4]||(e[4]=s(", and ",-1)),e[5]||(e[5]=i("code",null,"jmp",-1)),e[6]||(e[6]=s(" alongside a bunch of incomprehensible register names, a beginner's first instinct is usually to close the tab. When a template error pops up, we can at least search Stack Overflow, but assembly output looks like gibberish—there's no obvious starting point. However, if we run some targeted experiments using Compiler Explorer",-1)),t(a,{id:1,preview:"Matt Godbolt, Compiler Explorer, godbolt.org, 2012"}),e[7]||(e[7]=s(`, we quickly find that assembly can actually be "half-read, half-guessed." We don't need to know how to write it.`,-1))]),e[27]||(e[27]=n(`<h2 id="setting-the-stage" tabindex="-1">Setting the Stage <a class="header-anchor" href="#setting-the-stage" aria-label="Permalink to &quot;Setting the Stage&quot;">​</a></h2><p>All the experiments below were done on Compiler Explorer (godbolt.org). For compilers, we used GCC 16.1.1 for x86-64, the aarch64 version of GCC 16.1.1 for ARM64, and the riscv64 version of GCC 16.1.1 for RISC-V. The operating system is consistently set to Linux, because the calling convention on Windows is different, which leads to differences in the assembly output—we&#39;ll discuss this in detail later. We primarily look at the <code>-O2</code> optimization level, occasionally switching to <code>-O0</code> for comparison, and we&#39;ll explain why later.</p><h2 id="starting-with-the-simplest-possible-function" tabindex="-1">Starting with the Simplest Possible Function <a class="header-anchor" href="#starting-with-the-simplest-possible-function" aria-label="Permalink to &quot;Starting with the Simplest Possible Function&quot;">​</a></h2><p>To understand what assembly actually looks like across different architectures, we start with the most basic <code>square</code> function—taking an input integer, multiplying it by itself, and returning the result. The more plain the function, the better it is for observing compiler behavior: the logic is simple, the assembly is short, and the purpose of every single instruction is obvious.</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> square</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Intuitively, regardless of the CPU architecture, since we&#39;re doing the exact same thing, the compiled assembly should look more or less the same. But when we place the three architectures side by side in Compiler Explorer, we find they look completely different—the instruction formats, register names, and even the way multiplication is implemented are all different. But upon closer observation, a key pattern emerges: although they look different, the skeleton is actually the same—fetch the parameter from somewhere, perform the operation, and put the result in an agreed-upon location to return. Once we understand this skeleton, reading assembly is no longer intimidating.</p><h2 id="the-x86-64-version" tabindex="-1">The x86-64 Version <a class="header-anchor" href="#the-x86-64-version" aria-label="Permalink to &quot;The x86-64 Version&quot;">​</a></h2><p>Let&#39;s look at x86-64 first, since most development machines run this architecture. Under <code>-O2</code> optimization, GCC generates the following code:</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">square(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        imul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    edi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">edi</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">edi</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div>`,9)),i("p",null,[e[8]||(e[8]=s("Seeing this code for the first time might leave us wondering: shouldn't the parameter be on the stack? Why is it being read directly from ",-1)),e[9]||(e[9]=i("code",null,"edi",-1)),e[10]||(e[10]=s("? This is dictated by the System V AMD64 ABI",-1)),t(a,{id:2,preview:"System V Application Binary Interface, AMD64 Architecture, x86-64 psABI"}),e[11]||(e[11]=n(" (the calling convention for x86-64 on Linux)—the first few integer parameters of a function are passed via registers, with the first parameter in <code>edi</code> and the return value in <code>eax</code>. So the meaning of these three instructions is quite clear: <code>imul edi, edi</code> is the two-operand form of x86 multiplication—the left operand is both the source and the destination, multiplying the value in <code>edi</code> by itself and writing the result back to <code>edi</code>. Then it moves the result to <code>eax</code> as the return value, and finally <code>ret</code> returns.",15))]),e[28]||(e[28]=n("<p>A natural question follows: why not let the result of <code>imul</code> land directly in <code>eax</code>, avoiding the extra <code>mov</code>? In reality, the two-operand form of <code>imul</code> writes the result back to the first operand (which is <code>edi</code>), and the calling convention requires the return value to be in <code>eax</code>, so that <code>mov</code> is unavoidable. If we let the compiler use <code>imul eax, edi</code> (multiplying <code>edi</code> into <code>eax</code>), we could indeed eliminate <code>mov</code>, but that would require moving <code>edi</code> into <code>eax</code> before doing the multiplication. The instruction count would be the same, and GCC chose the former strategy.</p>",1)),i("p",null,[e[12]||(e[12]=s("Another easy pitfall: if we compile the same code on Windows, the parameter would be in ",-1)),e[13]||(e[13]=i("code",null,"ecx",-1)),e[14]||(e[14]=s(" instead of ",-1)),e[15]||(e[15]=i("code",null,"edi",-1)),e[16]||(e[16]=s(", though the return value would still be in ",-1)),e[17]||(e[17]=i("code",null,"eax",-1)),e[18]||(e[18]=s(". This is one of the biggest differences between Windows x64",-1)),t(a,{id:3,preview:"Microsoft, x64 Calling Convention, RCX/RDX/R8/R9"}),e[19]||(e[19]=s(" and Linux x86-64—different calling conventions. If we understand an assembly snippet on Linux and then compile it with MSVC on Windows, we'll find all the registers have changed. We didn't read it wrong; it's just a difference in calling conventions. So when reading assembly, the first step is to confirm the platform and calling convention—this saves a lot of confusion.",-1))]),e[29]||(e[29]=i("h2",{id:"the-arm64-version",tabindex:"-1"},[s("The ARM64 Version "),i("a",{class:"header-anchor",href:"#the-arm64-version","aria-label":'Permalink to "The ARM64 Version"'},"​")],-1)),i("p",null,[e[20]||(e[20]=s("Next, let's look at ARM64, also known as AArch64",-1)),t(a,{id:4,preview:"ARM, AArch64 Architecture Reference Manual, ARMv8"}),e[21]||(e[21]=s(". For the same function, GCC aarch64 at ",-1)),e[22]||(e[22]=i("code",null,"-O2",-1)),e[23]||(e[23]=s(" produces this output:",-1))]),e[30]||(e[30]=n(`<div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">square(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        mul</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">     w0, w0, w0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>This code has only two instructions, even cleaner than x86-64. <code>w0</code> is the register in ARM64 that holds the first integer parameter and the return value (the 32-bit version; the 64-bit version is called <code>x0</code>). Because the parameter is <code>int</code>, 32 bits are sufficient, so the compiler uses the <code>w</code> register instead of the <code>x</code> register. The <code>mul</code> instruction directly puts the result of <code>w0</code> multiplied by <code>w0</code> back into <code>w0</code>, then returns—no redundant <code>mov</code> needed. ARM64&#39;s instruction design allows the result to be flexibly placed in any of the operand positions.</p><p>It&#39;s worth noting that ARM64&#39;s register naming is much more regular than x86-64&#39;s. On the x86-64 side, <code>eax</code>, <code>edi</code>, and <code>rsi</code> are all different, requiring rote memorization of each register&#39;s special purpose. In ARM64, it&#39;s simply <code>x0</code> through <code>x30</code> plus a stack pointer <code>sp</code>, with the 32-bit versions uniformly getting a <code>w</code> prefix—very neat. This regular naming convention lowers the barrier to entry—there&#39;s no need to memorize a bunch of legacy names; we just need to know that <code>x0</code>/<code>w0</code> are for parameters and return values.</p><h2 id="the-risc-v-version" tabindex="-1">The RISC-V Version <a class="header-anchor" href="#the-risc-v-version" aria-label="Permalink to &quot;The RISC-V Version&quot;">​</a></h2>`,4)),i("p",null,[e[24]||(e[24]=s("Finally, we have RISC-V",-1)),t(a,{id:5,preview:"RISC-V International, RISC-V ISA Specification, 2019"}),e[25]||(e[25]=s(` (the V stands for the Roman numeral five, so it's pronounced "risk-five"). Its assembly looks like this:`,-1))]),e[31]||(e[31]=n(`<div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">square(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        mul</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">     a0, a0, a0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Wait, this is almost identical to ARM64? It sure is. <code>a0</code> in RISC-V is the register that holds the first parameter and the return value (<code>a</code> stands for argument). <code>mul</code> does the multiplication, the result goes back into <code>a0</code>, and then it returns. Two instructions, clean and simple.</p><p>As the youngest instruction set architecture, RISC-V&#39;s design drew on the lessons of its predecessors. Its integer registers are simply called <code>x0</code> through <code>x31</code>, and the ABI assigns them aliases: <code>a0</code>-<code>a7</code> are parameter/return value registers, <code>t0</code>-<code>t6</code> are temporary registers, and <code>s0</code>-<code>s11</code> are callee-saved registers. What we see in assembly are the aliases, but fundamentally they are just <code>x</code> numbers. This design of &quot;unified underlying numbers + upper-level semantic aliases&quot; is much easier to understand than x86-64&#39;s approach where every register has a unique name.</p><h2 id="looking-back-they-re-actually-saying-the-same-thing" tabindex="-1">Looking Back: They&#39;re Actually Saying the Same Thing <a class="header-anchor" href="#looking-back-they-re-actually-saying-the-same-thing" aria-label="Permalink to &quot;Looking Back: They&#39;re Actually Saying the Same Thing&quot;">​</a></h2><p>Placing the three architectures side by side reveals an interesting phenomenon: although the instruction names, register names, and instruction counts are all different, the &quot;semantics&quot; they express are exactly the same—&quot;fetch parameter → multiply → place return value → return.&quot; We don&#39;t need to recognize every single instruction to read assembly; as long as we grasp which registers the data flows between and what operation is being performed, we can roughly guess what it&#39;s doing.</p><p>It&#39;s like reading a poem written in an unfamiliar language—we don&#39;t need to look up every word to feel its rhythm and general meaning from the position and repetition patterns of the words. Assembly is the same way—seeing <code>mul</code> or <code>imul</code> tells us a multiplication is happening, seeing <code>ret</code> tells us the function is about to return, and seeing data move from one register to another tells us something is being passed along. This ability to &quot;half-read, half-guess&quot; is far more practical than rote memorization of every instruction&#39;s exact semantics.</p><h2 id="a-crucial-reminder-optimization-level-completely-changes-what-you-see" tabindex="-1">A Crucial Reminder: Optimization Level Completely Changes What You See <a class="header-anchor" href="#a-crucial-reminder-optimization-level-completely-changes-what-you-see" aria-label="Permalink to &quot;A Crucial Reminder: Optimization Level Completely Changes What You See&quot;">​</a></h2><p>Everything shown above was output under <code>-O2</code>. If we turn off optimization (<code>-O0</code>), we see a completely different picture—massive amounts of <code>push</code>, <code>pop</code>, memory reads and writes, with parameters being stored to the stack and read back, and intermediate results being repeatedly written to memory. The reason <code>-O0</code> assembly is so verbose is that <code>-O0</code> aims to allow debuggers to precisely map every C++ statement to assembly instructions, so it performs no optimization at all, faithfully placing all variables in memory. <code>-O2</code> is what the compiler &quot;really&quot; wants to generate. If our goal is to understand the compiler&#39;s optimization behavior and the actual performance of the code, we must look at the output from <code>-O2</code> or higher optimization levels; <code>-O0</code> will only lead us astray.</p><p>At this point, we&#39;ve gone through the assembly of the simplest function across three mainstream architectures. Even though it&#39;s just a <code>square</code> function, it helped us establish an important cognitive framework: knowing where parameters come from, where results go, and which instruction performs the core operation. With this framework in place, we won&#39;t be completely lost when looking at more complex function assembly later. Now, let&#39;s take this foundation and look at some more realistic scenarios.</p><hr><h1 id="what-exactly-is-the-relationship-between-machine-code-and-assembly" tabindex="-1">What Exactly Is the Relationship Between Machine Code and Assembly <a class="header-anchor" href="#what-exactly-is-the-relationship-between-machine-code-and-assembly" aria-label="Permalink to &quot;What Exactly Is the Relationship Between Machine Code and Assembly&quot;">​</a></h1><p>Many people use &quot;machine code&quot; and &quot;assembly code&quot; interchangeably, figuring they&#39;re both just incomprehensible stuff anyway. But if we look closely at objdump output, the column of <code>0f af ff</code> on the left and the column of <code>imul edi, edi</code> on the right actually have a very straightforward one-to-one mapping relationship—it&#39;s just something we rarely think about.</p><h2 id="clarifying-the-concepts-machine-code-is-for-machines-assembly-is-for-humans" tabindex="-1">Clarifying the Concepts: Machine Code Is for Machines, Assembly Is for Humans <a class="header-anchor" href="#clarifying-the-concepts-machine-code-is-for-machines-assembly-is-for-humans" aria-label="Permalink to &quot;Clarifying the Concepts: Machine Code Is for Machines, Assembly Is for Humans&quot;">​</a></h2><p>That pile of hex numbers on the left—<code>0f</code>, <code>af</code>, <code>ff</code>, and the like—is machine code. In essence, it&#39;s a string of bytes in memory. The CPU reads these bytes directly and interprets them according to rules hardwired into the hardware: when it reads <code>0f af</code>, it knows this is a multiplication instruction, and the following bytes tell it where the operands are. The CPU doesn&#39;t know what <code>imul</code> is; it only understands numbers.</p><p>The column of <code>imul edi, edi</code> on the right is the assembly code, the human-readable version. It has an essentially one-to-one mapping with machine code—one assembly instruction corresponds to a fixed-format sequence of machine code bytes. So we can &quot;assemble&quot; assembly code into machine code (which is what an assembler does), and we can &quot;disassemble&quot; machine code back into assembly code (which is what tools like objdump and IDA do). Of course, when disassembling back, the comments are gone, the variable names are gone, and semantic information like <code>int x = n * n</code> is completely lost—all that remains are cold, hard instructions.</p><p>But this bidirectional conversion path exists, and it&#39;s very direct. Assembly is not a &quot;high-level language&quot; that requires a compiler to do complex translation—it&#39;s almost just another way of writing machine code.</p><h2 id="writing-the-simplest-square-function-by-hand-to-see-what-the-assembly-looks-like" tabindex="-1">Writing the Simplest Square Function by Hand to See What the Assembly Looks Like <a class="header-anchor" href="#writing-the-simplest-square-function-by-hand-to-see-what-the-assembly-looks-like" aria-label="Permalink to &quot;Writing the Simplest Square Function by Hand to See What the Assembly Looks Like&quot;">​</a></h2><p>To figure out the register situation, let&#39;s start with the most plain square function:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// square.cpp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> square</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> n</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Then compile it with gcc into an object file, without linking, just to look at the assembly:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 我的环境：Arch Linux WSL, x86-64, gcc 16.1.1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">g++</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square.cpp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square.o</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -M</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> intel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square.o</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>We add <code>-M intel</code> because AT&amp;T syntax (where operands come after the instruction and have <code>%</code> prefixes) isn&#39;t very intuitive; Intel syntax at least has operand orders that match our intuition. <code>-O0</code> turns off all optimization so the compiler won&#39;t rewrite the code in any way, letting us see the most raw translation result.</p><p>The output looks roughly like this (GCC 16, -O0):</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0000000000000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;_Z6squarei&gt;:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">55</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                      push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   rbp</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">48</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 89</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> e5                </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    rbp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rsp</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">89</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 7d</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fc                </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mov</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    DWORD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PTR [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rbp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0x4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">edi</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   7</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   8b </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">45</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fc                </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DWORD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PTR [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rbp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0x4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">   a:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   0f af c0                </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">imul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eax</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">   d:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   5d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                      pop</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    rbp</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">   e:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   c3                      </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Our first reaction upon seeing this might be: wait, shouldn&#39;t the input parameter be &quot;passed in&quot; from somewhere? A C++ function has a parameter list, but assembly has no such thing. Where did the parameter go?</p><h2 id="registers-are-the-cpu-s-built-in-global-variables-but-their-use-has-rules" tabindex="-1">Registers Are the CPU&#39;s Built-in &quot;Global Variables,&quot; But Their Use Has Rules <a class="header-anchor" href="#registers-are-the-cpu-s-built-in-global-variables-but-their-use-has-rules" aria-label="Permalink to &quot;Registers Are the CPU&#39;s Built-in &quot;Global Variables,&quot; But Their Use Has Rules&quot;">​</a></h2><p>Inside the CPU, there is a small batch of extremely fast storage units called registers. We can think of them as a kind of &quot;ultra-high-speed global variable&quot;—located directly inside the CPU, requiring no memory access, with reads and writes that are nearly zero-latency. But unlike global variables, the number of registers is extremely limited; on x86-64, there are only a dozen or so general-purpose registers (RAX, RBX, RCX, RDX, RSI, RDI, R8-R15), making it impossible to fit all data into them.</p><p>The key question is: who dictates which register does what? If compiler A thinks parameters should go in RAX, and compiler B thinks they should go in RDI, then code compiled by the two couldn&#39;t call each other at all. We write a library, someone else writes a program, and because the register usage is inconsistent, the call would fail.</p><p>So there must be a set of &quot;traffic rules&quot; that everyone follows, so that code can interoperate. This set of rules is the ABI (Application Binary Interface). The ABI specifies many things, but the most fundamental rule from the perspective of reading assembly is this: during a function call, which register holds the parameters, which register holds the return value, which registers can be freely modified after the call, and which must be restored to their original state.</p><p>Linux uses the System V AMD64 ABI, while Windows uses Microsoft&#39;s own x64 ABI—the two sets of rules are different. This is one of the reasons why Linux and Windows binaries can&#39;t be directly mixed (there are of course more reasons, but the difference in register conventions is the most direct layer).</p><h2 id="parameters-come-in-through-edi-results-must-go-out-through-eax" tabindex="-1">Parameters Come In Through EDI, Results Must Go Out Through EAX <a class="header-anchor" href="#parameters-come-in-through-edi-results-must-go-out-through-eax" aria-label="Permalink to &quot;Parameters Come In Through EDI, Results Must Go Out Through EAX&quot;">​</a></h2><p>Returning to our square function. Under the System V ABI rules, the first integer parameter is placed in the RDI register. Note that we wrote RDI (64-bit), but our parameter is <code>int</code>, which is only 32 bits, so we actually use the lower 32 bits of RDI, which is EDI. The same logic applies to RAX/EAX: RAX is the 64-bit version, and EAX is the 32-bit version.</p><p>So when the function is entered, the value of <code>n</code> is already in EDI—we don&#39;t need to &quot;fetch&quot; it from anywhere; it&#39;s already there.</p><p>Then look at the instruction sequence: <code>push rbp; mov rbp, rsp</code> is the standard stack frame setup process, and <code>mov DWORD PTR [rbp-0x4], edi</code> stores the parameter from EDI onto the stack—this is typical <code>-O0</code> behavior, where the compiler performs no optimization and faithfully places all variables in memory. Next, <code>mov eax, DWORD PTR [rbp-0x4]</code> reads it back from the stack into EAX, <code>imul eax, eax</code> does the squaring, <code>pop rbp</code> restores the stack frame, and finally <code>ret</code> returns. The verbosity of <code>-O0</code> perfectly illustrates why we recommended looking at <code>-O2</code> output earlier—with three extra stack frame manipulation instructions, the core logic gets buried.</p><p>Then <code>imul eax, eax</code> multiplies EAX by EAX, storing the result back in EAX. This is a very distinctive feature of x86: most instructions only accept two operands, and the left operand is both the source and the destination. This is the same idea as <code>a *= a</code> in C++—read the value on the left, perform an operation with the value on the right, and write the result back to the left. It&#39;s a &quot;destructive&quot; operation; once it&#39;s done, the original value on the left is overwritten. If we need the original value later, we have to save it beforehand.</p><p>Finally, <code>ret</code> is the return, handing control back to the caller. At this point, EAX holds the squared result, and the caller knows to grab it from EAX—because that&#39;s what the ABI dictates.</p><h2 id="register-names-are-not-arbitrary" tabindex="-1">Register Names Are Not Arbitrary <a class="header-anchor" href="#register-names-are-not-arbitrary" aria-label="Permalink to &quot;Register Names Are Not Arbitrary&quot;">​</a></h2><p>Beginners seeing a bunch of names like RAX, EAX, AX, and AL might easily think they are different registers. In reality, they are different &quot;views&quot; of the same physical register: RAX is the full 64 bits, EAX is the lower 32 bits, AX is the lower 16 bits, and AL is the lowest 8 bits. Writing data to EAX overwrites the upper 32 bits of RAX (zeroing them), while writing to AL only changes the lowest byte, leaving the remaining bits unaffected.</p><p>This characteristic is particularly prone to causing confusion during debugging. When staring at the register window, we might notice that the value of RAX doesn&#39;t match EAX, and wonder if the debugger is glitching. In reality, it&#39;s because some instruction only modified the lower 32 bits, and the upper 32 bits are dirty data left over from a previous operation. So when looking at registers, we must be clear about which &quot;view&quot; we&#39;re currently looking at.</p><p>At this point, the assembly face of the simplest C++ function on x86-64 is clear: parameters are passed in via registers (not the stack—at least the first few parameters aren&#39;t), computation happens between registers, and results are returned via registers. The entire process involves no memory access and is extremely fast. Of course, this is just the simplest case; things get much more complicated when there are more parameters, local variables, or when optimization is enabled, but the basic framework remains the same.</p><hr><h1 id="understanding-register-parameter-passing-from-a-single-mov-instruction-arm-and-risc-v-calling-conventions" tabindex="-1">Understanding Register Parameter Passing from a Single MOV Instruction: ARM and RISC-V Calling Conventions <a class="header-anchor" href="#understanding-register-parameter-passing-from-a-single-mov-instruction-arm-and-risc-v-calling-conventions" aria-label="Permalink to &quot;Understanding Register Parameter Passing from a Single MOV Instruction: ARM and RISC-V Calling Conventions&quot;">​</a></h1><p>In the previous section, we looked at that square function, and after compilation, the core was just a single multiplication instruction. When the function returns, control goes back to the caller. The caller had previously stuffed the parameter into the EDI register (per the x86-64 calling convention), and now it expects to get the return value from the EAX register—this is the x86-64 rule: integer return values go through EAX (or RAX). So what that <code>imul edi, edi</code> does is very straightforward: it multiplies the value in EDI by itself, writes the result back to EDI, then moves it to EAX, and finally returns. The caller grabs it from EAX, and we&#39;re done.</p><p>So the question arises: how big is the &quot;felt&quot; difference when doing the same thing on different architectures? If we compile the same function on all three architectures and compare the assembly instruction by instruction, the differences are very obvious.</p><h2 id="the-cleanliness-of-arm64" tabindex="-1">The Cleanliness of ARM64 <a class="header-anchor" href="#the-cleanliness-of-arm64" aria-label="Permalink to &quot;The Cleanliness of ARM64&quot;">​</a></h2><p>Let&#39;s look at ARM64 (AArch64) first. Some people might think ARM assembly is similar to x86, just with different instruction names. But actually opening objdump reveals that the differences far exceed expectations.</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// square.cpp —— 就这么个简单函数</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> square</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Run it with a cross-compilation toolchain:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ARM64</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aarch64-linux-gnu-g++</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square.cpp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_arm64.o</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aarch64-linux-gnu-objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_arm64.o</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The output looks like this:</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">square:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    mul</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> w0, w0, w0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>That&#39;s it. Two instructions, clean as a whistle. One particularly nice thing is that W0 is both the input and the output. In ARM&#39;s calling convention, W0 (32-bit) or X0 (64-bit) serves as both the carrier for the first parameter and the return value. So <code>mul w0, w0, w0</code> reads as &quot;multiply w0 by w0, put the result back in w0&quot;—all three operands are the exact same register, visually extremely uniform.</p><p>Next, let&#39;s look at the machine code for these instructions, which reveals an important design difference.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aarch64-linux-gnu-objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .text</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_arm64.o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mul</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 0:   1b007c00    mul w0, w0, w0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>1b007c00</code>, four bytes. Now look at that <code>ret</code>:</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 4:   d65f03c0    ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>d65f03c0</code>, also four bytes. Two instructions, both exactly four bytes. This means the instruction decoder&#39;s job is particularly simple: the instruction fetch stage always fetches four bytes at a time, without needing to do any length determination. The elegance of this design becomes even more apparent when contrasted with x86.</p><h2 id="x86-s-variable-length-instructions" tabindex="-1">x86&#39;s Variable-Length Instructions <a class="header-anchor" href="#x86-s-variable-length-instructions" aria-label="Permalink to &quot;x86&#39;s Variable-Length Instructions&quot;">​</a></h2><p>The same function, compiled for x86-64:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">g++</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square.cpp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_x64.o</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_x64.o</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">square(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   0f af ff                </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">imul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   edi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">edi</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">89</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f8                   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">edi</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   c3                      </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The focus here is on the byte length of the instructions:</p><ul><li><code>imul</code> instruction: <code>0f af ff</code>, three bytes</li><li><code>mov</code> instruction: <code>89 f8</code>, two bytes</li><li><code>ret</code> instruction: <code>c3</code>, one byte</li></ul><p>Three instructions, three different lengths: 3, 2, 1. If we use a different multiplication variant, like <code>imul eax, edi</code>, its machine code is <code>0f af c7</code>, still three bytes, but with a different suffix than the imul above (<code>ff</code> vs <code>c7</code>) because the operand encoding is different. Change the scenario again, and if the multiplier is an immediate value, the instruction length changes yet again.</p><p>&quot;Variable-length instructions&quot; aren&#39;t just a textbook concept. Counting bytes against a hex dump reveals that every time the CPU&#39;s front end fetches an instruction, it has to read the first few bytes to determine exactly how long that instruction is, and only then can it decide where the next instruction starts. x86&#39;s decoder is notoriously complex, and Intel has stuffed massive amounts of pre-decode logic and micro-op caches into its CPUs to solve this problem—essentially using hardware brute force to compensate for the historical baggage of the instruction set design.</p><h2 id="risc-v-s-fixed-length-instructions" tabindex="-1">RISC-V&#39;s Fixed-Length Instructions <a class="header-anchor" href="#risc-v-s-fixed-length-instructions" aria-label="Permalink to &quot;RISC-V&#39;s Fixed-Length Instructions&quot;">​</a></h2><p>Now let&#39;s look at RISC-V (rv64gc):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">riscv64-linux-gnu-g++</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square.cpp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_rv64.o</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">riscv64-linux-gnu-objdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> square_rv64.o</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">square:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   02b50533    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mul</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a0, a0, a0</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8082</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Just like ARM, a0 is both the first parameter and the return value, and the <code>mul a0, a0, a0</code> semantics are completely identical. However, there&#39;s a detail here: the <code>mul</code> instruction is four bytes (<code>02b50533</code>), but the <code>ret</code> instruction is only two bytes (<code>8082</code>). RISC-V&#39;s base instructions are fixed-length four-byte, but it supports the 16-bit compressed instruction extension (RVC), so common instructions like <code>ret</code> get compressed into two bytes. This can be considered a compromise between fixed-length and variable-length—still much more disciplined than x86&#39;s &quot;completely unpredictable&quot; variable-length approach.</p><h2 id="operand-count-not-all-instructions-are-so-neat" tabindex="-1">Operand Count: Not All Instructions Are So Neat <a class="header-anchor" href="#operand-count-not-all-instructions-are-so-neat" aria-label="Permalink to &quot;Operand Count: Not All Instructions Are So Neat&quot;">​</a></h2><p>At this point, we might think that instructions are just &quot;opcode + a few operands&quot;—pretty neat and tidy. But after looking through more assembly, we find that reality is far from this rosy.</p><p>The <code>mul</code> and <code>imul</code> we saw above are typical three-operand instructions (destination + source1 + source2), or two-operand instructions (where the destination is also source1). But there are many instructions that don&#39;t follow this pattern at all. Zero-operand instructions are the simplest, like <code>ret</code> and <code>nop</code>, which don&#39;t need any additional information. Single-operand instructions are also common, like various jump instructions. We just saw double-operand and three-operand instructions.</p><p>But what&#39;s truly confusing is &quot;implicit operands.&quot; For example, x86 has an instruction called <code>rep stosb</code>, which &quot;repeatedly writes the value of the AL register to the memory pointed to by RDI (or EDI), automatically incrementing RDI/EDI after each write, with the repeat count controlled by RCX (or ECX).&quot; AL, RDI/EDI, RCX/ECX—not a single one of these three operands is visible in the instruction text; they are all implicit, hardcoded into the instruction definition. Anyone reading the assembly must memorize which registers this instruction uses by default. The &quot;operand count&quot; of such an instruction is actually very hard to define.</p><h2 id="intel-s-historical-baggage" tabindex="-1">Intel&#39;s Historical Baggage <a class="header-anchor" href="#intel-s-historical-baggage" aria-label="Permalink to &quot;Intel&#39;s Historical Baggage&quot;">​</a></h2><p>The problem of implicit operands makes x86 a veritable &quot;disaster zone.&quot; The reason isn&#39;t complicated: the x86 instruction set started with the 8086 in 1978 and evolved all the way to today&#39;s x86-64, spanning over 40 years. Each new generation of CPU had to add new things on top of the old instruction set, and it had to maintain backward compatibility—8086 machine code written in 1985 will still run perfectly on a CPU in 2026. This constraint sounds wonderful, but the price is that the instruction set became increasingly bloated and irregular. The encoding space for new instructions was already occupied by old instructions, so they could only use various prefix bytes to extend it, leading to increasingly complex decoding logic.</p><p>Does this situation sound familiar? C++&#39;s backward compatibility issues are almost exactly the same—when writing C++26 code today, the compiler still has to handle C89-style declarations, C-style casts, and various legacy features. Every time someone proposes &quot;let&#39;s remove some old feature,&quot; the answer is always &quot;no, it would break existing code.&quot; So we carry this baggage and keep moving forward.</p><p>By comparison, ARM and RISC-V are much cleaner. ARM64 was designed around 2011 (AArch64), making it a &quot;clean-room implementation&quot;—not burdened with 32-bit ARM&#39;s historical legacy, it redesigned a completely new instruction encoding. RISC-V is even more of an academic project started from scratch in 2010, with excellent instruction orthogonality: the same opcode format can be used just by changing the register number, without any maddening rules like &quot;this instruction implicitly uses EAX, that instruction implicitly uses EDX.&quot;</p><h2 id="register-naming-the-origin-of-the-a-register" tabindex="-1">Register Naming: The Origin of the A Register <a class="header-anchor" href="#register-naming-the-origin-of-the-a-register" aria-label="Permalink to &quot;Register Naming: The Origin of the A Register&quot;">​</a></h2><p>We&#39;ve been talking about names like EAX, W0, and a0, but have you ever wondered why x86 registers have these strange names? There is historical meaning behind these names.</p><p>In x86, there is a register called A (Accumulator). In the 8080 era or even earlier with the 8008, the A register was simply &quot;the default register&quot;—many operations defaulted to acting on A, without needing to specify it in the instruction. For example, the instruction encoding for &quot;add a certain value to A&quot; was shorter than &quot;add a certain value to B,&quot; because A was the &quot;default destination,&quot; saving the few bits needed to specify the destination register.</p><p>This design philosophy carried through all the way to x86. Today, when we write <code>imul edi, edi</code>, if we change it to <code>imul ebx, ebx</code>, the machine code might be longer (depending on the specific encoding), because EAX (or rather RAX) is still a &quot;privileged register&quot; in many instructions—it&#39;s the default target for implicit instructions and a fixed participant in certain special operations (like the high-order bits of <code>mul</code>&#39;s double-precision result being placed in EDX).</p><p>Many tutorials always say &quot;try to use EAX.&quot; This isn&#39;t some mystical optimization trick; it&#39;s a &quot;privilege&quot; granted at the instruction set encoding level—using the A register might make instructions shorter and decoding faster. Of course, on modern CPUs, this difference has been largely smoothed over by various microarchitectural optimizations, but once we understand this background, those instructions with implicit operands no longer seem so baffling.</p><p>At this point, we&#39;ve walked through &quot;what a simple function call actually looks like at the assembly level&quot; from start to finish: from how parameters are passed and return values are placed, to the instruction encoding differences across architectures, to the historical origins of register naming. None of the individual steps are complicated, but when we look at them all together, the entire system connects.</p><hr><hr><h1 id="figuring-out-where-parameters-go-during-function-calls—from-register-naming-to-the-abi" tabindex="-1">Figuring Out Where Parameters Go During Function Calls—From Register Naming to the ABI <a class="header-anchor" href="#figuring-out-where-parameters-go-during-function-calls—from-register-naming-to-the-abi" aria-label="Permalink to &quot;Figuring Out Where Parameters Go During Function Calls—From Register Naming to the ABI&quot;">​</a></h1><p>When looking at assembly code generated by Compiler Explorer, the biggest psychological barrier is often not the instructions themselves, but those messy register names. RAX, EAX, AX, AL, AH—are these one thing or four things? Once we clarify x86&#39;s register layout, this problem is easily solved.</p><h2 id="first-clarify-the-relationship-between-rax-eax-and-ax" tabindex="-1">First, Clarify the Relationship Between RAX, EAX, and AX <a class="header-anchor" href="#first-clarify-the-relationship-between-rax-eax-and-ax" aria-label="Permalink to &quot;First, Clarify the Relationship Between RAX, EAX, and AX&quot;">​</a></h2><p>Going back to the most fundamental question: what is a register? We can think of it as a small row of ultra-high-speed storage slots inside the CPU, extremely limited in number. In the 8-bit era, the most core register was called the A register, short for Accumulator, around which most arithmetic operations revolved. Later, as CPUs evolved from 8-bit to 16-bit, 32-bit, and 64-bit, this register&#39;s width grew along with them, but its &quot;status&quot; never changed—it remained the general-purpose register bearing the primary computational workload.</p><p>The key point is this: when we see RAX, we&#39;re looking at a 64-bit value. But when we see EAX, we&#39;re not looking at another register—we&#39;re looking at <strong>the lower 32 bits of the same register</strong>. Similarly, AX is the lower 16 bits, AL is the lowest 8 bits, and AH is the second-to-lowest 8 bits (that is, bits 8-15). They all point to the exact same physical storage, just &quot;slicing&quot; it with different names.</p><p>Let&#39;s use a simple diagram to illustrate:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>63                              31        15  7    0</span></span>
<span class="line"><span>+--------------------------------+----------+----+----+</span></span>
<span class="line"><span>|              RAX               |   EAX    | AX      |</span></span>
<span class="line"><span>|                                |          +----+----+</span></span>
<span class="line"><span>|                                |          | AH | AL |</span></span>
<span class="line"><span>+--------------------------------+----------+----+----+</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>So when we see code like this in assembly, there&#39;s no need to panic:</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      ; 把 64 位参数放进 rax 做计算</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">shr</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       ; 右移 32 位</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> eax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eax</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      ; 只保留低 32 位作为返回值</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Here, switching from rax to eax isn&#39;t data being moved back and forth between two registers; it&#39;s the compiler saying &quot;the calculation is done, now we only care about the lower 32 bits.&quot; Type information from the C++ source code (for example, if the parameter is int64_t but the return value is int32_t) is directly reflected in the assembly&#39;s use of different names for the same register. After type information disappears, this is how it &quot;lingers&quot; in the assembly.</p><h2 id="those-weirdly-named-registers-and-their-easy-to-remember-new-friends" tabindex="-1">Those Weirdly Named Registers, and Their Easy-to-Remember New Friends <a class="header-anchor" href="#those-weirdly-named-registers-and-their-easy-to-remember-new-friends" aria-label="Permalink to &quot;Those Weirdly Named Registers, and Their Easy-to-Remember New Friends&quot;">​</a></h2><p>Once we understand the naming pattern of RAX, we might wonder: what about the rest? RAX, RCX, RDX, RSP, RBP, RSI, RDI... these names seem to follow no pattern at all. They are all legacy names inherited from ancient times: A is for Accumulator, C is for Counter, D is for Data, SP is for Stack Pointer, BP is for Base Pointer, and SI and DI are for Source Index and Destination Index, respectively. Knowing the historical background makes them slightly easier to remember, but to a large extent, it still relies on muscle memory built through repeated use.</p><p>There is good news, however: when AMD extended the architecture from 32-bit to 64-bit, the eight new general-purpose registers were simply named R8 through R15. Clean and simple. So x86-64 now has a total of 16 general-purpose registers, eight with historically weird names and eight with clean numeric designations.</p><p>There are also SIMD/multimedia registers (XMM/YMM/ZMM and such), but those are a whole other topic; today we&#39;ll focus on general-purpose registers and function calls.</p><h2 id="which-register-are-function-parameters-actually-in" tabindex="-1">Which Register Are Function Parameters Actually In? <a class="header-anchor" href="#which-register-are-function-parameters-actually-in" aria-label="Permalink to &quot;Which Register Are Function Parameters Actually In?&quot;">​</a></h2><p>One of the biggest confusions when reading assembly is this: we write a function, pass three parameters into it, and the assembly turns into a bunch of mov instructions shuffling data between registers. Where do the parameters actually come from? This involves the ABI (Application Binary Interface).</p><p>The ABI specifies many things, but from the perspective of reading assembly, the one thing we care about most is this: <strong>which registers hold the first few parameters of a function</strong>. As long as we know this, we can track what C++ variables have turned into in the assembly.</p><p>Take Linux (System V AMD64 ABI) as an example. The first six integer parameters (including pointers) are placed in these registers in order:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>第 1 个参数 → RDI</span></span>
<span class="line"><span>第 2 个参数 → RSI</span></span>
<span class="line"><span>第 3 个参数 → RDX</span></span>
<span class="line"><span>第 4 个参数 → RCX</span></span>
<span class="line"><span>第 5 个参数 → R8</span></span>
<span class="line"><span>第 6 个参数 → R9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Any parameters beyond the first six can only be pushed onto the stack, accessed via stack pointer offsets. When using <code>std::forward</code> for perfect forwarding, if there are many parameters, we&#39;ll see a lot of stack operations in the assembly, because forwarding might &quot;unroll&quot; the parameters, causing the count to suddenly exceed the capacity of the six registers.</p><p>Return values are simpler, uniformly placed in RAX (if it&#39;s a 128-bit return value, RDX:RAX are combined).</p><p>Floating-point parameters are slightly more complex, going through a separate set of registers (XMM0 through XMM7), but the basic idea is the same—the first few go in registers, and any extras go on the stack.</p><h2 id="windows-has-different-rules" tabindex="-1">Windows Has Different Rules <a class="header-anchor" href="#windows-has-different-rules" aria-label="Permalink to &quot;Windows Has Different Rules&quot;">​</a></h2><p>If we use MSVC on Windows, the situation is different. The Windows x64 ABI only provides four registers for passing parameters:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>第 1 个参数 → RCX</span></span>
<span class="line"><span>第 2 个参数 → RDX</span></span>
<span class="line"><span>第 3 个参数 → R8</span></span>
<span class="line"><span>第 4 个参数 → R9</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Note that both the order and the names are different from Linux. This means that for the exact same function, on Linux the first six parameters all go through registers, while on Windows the fifth and sixth parameters already need to be pushed to the stack. When debugging cross-platform performance issues, the exact same C++ code producing completely different assembly on both sides is often caused by ABI differences.</p><p>This difference actually has a subtle impact on API design. If we know that only four registers are available on Windows, we&#39;ll tend to be more conservative with parameter counts when designing frequently called interfaces. But we&#39;ll expand on this topic when we encounter specific scenarios later.</p><h2 id="let-s-verify-this-hands-on" tabindex="-1">Let&#39;s Verify This Hands-On <a class="header-anchor" href="#let-s-verify-this-hands-on" aria-label="Permalink to &quot;Let&#39;s Verify This Hands-On&quot;">​</a></h2><p>Talk is cheap, let&#39;s write the simplest function and throw it into Compiler Explorer:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 编译选项：-O1 -m64</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 平台：x86-64 Linux (GCC)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add_three</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> a</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> c</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> c;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The corresponding assembly looks roughly like this (GCC 16, -O1):</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">add_three(long, long, long):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rsi</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           ; rdi(a) += rsi(b)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    lea</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> + </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">; rax = rdi + rdx(c)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>See? a is in RDI, b is in RSI, c is in RDX—completely in line with the rules we discussed. The return value is in RAX. Clean.</p><p>Let&#39;s try one with more than six parameters:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sum_seven</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> a</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> c</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> d</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">               long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> g</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> d </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> e </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> g;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The assembly becomes this:</p><div class="language-asm vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sum_seven(long, long, long, long, long, long, long):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    lea</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> + </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rsi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">; a + b</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rdx</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               ; + c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rcx</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               ; + d</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">r8</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                ; + e</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">r9</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                ; + f</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">QWORD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PTR [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rsp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">; + g，从栈上取！注意偏移 +8，因为 [rsp] 是 call 压入的返回地址</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ret</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>The first six parameters are in RDI, RSI, RDX, RCX, R8, and R9 respectively, and the seventh parameter g ends up on the stack, accessed via <code>[rsp+8]</code> (the <code>call</code> instruction pushed the return address into <code>[rsp]</code>, so the first stack parameter requires an 8-byte offset). Once we know the ABI rules, reading assembly is like having a map—it&#39;s no longer a screen full of gibberish.</p><h2 id="a-quick-note-on-arm64" tabindex="-1">A Quick Note on ARM64 <a class="header-anchor" href="#a-quick-note-on-arm64" aria-label="Permalink to &quot;A Quick Note on ARM64&quot;">​</a></h2><p>If we&#39;ve worked with ARM64 (like Apple Silicon or embedded development), things are much cleaner over there. The general-purpose registers are simply called X0 through X30, with no historical baggage. Function parameters are just X0, X1, X2, and so on, and the return value is in X0. If we want the 32-bit version, we just replace X with W—for example, W0 is the lower 32 bits of X0. The naming logic is the same idea as x86&#39;s RAX/EAX, but the names are much easier to</p>`,126))])}const f=r(p,[["render",d]]);export{y as __pageData,f as default};
