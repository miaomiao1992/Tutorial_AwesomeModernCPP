import{_ as t,o,c as r,a2 as a}from"./chunks/framework.BuBU3QrY.js";const m=JSON.parse('{"title":"Cross-Thread Safety, Performance Trade-offs, and Design Principles Summary","description":"Lifetime safety does not equal thread safety—the ultimate engineering rules and recommended naming conventions for pointer-semantic design","frontmatter":{"title":"Cross-Thread Safety, Performance Trade-offs, and Design Principles Summary","description":"Lifetime safety does not equal thread safety—the ultimate engineering rules and recommended naming conventions for pointer-semantic design","chapter":1,"order":6,"tags":["host","cpp-modern","intermediate","智能指针","内存管理"],"difficulty":"intermediate","platform":"host","reading_time_minutes":7,"prerequisites":["std::weak_ptr 对比与异步回调实战"],"related":["卷二 · 第一章：智能指针与 RAII"],"cpp_standard":[17,20],"translation":{"source":"documents/vol8-domains/cpp-deep-dives/pointer-semantics/06-design-principles.md","source_hash":"1377d6587f4356ac48fbc24fad885afa68c2202f49efd783e4cae39993492fca","translated_at":"2026-05-26T11:56:27.979531+00:00","engine":"anthropic","token_count":1283}},"headers":[],"relativePath":"en/vol8-domains/cpp-deep-dives/pointer-semantics/06-design-principles.md","filePath":"en/vol8-domains/cpp-deep-dives/pointer-semantics/06-design-principles.md","lastUpdated":null}'),n={name:"en/vol8-domains/cpp-deep-dives/pointer-semantics/06-design-principles.md"};function s(d,e,i,c,l,h){return o(),r("div",null,[...e[0]||(e[0]=[a('<h1 id="cross-thread-safety-performance-trade-offs-and-design-principles-summary" tabindex="-1">Cross-Thread Safety, Performance Trade-offs, and Design Principles Summary <a class="header-anchor" href="#cross-thread-safety-performance-trade-offs-and-design-principles-summary" aria-label="Permalink to &quot;Cross-Thread Safety, Performance Trade-offs, and Design Principles Summary&quot;">​</a></h1><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>By this point, we have walked through the entire spectrum of non-owning pointers—from the simplest <code>T*</code> and <code>T&amp;</code>, to hand-rolled <code>Borrowed&lt;T&gt;</code> and <code>ObserverPtr&lt;T&gt;</code>, to three weak reference approaches (<code>UnsafeWeakPtr</code>, <code>SimpleWeakPtr</code>, and Chrome-like <code>WeakPtr</code>), and finally a comprehensive comparison with <code>std::weak_ptr</code>.</p><p>This chapter serves as the conclusion. We will clarify three topics that haven&#39;t been fully explored yet: where exactly the boundaries of cross-thread safety lie, how the performance overhead of each type compares, and how to synthesize everything into a set of actionable engineering rules.</p><h2 id="lifetime-safety-thread-safety" tabindex="-1">Lifetime Safety ≠ Thread Safety <a class="header-anchor" href="#lifetime-safety-thread-safety" aria-label="Permalink to &quot;Lifetime Safety ≠ Thread Safety&quot;">​</a></h2><p>This is the most important conclusion of the entire series, and it is worth repeating.</p><p><strong>Lifetime safety</strong> means &quot;can you safely detect invalidation after an object is destroyed.&quot; The control block of <code>WeakPtr</code> solves exactly this problem—you can safely call <code>is_valid()</code> or <code>get()</code> without invoking undefined behavior (UB).</p><p><strong>Thread safety</strong> means &quot;will problems arise when multiple threads access the object simultaneously.&quot; This is a completely different dimension from lifetime safety.</p><p>We can use a 2×2 table to clarify these four quadrants:</p><table tabindex="0"><thead><tr><th></th><th>Lifetime Unsafe</th><th>Lifetime Safe</th></tr></thead><tbody><tr><td><strong>Thread Unsafe</strong></td><td><code>T*</code>, <code>T&amp;</code>, <code>ObserverPtr</code></td><td>Chrome <code>WeakPtr</code> (single sequence)</td></tr><tr><td><strong>Partially Thread Safe</strong></td><td>N/A (meaningless)</td><td><code>std::weak_ptr</code> (atomic lock, but T&#39;s internal state requires synchronization)</td></tr></tbody></table><p><code>T*</code> sits in the top-left corner—solving neither the lifetime problem nor the thread problem. Chrome <code>WeakPtr</code> solves the lifetime problem, but still suffers from TOCTOU races in cross-thread scenarios. The <code>lock()</code> of <code>std::weak_ptr</code> is atomic, and once locked, the object will not be destroyed, but concurrent access to the object&#39;s <strong>internal state</strong> still requires protection from a mutex or other mechanisms.</p><p>So: <code>WeakPtr</code> solves &quot;do I know if the object is dead,&quot; not &quot;is it safe for multiple threads to touch this object at the same time.&quot;</p><h3 id="why-chrome-weakptr-is-sequence-bound" tabindex="-1">Why Chrome WeakPtr is Sequence-Bound <a class="header-anchor" href="#why-chrome-weakptr-is-sequence-bound" aria-label="Permalink to &quot;Why Chrome WeakPtr is Sequence-Bound&quot;">​</a></h3><p>Chrome&#39;s design philosophy is that most callbacks in UI and asynchronous frameworks run on the same logical sequence. Timer callbacks, event handling, IO completion notifications—they are all dispatched and executed by the same task runner. Under this model, <code>invalidate</code> and <code>get</code> cannot execute simultaneously because they run sequentially in a queue.</p><p>This is far more efficient than &quot;just add a mutex for cross-thread safety&quot;—a mutex incurs runtime overhead, whereas being sequence-bound is a zero-overhead design constraint. The trade-off is that your usage pattern is restricted: you cannot pass a WeakPtr across sequences. However, this constraint naturally holds true in most UI and event loop frameworks.</p><h3 id="how-to-handle-cross-thread-scenarios" tabindex="-1">How to Handle Cross-Thread Scenarios <a class="header-anchor" href="#how-to-handle-cross-thread-scenarios" aria-label="Permalink to &quot;How to Handle Cross-Thread Scenarios&quot;">​</a></h3><p>If you genuinely need cross-thread weak references, there are a few options:</p><ul><li><strong>Use <code>std::weak_ptr</code></strong>: <code>lock()</code> atomically acquires a <code>shared_ptr</code>, ensuring the object will not be destroyed within your scope. However, the thread safety of T&#39;s internals must be handled separately.</li><li><strong>Use <code>std::atomic&lt;std::shared_ptr&lt;T&gt;&gt;</code> (C++20)</strong>: Provides atomic operations to safely read and write <code>shared_ptr</code> across threads.</li><li><strong>Use message passing</strong>: Instead of sharing a WeakPtr directly across threads, pass a &quot;please do this on your sequence&quot; request through a message queue, letting the target sequence handle it itself.</li></ul><h2 id="performance-comparison" tabindex="-1">Performance Comparison <a class="header-anchor" href="#performance-comparison" aria-label="Permalink to &quot;Performance Comparison&quot;">​</a></h2><p>Let&#39;s do a performance comparison of all the types covered in this series. The numbers are approximate and depend on the specific platform and compiler:</p><table tabindex="0"><thead><tr><th>Type</th><th>Object Size</th><th>Control Block Allocation</th><th>Atomic Operations</th><th>Best For</th></tr></thead><tbody><tr><td><code>T*</code></td><td>8B</td><td>None</td><td>None</td><td>Synchronous function parameters</td></tr><tr><td><code>T&amp;</code></td><td>8B (pointer implementation)</td><td>None</td><td>None</td><td>Synchronous function parameters</td></tr><tr><td><code>Borrowed&lt;T&gt;</code></td><td>8B</td><td>None</td><td>None</td><td>Synchronous function parameters (explicit semantics)</td></tr><tr><td><code>ObserverPtr&lt;T&gt;</code></td><td>8B</td><td>None</td><td>None</td><td>Class member observation</td></tr><tr><td><code>UnsafeWeakPtr&lt;T&gt;</code></td><td>16B</td><td>None</td><td>None</td><td>Should not be used</td></tr><tr><td><code>SimpleWeakPtr&lt;T&gt;</code></td><td>24B (T* + shared_ptr)</td><td>1x <code>new</code></td><td>1x copy, 1~2x destruction atomic ops</td><td>Teaching, simple scenarios</td></tr><tr><td>Chrome <code>WeakPtr&lt;T&gt;</code></td><td>16B (<code>T*</code> + <code>WeakFlag*</code>)</td><td>1x <code>new</code></td><td>1x atomic op each for copy/destruction</td><td>In-framework async callbacks</td></tr><tr><td><code>std::weak_ptr&lt;T&gt;</code></td><td>16B</td><td>Managed by <code>shared_ptr</code></td><td>2x atomic ops each for lock/unlock</td><td>shared_ptr ecosystem</td></tr></tbody></table><p>A few details worth noting:</p><p>The overhead of <code>Borrowed&lt;T&gt;</code> and <code>ObserverPtr&lt;T&gt;</code> is zero—after compiler optimization, they are identical to raw pointers. Their value lies purely in semantics.</p><p><code>SimpleWeakPtr&lt;T&gt;</code> is 8 bytes larger than Chrome <code>WeakPtr&lt;T&gt;</code> because <code>shared_ptr</code> internally holds two pointers (object pointer + control block pointer), whereas Chrome&#39;s <code>WeakPtr</code> only stores <code>T*</code> and <code>WeakFlag*</code>. Every copy of <code>shared_ptr</code> requires two atomic operations (strong count + weak count), while Chrome only needs one.</p><p>The control block of Chrome <code>WeakPtr</code> (<code>WeakFlag</code>) is much smaller than that of <code>shared_ptr</code>—it contains only one atomic bool and one atomic int, with no virtual destructor, no allocator, and no weak count.</p><p>The extra overhead of <code>std::weak_ptr</code> depends on the <code>shared_ptr</code> it relies on. If you force an object that doesn&#39;t originally need <code>shared_ptr</code> to be managed by <code>shared_ptr</code> just to use <code>weak_ptr</code>, you not only pay the cost of the control block but also introduce the risk of atomic reference count contention.</p><h2 id="engineering-rules" tabindex="-1">Engineering Rules <a class="header-anchor" href="#engineering-rules" aria-label="Permalink to &quot;Engineering Rules&quot;">​</a></h2><p>Summarized into a set of actionable rules:</p><p><strong>For function parameters</strong>, prefer <code>T&amp;</code>, <code>T*</code>, or <code>Borrowed&lt;T&gt;</code>. Do not use smart pointers to express non-owning relationships in function parameters. <code>Borrowed&lt;T&gt;</code> provides the most explicit semantics (non-null + non-owning), but <code>const T&amp;</code> is sufficient in most scenarios.</p><p><strong>For class member observation</strong>, you can use <code>ObserverPtr&lt;T&gt;</code>. When you want to express &quot;I observe it but don&#39;t own it,&quot; <code>ObserverPtr&lt;T&gt;</code> is much more readable than a raw <code>T*</code>. But remember that it cannot check liveness.</p><p><strong>For async callbacks</strong>, never capture a raw <code>this</code>, a raw <code>T*</code>, <code>ObserverPtr</code>, or any &quot;weak reference&quot; without an independent control block. The correct choices are a Chrome-like <code>WeakPtr&lt;T&gt;</code> (for non-<code>shared_ptr</code> scenarios) or <code>std::weak_ptr&lt;T&gt;</code> (for <code>shared_ptr</code> scenarios).</p><p><strong>Do not use <code>ObserverPtr</code> as a WeakPtr</strong>. <code>ObserverPtr</code> can only express &quot;I don&#39;t own it&quot;; it cannot express &quot;do I know if it&#39;s still alive.&quot;</p><p><strong>Do not call <code>T* + raw Flag*</code> a WeakPtr</strong>. If the Flag&#39;s lifetime is bound to the Owner, it is not a reliable WeakPtr. Give it an honest name—<code>UnsafeWeakPtr</code> or <code>OwnerBoundWeakPtr</code>.</p><p><strong>For cross-thread scenarios</strong>, prefer <code>std::weak_ptr&lt;T&gt;</code> or message passing. Chrome-like <code>WeakPtr</code> is designed to be sequence-bound; do not use it as a cross-thread safe pointer.</p><p><strong>WeakPtr solves lifetime awareness, not thread safety</strong>. Regardless of which WeakPtr you use, concurrent access to T&#39;s internal state requires additional synchronization mechanisms.</p><h2 id="recommended-naming-system" tabindex="-1">Recommended Naming System <a class="header-anchor" href="#recommended-naming-system" aria-label="Permalink to &quot;Recommended Naming System&quot;">​</a></h2><p>Finally, here is a set of recommended naming conventions:</p><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Meaning</th></tr></thead><tbody><tr><td><code>Borrowed&lt;T&gt;</code></td><td>Borrow</td><td>Non-null, non-owning, short-term use, suitable for function parameters</td></tr><tr><td><code>ObserverPtr&lt;T&gt;</code></td><td>Observer</td><td>Nullable, non-owning, no liveness check, suitable for class members</td></tr><tr><td><code>UnsafeWeakPtr&lt;T&gt;</code></td><td>Unsafe Weak Reference</td><td><code>T*</code> + <code>raw Flag*</code>, the name explicitly flags it as unsafe</td></tr><tr><td><code>WeakPtr&lt;T&gt;</code></td><td>Safe Weak Reference</td><td>A true weak reference that can safely check for null after the object is destroyed</td></tr><tr><td><code>WeakPtrFactory&lt;T&gt;</code></td><td>Weak Reference Factory</td><td>Centrally creates and manages the invalidation of WeakPtrs</td></tr></tbody></table><p>The name <code>UnsafeWeakPtr</code> is not derogatory—it is an <strong>honest naming</strong>. When you see <code>UnsafeWeakPtr</code> in a codebase, you immediately know &quot;this has pitfalls, pay attention to the constraints when using it.&quot; This is far more responsible than wrapping it as <code>WeakPtr</code> and burying a single line of fine print in the documentation saying &quot;ensure the WeakPtr does not outlive the Owner.&quot;</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><ul><li>Lifetime safety and thread safety are two orthogonal problems; WeakPtr only solves the former</li><li>Chrome <code>WeakPtr</code> achieves zero-overhead safety through the sequence-bound model, but restricts cross-thread usage</li><li><code>Borrowed</code> and <code>ObserverPtr</code> have zero runtime overhead; their value lies in semantic expression</li><li>The control block of Chrome <code>WeakPtr</code> is lighter than that of <code>shared_ptr</code></li><li>Do not force the introduction of <code>shared_ptr</code> just to use <code>weak_ptr</code></li><li>Naming should be honest—if something is unsafe, call it unsafe</li></ul><p>This concludes the entire series. Starting from <code>T*</code>, we hand-rolled Borrowed, ObserverPtr, UnsafeWeakPtr, SimpleWeakPtr, and a Chrome-like WeakPtr, explaining the design rationale and engineering trade-offs at every step. We hope this content helps you make clearer pointer semantic choices in your real-world engineering work.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines" target="_blank" rel="noreferrer">C++ Core Guidelines</a></li><li><a href="https://www.chromium.org/developers/smart-pointer-guidelines/" target="_blank" rel="noreferrer">Chromium Smart Pointer Guidelines</a></li><li><a href="https://en.cppreference.com/w/cpp/memory/weak_ptr" target="_blank" rel="noreferrer">std::weak_ptr - cppreference</a></li><li><a href="https://github.com/microsoft/GSL" target="_blank" rel="noreferrer">GSL: Guidelines Support Library</a></li><li><a href="https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2019/p1408r0.pdf" target="_blank" rel="noreferrer">P1408R0: Abandon observer_ptr (Stroustrup)</a></li></ul>',44)])])}const u=t(n,[["render",s]]);export{m as __pageData,u as default};
