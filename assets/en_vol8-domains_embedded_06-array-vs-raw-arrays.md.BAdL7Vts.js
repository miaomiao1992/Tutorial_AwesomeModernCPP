import{_ as a,o as r,c as t,a2 as o}from"./chunks/framework.CZcFxxdw.js";const u=JSON.parse('{"title":"std::array vs regular arrays","description":"Comparing std::array with traditional arrays","frontmatter":{"chapter":5,"cpp_standard":[11,14,17,20],"description":"Comparing std::array with traditional arrays","difficulty":"intermediate","order":6,"platform":"stm32f1","prerequisites":["Chapter 3: 内存与对象管理"],"reading_time_minutes":5,"tags":["cpp-modern","intermediate","stm32f1"],"title":"std::array vs regular arrays"},"headers":[],"relativePath":"en/vol8-domains/embedded/06-array-vs-raw-arrays.md","filePath":"en/vol8-domains/embedded/06-array-vs-raw-arrays.md","lastUpdated":null}'),i={name:"en/vol8-domains/embedded/06-array-vs-raw-arrays.md"};function s(n,e,d,c,l,h){return r(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="embedded-c-tutorial-—-std-array-vs-c-arrays-do-you-know-the-difference" tabindex="-1">Embedded C++ Tutorial — <code>std::array</code> vs C Arrays, Do You Know the Difference? <a class="header-anchor" href="#embedded-c-tutorial-—-std-array-vs-c-arrays-do-you-know-the-difference" aria-label="Permalink to &quot;Embedded C++ Tutorial — `std::array` vs C Arrays, Do You Know the Difference?&quot;">​</a></h1><p>When writing embedded code, you will likely hesitate between two approaches: <code>int buf[16];</code> and <code>std::array&lt;int, 16&gt; buf;</code>. If you are like me—valuing both performance and elegance—you will want to know: which one is more &quot;embedded-friendly&quot;?</p><hr><h2 id="why-std-array-looks-like-a-c-array-wearing-a-coat-—-but-is-actually-smarter" tabindex="-1">Why <code>std::array</code> Looks Like &quot;a C Array Wearing a Coat&quot; — But Is Actually Smarter <a class="header-anchor" href="#why-std-array-looks-like-a-c-array-wearing-a-coat-—-but-is-actually-smarter" aria-label="Permalink to &quot;Why `std::array` Looks Like &quot;a C Array Wearing a Coat&quot; — But Is Actually Smarter&quot;">​</a></h2><p>On the surface, <code>std::array&lt;T, N&gt;</code> is simply an aggregate type containing a <code>T elems[N]</code> underneath: elements are contiguous in memory, and there is no mysterious overhead in the layout. Therefore, in many scenarios, <code>std::array</code> is equivalent to a raw array in terms of performance and memory footprint. In other words, you do not pay any extra runtime cost by switching to <code>std::array</code>.</p><p>But <code>std::array</code> wraps the array in a type: it has value semantics (it can be copied and assigned), provides <code>.size()</code>, offers <code>.data()</code>, includes <code>begin()</code>/<code>end()</code>, integrates seamlessly with STL algorithms, supports <code>constexpr</code> (with modern compilers), and can be better deduced as a template parameter. Most importantly, it makes the information that &quot;length is part of the type&quot; explicit, making it much harder to lose size information when calling interfaces.</p><p>In other words: <code>std::array</code> is a &quot;safer, more modern&quot; array.</p><hr><h2 id="the-honest-simplicity-and-fatal-naivety-of-raw-c-arrays" tabindex="-1">The Honest Simplicity and Fatal Naivety of Raw C Arrays <a class="header-anchor" href="#the-honest-simplicity-and-fatal-naivety-of-raw-c-arrays" aria-label="Permalink to &quot;The Honest Simplicity and Fatal Naivety of Raw C Arrays&quot;">​</a></h2><p>The advantage of raw arrays is &quot;zero abstraction&quot;—you have complete control over memory. This is crucial in startup code, driver layers, and buffers located in specific address spaces (such as those mapped to peripheral register addresses). Raw arrays do not create headaches with ABI, linkers, or alignment—as long as you know what you are doing, they are highly reliable.</p><p>However, raw arrays also bring a host of common pitfalls: they decay into pointers in function parameters (so <code>sizeof</code> yields a pointer size inside a function), cannot be directly copied or assigned (<code>b = a;</code> will fail to compile), and offer no bounds or size protection. In embedded code, these &quot;missing conveniences&quot; force you to frequently write <code>memcpy</code>, constantly double-check whether <code>N</code> is correct, and make rookie mistakes like &quot;forgetting to pass the length&quot; during code reviews.</p><p>A real-world scenario: you pass a raw array to a C API for DMA, but forget to tell the caller the length. As a result, DMA writes out of bounds and overwrites your most precious variable. Raw arrays do not warn you about these low-probability, high-cost errors.</p><hr><h2 id="advantages-of-std-array-safer-more-readable-and-more-modern-c-friendly" tabindex="-1">Advantages of <code>std::array</code>: Safer, More Readable, and More Modern C++ Friendly <a class="header-anchor" href="#advantages-of-std-array-safer-more-readable-and-more-modern-c-friendly" aria-label="Permalink to &quot;Advantages of `std::array`: Safer, More Readable, and More Modern C++ Friendly&quot;">​</a></h2><p>The everyday advantages of <code>std::array</code> can be summarized as: clear semantics, friendly interfaces, and direct compatibility with algorithms. For example, <code>std::sort(a.begin(), a.end())</code> or <code>std::span(a)</code> are readily available benefits. <code>std::array</code> can be <code>=</code>, copied, or even safely returned as a function return value (without decaying), which makes code in many mid-level logic layers more concise and less prone to memory manipulation bugs.</p><p>In an embedded context, this means test code, unit test stubs, and buffer wrappers will be much cleaner: you can write functions that return <code>std::array</code> instead of a messy pile of <code>memcpy</code>. Furthermore, when the compiler supports <code>constexpr</code>, <code>std::array</code> can construct constant tables at compile time, resulting in code that is both efficient and safe.</p><hr><h2 id="so-when-should-we-stick-with-raw-c-arrays" tabindex="-1">So When Should We Stick with Raw C Arrays? <a class="header-anchor" href="#so-when-should-we-stick-with-raw-c-arrays" aria-label="Permalink to &quot;So When Should We Stick with Raw C Arrays?&quot;">​</a></h2><p><code>std::array</code> is great, but it is not invincible. In the following scenarios, raw arrays remain the more appropriate choice:</p><ol><li><strong>Initialization phase or early boot code (startup / crt0)</strong>: Before <code>main()</code>, C++ global construction rules and runtime support can be problematic. Raw arrays are more straightforward and reliable in such code, especially when you need to absolutely guarantee that no constructors or runtime code intervene.</li><li><strong>Placed in specific linker sections / at fixed addresses</strong>: Interrupt vector tables, device-mapped buffers, and bootloader tables often require precisely declaring object locations and byte order in the linker script. Raw arrays map more directly to the expected memory layout, reducing unnecessary abstraction.</li><li><strong>Strict ABI or interoperability with external C APIs where you need raw pointers</strong>: Although <code>std::array</code> has <code>.data()</code>, in scenarios that are highly particular about binary compatibility, using raw arrays is more intuitive during auditing (especially in legacy codebases).</li><li><strong>Extreme resource constraints where you must avoid the compiler generating any extra metadata</strong>: Such situations are rare, but they do exist in some ultra-embedded or lowest-level kernel code.</li></ol><hr><h2 id="the-bottom-line" tabindex="-1">The Bottom Line <a class="header-anchor" href="#the-bottom-line" aria-label="Permalink to &quot;The Bottom Line&quot;">​</a></h2><p>Raw arrays are simple, reliable tools suited for the layer closest to the hardware; <code>std::array</code> is a more modern, safer container that better aligns with C++ philosophy, suited for business logic, algorithm layers, and the vast majority of embedded application code. Treat them as two different knives in your toolbox: use the survival knife (raw arrays) to fix chip pins, and use the precision knife (<code>std::array</code>) for protocol parsing and buffer logic.</p><p>One final piece of advice: when you can express the array size as a template parameter of <code>std::array&lt;T, N&gt;</code>, go ahead and use <code>std::array</code>; when you must precisely control every single byte in a linker script or the earliest boot code, fall back to raw arrays without hesitation. Embedded development is not about &quot;staying pure&quot;—it is about using the right tool for the actual need. <code>std::array</code> will often give you less code and fewer bugs, but occasionally you still need to roll up your sleeves and reach into raw memory to fix the low-level stuff.</p><hr><h2 id="code-examples" tabindex="-1">Code Examples <a class="header-anchor" href="#code-examples" aria-label="Permalink to &quot;Code Examples&quot;">​</a></h2>',26)])])}const m=a(i,[["render",s]]);export{u as __pageData,m as default};
