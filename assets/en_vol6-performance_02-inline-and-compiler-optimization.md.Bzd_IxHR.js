import{_ as i,E as t,o,c as a,a2 as s,H as l}from"./chunks/framework.BuBU3QrY.js";const b=JSON.parse('{"title":"Inlining and Compiler Optimization","description":"Exploring how inline functions work","frontmatter":{"chapter":2,"cpp_standard":[11,14,17,20],"description":"Exploring how inline functions work","difficulty":"intermediate","order":2,"platform":"host","prerequisites":["Chapter 1: 构建工具链"],"reading_time_minutes":4,"tags":["cpp-modern","host","intermediate"],"title":"Inlining and Compiler Optimization","translation":{"source":"documents/vol6-performance/02-inline-and-compiler-optimization.md","source_hash":"06a9317198c969abccdf3e2276bbeee56856b754f7c4c957a30a250c86969747","translated_at":"2026-05-26T11:49:35.014843+00:00","engine":"anthropic","token_count":530}},"headers":[],"relativePath":"en/vol6-performance/02-inline-and-compiler-optimization.md","filePath":"en/vol6-performance/02-inline-and-compiler-optimization.md","lastUpdated":null}'),r={name:"en/vol6-performance/02-inline-and-compiler-optimization.md"};function c(d,e,h,p,u,m){const n=t("OnlineCompilerDemo");return o(),a("div",null,[e[0]||(e[0]=s('<h1 id="modern-c-for-embedded-systems—inline-functions-and-compiler-optimization" tabindex="-1">Modern C++ for Embedded Systems—Inline Functions and Compiler Optimization <a class="header-anchor" href="#modern-c-for-embedded-systems—inline-functions-and-compiler-optimization" aria-label="Permalink to &quot;Modern C++ for Embedded Systems—Inline Functions and Compiler Optimization&quot;">​</a></h1><p>In embedded development, <code>inline</code> is a keyword almost every engineer uses. It seems simple and direct, even carrying a hint of &quot;performance guarantee&quot;: if a function is short, called frequently, and timing-sensitive, just <code>inline</code> it—seems like a no-brainer.</p><p>True, in the past, the <code>inline</code> keyword did serve this purpose. But in reality, in the era of modern C++ and with today&#39;s highly advanced compiler optimizations, <strong><code>inline</code> is not a performance optimization button, and it often does nothing at all.</strong></p><h2 id="inline-was-never-born-to-be-fast" tabindex="-1">Inline was never born to be &quot;fast&quot; <a class="header-anchor" href="#inline-was-never-born-to-be-fast" aria-label="Permalink to &quot;Inline was never born to be &quot;fast&quot;&quot;">​</a></h2><p>At the language level, the core purpose of <code>inline</code> is actually quite restrained. The modern C++ standard does not promise that &quot;if you write <code>inline</code>, the compiler will definitely expand the function body.&quot; The only thing it truly guarantees is this: <strong>it allows the function to have a definition in multiple translation units without violating the ODR.</strong></p><p>This is why a large number of small functions in header files, template functions, and <code>traits</code> utility functions naturally carry an <code>inline</code> vibe. It solves a &quot;linkage-level problem,&quot; not a &quot;performance problem.&quot; As for whether inlining actually occurs, that is entirely at the compiler&#39;s discretion. In the face of modern compilers, <code>inline</code> is more like a suggestion—&quot;I think you might consider expanding this&quot;—rather than a command.</p><hr><h2 id="so-are-function-calls-really-slow-in-embedded" tabindex="-1">So, are function calls really slow in embedded? <a class="header-anchor" href="#so-are-function-calls-really-slow-in-embedded" aria-label="Permalink to &quot;So, are function calls really slow in embedded?&quot;">​</a></h2><p>Much of the intuition surrounding <code>inline</code> stems from a fear of function call overhead. On architectures like Cortex-M, a function call does indeed mean a jump, saving LR, parameter passing, and restoring the return path. If you stare at the assembly line by line, it&#39;s easy to conclude: this doesn&#39;t look cheap.</p><p>The problem is whether this cost <strong>actually lands on your performance bottleneck path.</strong></p><p>In real-world embedded engineering, the time consumption of many functions doesn&#39;t lie in the &quot;call itself&quot; at all, but rather in peripheral access, bus waits, Flash reads, cache misses, or even interrupt preemption. Agonizing over whether to <code>inline</code> a GPIO read function is often optimizing at a completely irrelevant level.</p><p>More critically, after enabling optimizations (even just <code>-O2</code>), short, side-effect-free functions with clear semantics <strong>will almost certainly be automatically inlined by the compiler, even if you don&#39;t write <code>inline</code>.</strong></p><p>When deciding whether to inline a function today, compilers consider a combination of factors: function body size, number of call sites, register pressure, instruction cache behavior, and even cross-file call graph analysis when LTO is enabled. The information it has far exceeds the little context you see when writing the code. This is why you often see this situation: you explicitly write <code>inline</code>, but the disassembly reveals the function still exists; and when you write nothing at all, the function is silently expanded.</p><hr><h2 id="inline-that-expands-into-a-call-isn-t-very-safe" tabindex="-1">Inline that expands into a call isn&#39;t very safe <a class="header-anchor" href="#inline-that-expands-into-a-call-isn-t-very-safe" aria-label="Permalink to &quot;Inline that expands into a call isn&#39;t very safe&quot;">​</a></h2><p>If the biggest risk of <code>inline</code> in PC development is &quot;doing nothing,&quot; then in embedded systems, its real risk is often <strong>code bloat</strong>.</p><p>The essence of inlining is copying. A frequently called small function, if expanded in multiple places, will have its instructions physically duplicated. On MCUs with tight Flash resources, this duplication cannot be ignored. A more subtle point is that larger code doesn&#39;t just consume Flash—it also affects instruction cache locality. Even on cores with an I-Cache, excessive inlining can lead to more cache misses, ultimately manifesting as a performance decrease rather than an improvement.</p><hr><h2 id="so-when-is-inline-truly-valuable" tabindex="-1">So, when is inline truly valuable? <a class="header-anchor" href="#so-when-is-inline-truly-valuable" aria-label="Permalink to &quot;So, when is inline truly valuable?&quot;">​</a></h2><p>In practice, the scenarios where <code>inline</code> truly shows its value are often not &quot;to save one function call,&quot; but to <strong>eliminate the cost of abstraction boundaries</strong>.</p><p>Examples include template functions, type-safe register access wrappers, and compile-time calculations involving <code>constexpr</code>. The <code>inline</code> in these places allows us to write highly expressive C++ code, while at the generated assembly level, it is almost indistinguishable from hand-written C.</p><p>This is the most fascinating aspect of modern C++ in the embedded domain: <strong>abstraction is not a burden, but a semantic tool that can be completely optimized away.</strong></p><p>In an interrupt service routine (ISR) or an extreme hot path, <code>inline</code> might also be reasonable, but there is only ever one prerequisite: you have actually looked at the assembly and confirmed it solves a real problem.</p><h2 id="run-online" tabindex="-1">Run Online <a class="header-anchor" href="#run-online" aria-label="Permalink to &quot;Run Online&quot;">​</a></h2><p>Online comparison of C-style function calls versus C++ template zero-overhead abstractions, observing compiler inline optimization effects:</p>',25)),l(n,{title:"内联与编译器优化","source-path":"code/examples/vol34567/12_inline_optimization.cpp",description:"对比 C 风格函数与 C++ 模板的内联优化效果，观察 constexpr 编译期计算","allow-run":"","allow-x86-asm":""})])}const y=i(r,[["render",c]]);export{b as __pageData,y as default};
