import{_ as i,o as a,c as t,a2 as o}from"./chunks/framework.BuBU3QrY.js";const m=JSON.parse('{"title":"In-Depth Understanding of C/C++ Compilation and Linking Part 4: Shared Library A1: Basic Discussion of -fPIC","description":"","frontmatter":{"title":"In-Depth Understanding of C/C++ Compilation and Linking Part 4: Shared Library A1: Basic Discussion of `-fPIC`","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":13,"order":4,"translation":{"source":"documents/compilation/04-dynamic-libraries-1.md","source_hash":"ea133fb871fb203dc57822edf6c9d9bc1fe1c6a35f84e6a9705272f4ba437436","translated_at":"2026-05-26T10:10:22.009614+00:00","engine":"anthropic","token_count":476}},"headers":[],"relativePath":"en/compilation/04-dynamic-libraries-1.md","filePath":"en/compilation/04-dynamic-libraries-1.md","lastUpdated":null}'),r={name:"en/compilation/04-dynamic-libraries-1.md"};function s(n,e,l,c,d,h){return a(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="deep-dive-into-c-c-compilation-and-linking-part-4-dynamic-libraries-a1-basic-discussion-on-fpic" tabindex="-1">Deep Dive into C/C++ Compilation and Linking Part 4: Dynamic Libraries A1: Basic Discussion on <code>-fPIC</code> <a class="header-anchor" href="#deep-dive-into-c-c-compilation-and-linking-part-4-dynamic-libraries-a1-basic-discussion-on-fpic" aria-label="Permalink to &quot;Deep Dive into C/C++ Compilation and Linking Part 4: Dynamic Libraries A1: Basic Discussion on `-fPIC`&quot;">​</a></h1><h2 id="preface" tabindex="-1">Preface <a class="header-anchor" href="#preface" aria-label="Permalink to &quot;Preface&quot;">​</a></h2><p>It has been an exhausting few weeks, juggling a bunch of tasks and preparing to start a new job. I finally found a moment to catch my breath and continue updating this blog series.</p><p>This article primarily covers the basics of dynamic libraries. Specifically, we discuss how to build a dynamic library (focusing on Linux; building on Windows via the MSVC toolchain from the command line is rather painful, and plenty of mature build systems already abstract away those details, so we will skip a deep dive into Windows dynamic library builds here), along with some issues related to symbol name mangling.</p><h2 id="how-to-create-a-dynamic-library-on-linux" tabindex="-1">How to Create a Dynamic Library on Linux <a class="header-anchor" href="#how-to-create-a-dynamic-library-on-linux" aria-label="Permalink to &quot;How to Create a Dynamic Library on Linux&quot;">​</a></h2><p>Creating a dynamic library is not complicated, but it generally requires following these steps:</p><ul><li>The integrated binary relocatable files must be compiled with the position-independent flag (<code>-fPIC</code>, i.e., the Position Independent Code flag)</li><li>Integrate these PIC binary relocatable files, and then pass the <code>-shared</code> flag</li></ul><h2 id="let-s-talk-about-fpic" tabindex="-1">Let&#39;s Talk About -fPIC <a class="header-anchor" href="#let-s-talk-about-fpic" aria-label="Permalink to &quot;Let&#39;s Talk About -fPIC&quot;">​</a></h2><p>This option is quite interesting. Of course, there is not much to say about the <code>-shared</code> option—it simply tells our compiler to link a dynamic library. But why do these relocatable files need to be compiled as position-independent code?</p><p>In <em>Advanced C/C++ Compilation Technology</em>, three progressively deeper questions are raised:</p><ul><li>What is <code>-fPIC</code>?</li><li>Is <code>-fPIC</code> strictly required to create a dynamic library (.so)?</li><li>Is <code>-fPIC</code> only used when compiling dynamic libraries?</li></ul><p>Below, I have organized the explanations from that book, combined with some of my own perspectives, and laid them out here.</p><h4 id="what-is-fpic" tabindex="-1">What is <code>-fPIC</code>? <a class="header-anchor" href="#what-is-fpic" aria-label="Permalink to &quot;What is `-fPIC`?&quot;">​</a></h4><p><code>-fPIC</code> stands for <strong><code>Position-Independent Code</code></strong> (generating position-independent code). In other words, the compiled machine instructions <strong>do not rely on a fixed load address</strong> and can be loaded into any memory location at runtime without modifying the code itself. This aligns perfectly with our understanding of how dynamic libraries function. Ultimately, we need to export symbols from a dynamic library for use by third-party applications or other libraries. Therefore, we obviously cannot assign an absolute mapped address to these dynamic library symbols. Instead, at the point of reuse, we dynamically provide an offset address mapped into the consumer&#39;s process address space, which is how we achieve symbol reuse. Breaking it down step by step:</p><ul><li><code>-fPIC</code> maps symbols using <strong>relative addresses</strong> rather than absolute addresses</li><li>Global variables are accessed indirectly through the <strong>GOT (Global Offset Table)</strong></li><li>Function calls jump through the <strong>PLT (Procedure Linkage Table)</strong></li></ul><hr><h4 id="is-fpic-strictly-required-to-create-a-dynamic-library-so" tabindex="-1"><strong>Is <code>-fPIC</code> strictly required to create a dynamic library (.so)?</strong> <a class="header-anchor" href="#is-fpic-strictly-required-to-create-a-dynamic-library-so" aria-label="Permalink to &quot;**Is `-fPIC` strictly required to create a dynamic library (.so)?**&quot;">​</a></h4><p>Strictly speaking, not necessarily. Of course, if we consider that 32-bit PCs are practically extinct today (forgive my ignorance, but I have never actually seen a physical 32-bit PC, though I have tinkered a bit with MCUs), then we might affirm the above proposition.</p><p>Let&#39;s think about it: modern dynamic libraries are synonymous with shared libraries, where multiple processes share the code segment of a dynamic library. For different processes, it is perfectly reasonable to require that the code be loaded at any virtual address. Otherwise, the loader would have to perform <strong>relocation patching</strong> on the code at load time, which prevents the code segment from being shared and slows down loading.</p><p>However, on x86-64, it is still possible to compile a usable dynamic library without <code>-fPIC</code>, but we lose the sharing property, and loading becomes slower (because addresses for all symbols must be fixed up at load time). So, if we think about it seriously, my conclusion is:</p><blockquote><p><strong>Today, compiling a dynamic library must include the -<code>fPIC</code> flag; the benefits far outweigh the drawbacks (unless you are deeply concerned about minor performance penalties, in which case we are simply considering different scenarios).</strong></p></blockquote><h4 id="is-fpic-exclusive-to-dynamic-libraries-can-we-use-fpic-with-static-libraries" tabindex="-1">Is <code>-fPIC</code> exclusive to dynamic libraries? Can we use <code>-fPIC</code> with static libraries? <a class="header-anchor" href="#is-fpic-exclusive-to-dynamic-libraries-can-we-use-fpic-with-static-libraries" aria-label="Permalink to &quot;Is `-fPIC` exclusive to dynamic libraries? Can we use `-fPIC` with static libraries?&quot;">​</a></h4><p>Obviously not; otherwise, there would be no need to make this flag independent. In fact, we can absolutely apply <code>-fPIC</code> to relocatable files that are destined to be compiled into a static library. This is very common.</p><p>For example, I have a fairly large project on hand that generates a static library for each sub-module, and then packages all the generated static libraries in a directory into a single dynamic library. As we discussed in previous articles, a static library is simply a collection of relocatable files. So, it naturally follows that in this scenario, we must compile the source files with the <code>-fPIC</code> flag for the relocatable files contained within those static libraries.</p>',24)])])}const u=i(r,[["render",s]]);export{m as __pageData,u as default};
