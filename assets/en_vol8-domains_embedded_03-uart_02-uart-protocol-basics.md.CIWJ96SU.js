import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.CZcFxxdw.js";const u=JSON.parse('{"title":"Part 32: Detailed Explanation of the UART Protocol — How to Synchronize Without a Clock Line","description":"","frontmatter":{"title":"Part 32: Detailed Explanation of the UART Protocol — How to Synchronize Without a Clock Line","description":"","tags":["beginner","cpp-modern","stm32f1"],"difficulty":"beginner","platform":"stm32f1","chapter":17,"order":2},"headers":[],"relativePath":"en/vol8-domains/embedded/03-uart/02-uart-protocol-basics.md","filePath":"en/vol8-domains/embedded/03-uart/02-uart-protocol-basics.md","lastUpdated":null}'),n={name:"en/vol8-domains/embedded/03-uart/02-uart-protocol-basics.md"};function r(o,e,l,h,p,d){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="part-32-uart-protocol-in-depth-—-how-to-synchronize-without-a-clock-line" tabindex="-1">Part 32: UART Protocol In Depth — How to Synchronize Without a Clock Line <a class="header-anchor" href="#part-32-uart-protocol-in-depth-—-how-to-synchronize-without-a-clock-line" aria-label="Permalink to &quot;Part 32: UART Protocol In Depth — How to Synchronize Without a Clock Line&quot;">​</a></h1><blockquote><p>Following up on the previous article: we learned what UART is, why we should learn it, and how to wire the hardware. In this article, we tear down the protocol itself to understand exactly what &quot;no clock line&quot; means.</p></blockquote><hr><h2 id="the-core-challenge-of-asynchronous-communication" tabindex="-1">The Core Challenge of Asynchronous Communication <a class="header-anchor" href="#the-core-challenge-of-asynchronous-communication" aria-label="Permalink to &quot;The Core Challenge of Asynchronous Communication&quot;">​</a></h2><p>If you have worked with SPI or I2C before, you will remember they both have a dedicated clock line. SPI has SCK, and I2C has SCL. The purpose of the clock line is clear: the transmitter places data on one edge of the clock signal, and the receiver reads data on the other edge. The clock acts like a conductor&#39;s baton—every beat has a precise moment, and everyone knows exactly when to do what.</p><p>UART has no baton. The TX and RX lines operate independently—the TX line carries only the signal from the transmitter, and the RX line carries only the signal arriving at the receiver. There is no shared clock transition to tell the receiver, &quot;this is the start of a new bit.&quot; So how does the receiver know where a data frame begins, where it ends, and where the boundary of each bit lies in between?</p><p>The answer is: both sides agree on a rate before communication begins, and then each side uses its own clock to &quot;count the beats&quot; at that rate. This agreed-upon rate is the baud rate. For example, if both sides agree on 115200 baud, it means 115200 bits are transmitted per second, so the duration of each bit is 1/115200 ≈ 8.68 microseconds. The transmitter places a new bit on the TX line every 8.68 microseconds, and the receiver samples a bit from the RX line every 8.68 microseconds. If both clocks are accurate enough, the whole process aligns.</p><p>This is what &quot;asynchronous&quot; means—no shared clock, but achieving synchronization through a pre-agreed rate plus each side&#39;s local clock. Sounds unreliable? It actually works extremely well, because the receiver uses a technique called &quot;oversampling&quot; to align its sampling moments.</p><hr><h2 id="anatomy-of-a-data-frame" tabindex="-1">Anatomy of a Data Frame <a class="header-anchor" href="#anatomy-of-a-data-frame" aria-label="Permalink to &quot;Anatomy of a Data Frame&quot;">​</a></h2><p>A complete UART data frame consists of the following parts. Let&#39;s break it down from start to finish:</p><h3 id="idle-state" tabindex="-1">Idle State <a class="header-anchor" href="#idle-state" aria-label="Permalink to &quot;Idle State&quot;">​</a></h3><p>When no data is being transmitted, the TX line remains high. This is the default state of UART—when no one is talking on the line, it stays high. This is important because it allows the receiver to distinguish between &quot;no one is talking&quot; and &quot;some state during an active transmission.&quot;</p><h3 id="start-bit" tabindex="-1">Start Bit <a class="header-anchor" href="#start-bit" aria-label="Permalink to &quot;Start Bit&quot;">​</a></h3><p>When the transmitter is ready to send a byte, it first pulls the TX line low for the duration of one bit. This high-to-low transition is the start bit. The start bit is the anchor of the entire frame—when the receiver detects this falling edge, it knows &quot;data is coming&quot; and uses this as a reference point to start sampling the subsequent bits.</p><p>Why is the start bit always low? Because the idle state is high. A high-to-low transition is a definitive signal change that the receiver cannot confuse with the idle state. If the idle state were also low, the low level of the start bit would be indistinguishable from the idle state.</p><h3 id="data-bits" tabindex="-1">Data Bits <a class="header-anchor" href="#data-bits" aria-label="Permalink to &quot;Data Bits&quot;">​</a></h3><p>Following the start bit is the actual data. UART supports 7, 8, or 9 data bits, with 8 bits being the most common configuration (which is why we often say UART transmits &quot;one byte&quot;). Data is sent starting from the least significant bit (LSB)—bit0 first, then bit1, and so on. This means if you send the value <code>0x41</code> (the letter &#39;A&#39;, binary <code>01000001</code>), the actual order on the wire is <code>1-0-0-0-0-0-1-0</code>.</p><p>Eight data bits can cover the standard ASCII character set (0-127) and extended ASCII (128-255). Nine data bits are typically used for address/data marking in multi-drop communication protocols—the 9th bit distinguishes whether the current frame is an address or data.</p><h3 id="parity-bit-—-optional" tabindex="-1">Parity Bit — Optional <a class="header-anchor" href="#parity-bit-—-optional" aria-label="Permalink to &quot;Parity Bit — Optional&quot;">​</a></h3><p>After the data bits, an optional parity bit can be added. The purpose of the parity bit is to ensure that the total number of &quot;1&quot;s in the entire frame (data bits + parity bit) meets a specific odd or even requirement. There are three choices: no parity (None, most common), even parity (Even, total number of 1s is even), and odd parity (Odd, total number of 1s is odd). When no parity is selected, this bit is omitted entirely, which is the configuration used in the vast majority of embedded projects.</p><p>A parity bit can detect single-bit errors, but the cost is transmitting an extra bit, and in noisy environments, the detection rate of a single-bit parity check is not high enough. In real-world projects, we either skip parity (relying on upper-layer protocols for CRC) or use 9 data bits for special purposes.</p><h3 id="stop-bit" tabindex="-1">Stop Bit <a class="header-anchor" href="#stop-bit" aria-label="Permalink to &quot;Stop Bit&quot;">​</a></h3><p>The end of the frame is the stop bit, which is fixed at a high level and lasts for 1 or 2 bit times (some devices support 1.5 bits). The stop bit pulls the TX line back to high—也就是 the idle state. It serves two purposes: first, it lets the receiver confirm &quot;this frame is over&quot;; second, it ensures that the start bit of the next frame can produce a clean high-to-low transition (because the stop bit pulled the line back to high).</p><p>A complete 8N1 frame (8 data bits, no parity, 1 stop bit) looks like this in timing:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>空闲  起始位  D0  D1  D2  D3  D4  D5  D6  D7  停止位  空闲</span></span>
<span class="line"><span>HIGH   LOW   x   x   x   x   x   x   x   x   HIGH   HIGH</span></span>
<span class="line"><span>       |&lt;--- 10 bits 总共 (1+8+1) ---&gt;|</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Transmitting one byte requires sending 10 bits (1 start bit + 8 data bits + 1 stop bit). At 115200 baud, the transmission time for one frame is 10/115200 ≈ 86.8 microseconds, and the effective data rate is 11520 bytes per second.</p><hr><h2 id="baud-rate-and-oversampling" tabindex="-1">Baud Rate and Oversampling <a class="header-anchor" href="#baud-rate-and-oversampling" aria-label="Permalink to &quot;Baud Rate and Oversampling&quot;">​</a></h2><p>The baud rate is defined as the number of symbols transmitted per second. In UART, one symbol is one bit, so the baud rate equals the bit rate. Common baud rates include 9600, 19200, 38400, 57600, 115200, 230400, 460800, and 921600. Among these, 115200 is the most common default in embedded projects—it is fast enough to meet most debugging and communication needs, while its demands on clock accuracy are not too strict.</p><p>You might wonder why these numbers are not round tens or hundreds—9600, 115200, rather than 10000, 100000. The reason is that these numbers relate to clock division in early telecommunication systems. 9600 = 9600, 115200 = 9600 x 12. Historically, clock sources were typically 1.8432 MHz or multiples thereof, and dividing by the appropriate integer yielded these baud rates.</p><h3 id="oversampling-how-the-receiver-finds-the-center-of-a-bit" tabindex="-1">Oversampling: How the Receiver Finds the Center of a Bit <a class="header-anchor" href="#oversampling-how-the-receiver-finds-the-center-of-a-bit" aria-label="Permalink to &quot;Oversampling: How the Receiver Finds the Center of a Bit&quot;">​</a></h3><p>As mentioned earlier, the receiver samples once per bit time according to the agreed baud rate. But here is the problem: the receiver&#39;s clock and the transmitter&#39;s clock can never be perfectly identical. If there is a slight deviation (for example, the transmitter&#39;s actual rate is 115201 baud and the receiver&#39;s is 115199 baud), as the number of bits increases, the sampling point will gradually drift away from the center, eventually leading to incorrect sampled values.</p><p>The solution is oversampling. The STM32 USART receiver does not sample just once per bit time; instead, it samples 16 times (16x oversampling) or 8 times (8x oversampling). 16x oversampling is the default mode and the one we use in our code.</p><p>The process works like this: after the receiver detects the falling edge of the start bit, it samples at 16 times the baud rate. For 115200 baud, the sampling frequency is 115200 x 16 = 1,843,200 Hz. The receiver confirms the start bit is valid at the middle of the start bit (the 8th sample point), and then reads data every 16 sample points—meaning it samples right at the center of each bit. Even if the two devices&#39; clocks have a minor deviation, as long as the deviation is within 2-3%, the cumulative offset over 16 sample points is not enough to let the sampling point slip out of the current bit&#39;s range, and communication remains reliable.</p><p>This is why the oversampling configuration we see in <code>uart_config.hpp</code> is fixed to <code>UART_OVERSAMPLING_16</code>:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 来源: code/stm32f1-tutorials/3_uart_logger/device/uart/uart_driver.hpp</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">huart_.Init.OverSampling </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> UART_OVERSAMPLING_16;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h3 id="baud-rate-error-why-correct-configuration-sometimes-still-produces-garbled-text" tabindex="-1">Baud Rate Error: Why &quot;Correct Configuration&quot; Sometimes Still Produces Garbled Text <a class="header-anchor" href="#baud-rate-error-why-correct-configuration-sometimes-still-produces-garbled-text" aria-label="Permalink to &quot;Baud Rate Error: Why &quot;Correct Configuration&quot; Sometimes Still Produces Garbled Text&quot;">​</a></h3><p>Ideally, the receiver&#39;s and transmitter&#39;s baud rates are perfectly identical. But in reality, the baud rate is derived by dividing the system clock, and the divisor can only be an integer. If your system clock is 64 MHz (as configured in our code) and you want to generate 115200 baud:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BRR = 64,000,000 / 115200 = 555.555...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>After rounding, BRR = 556, actual baud rate = 64,000,000 / 556 = 115107.9, error = (115200 - 115107.9) / 115200 = 0.08%. This error is well within UART&#39;s tolerance (typically 2-3%), so communication is fine.</p><p>But if you set a higher baud rate, such as 921600:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BRR = 64,000,000 / 921600 = 69.444...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>After rounding, BRR = 69, actual baud rate = 64,000,000 / 69 = 927536.2, error = (927536.2 - 921600) / 921600 = 0.64%. This is still within the tolerance range, but it is an order of magnitude larger than with 115200.</p><p>Our code has a <code>consteval</code> function that checks this error at compile time, ensuring it does not exceed three-thousandths (3%):</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 来源: code/stm32f1-tutorials/3_uart_logger/device/uart/uart_config.hpp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint32_t</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> APBClockHz</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint32_t</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BaudRate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">consteval</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> bool</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> is_baud_rate_valid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> brr    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (APBClockHz </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BaudRate </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BaudRate;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> actual </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> APBClockHz </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> brr;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> error_permille </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        (actual </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BaudRate) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (actual </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BaudRate) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> /</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BaudRate</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                            :</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (BaudRate </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> actual) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> /</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BaudRate;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> error_permille </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>We will break down this function in detail in Part 40 when we discuss C++ template metaprogramming. For now, you just need to know: the compiler checks for you at compile time whether &quot;this baud rate&#39;s error is acceptable at your clock frequency.&quot; If it is not acceptable, compilation fails directly—rather than waiting until you flash the firmware to the board only to discover that everything you receive is garbled.</p><hr><h2 id="flow-control" tabindex="-1">Flow Control <a class="header-anchor" href="#flow-control" aria-label="Permalink to &quot;Flow Control&quot;">​</a></h2><p>UART also has an optional mechanism called flow control, used to prevent data loss when the receiver cannot process data fast enough. There are two methods:</p><p>Hardware flow control uses two additional signal lines: RTS (Request To Send) and CTS (Clear To Send). When the receiver&#39;s buffer is nearly full, it pulls the RTS signal high to tell the transmitter &quot;pause transmission&quot;; when the buffer has space again, it pulls RTS low to resume transmission. CTS works in the opposite direction—the transmitter checks the CTS signal to decide whether to continue sending.</p><p>Software flow control uses special control characters XON (0x11) and XOFF (0x13) to replace the hardware signal lines. The receiver sends XOFF to tell the other side to pause, and XON to tell it to resume. The advantage is that no extra wires are needed; the disadvantage is that these control characters cannot appear in normal data.</p><p>Our code is configured with no flow control (<code>HwFlowControl::None</code>), which is the simplest setup. For 115200 baud debug communication, the data volume is usually small, so flow control is unnecessary. In high-speed communication or high-volume scenarios (such as transferring a firmware image over UART), you might need to consider enabling hardware flow control.</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 来源: code/stm32f1-tutorials/3_uart_logger/device/uart/uart_config.hpp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HwFlowControl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    None</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">   =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> UART_HWCONTROL_NONE,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    Rts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> UART_HWCONTROL_RTS,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    Cts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> UART_HWCONTROL_CTS,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    RtsCts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> UART_HWCONTROL_RTS_CTS,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><hr><h2 id="signal-levels-ttl-vs-rs-232" tabindex="-1">Signal Levels: TTL vs RS-232 <a class="header-anchor" href="#signal-levels-ttl-vs-rs-232" aria-label="Permalink to &quot;Signal Levels: TTL vs RS-232&quot;">​</a></h2><p>The last concept to clarify is signal levels.</p><p>The USART pins on the STM32F103 output TTL levels: logic high = 3.3V (close to VDD), logic low = 0V (close to GND). This voltage range is well-suited for chip-to-chip communication or for connecting to a PC via a USB-TTL adapter.</p><p>Historically, however, UART used RS-232 levels: logic high = -3V to -15V, logic low = +3V to +15V. The RS-232 voltage range is much higher than TTL, so it supports longer transmission distances and has stronger noise immunity. If your project needs to connect to the RS-232 serial port of legacy equipment (such as industrial PCs or old instruments), you will need a level-shifting chip (such as the MAX232) between the STM32 and the RS-232 interface.</p><p>We are using a USB-TTL adapter—one end is USB (connected to the PC), and the other end has TTL-level TX/RX/GND (connected to the Blue Pill). Both sides use TTL levels, so we can connect them directly with Dupont wires, with no level shifting needed.</p><p>Let&#39;s go over the wiring one more time, because this is really easy to get backwards:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>USB-TTL 适配器      Blue Pill</span></span>
<span class="line"><span>  TX ────────────── PA10 (USART1 RX)</span></span>
<span class="line"><span>  RX ────────────── PA9  (USART1 TX)</span></span>
<span class="line"><span>  GND ───────────── GND</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The adapter&#39;s TX connects to the Blue Pill&#39;s RX, and the adapter&#39;s RX connects to the Blue Pill&#39;s TX. &quot;I send, you receive; you send, I receive&quot;—remember this crossover relationship, and you will avoid the number one wiring mistake in UART debugging.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>In this article, we broke down the complete mechanism of the UART protocol: no shared clock, relying on a pre-agreed baud rate plus oversampling for synchronization; data frames consist of a start bit, data bits, an optional parity bit, and a stop bit; baud rate error must be kept within 3%; and TTL levels can connect directly to a USB-TTL adapter to communicate with a PC.</p><p>In the next article, we shift to the hardware: what exactly the USART peripheral on the STM32F103 looks like, what registers it has, and how to configure the clock and GPIO alternate functions. Once we understand these, we will be ready to write code in the article after that.</p>`,67)])])}const k=a(n,[["render",r]]);export{u as __pageData,k as default};
