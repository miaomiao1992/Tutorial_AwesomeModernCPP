import{_ as n,o as i,c as t,a2 as o}from"./chunks/framework.CeYlivdO.js";const p=JSON.parse('{"title":"Inlining and compiler optimization","description":"Exploring how inline functions work","frontmatter":{"chapter":2,"cpp_standard":[11,14,17,20],"description":"Exploring how inline functions work","difficulty":"intermediate","order":2,"platform":"host","prerequisites":["Chapter 1: 构建工具链"],"reading_time_minutes":4,"tags":["cpp-modern","host","intermediate"],"title":"Inlining and compiler optimization"},"headers":[],"relativePath":"en/vol6-performance/02-inline-and-compiler-optimization.md","filePath":"en/vol6-performance/02-inline-and-compiler-optimization.md","lastUpdated":null}'),a={name:"en/vol6-performance/02-inline-and-compiler-optimization.md"};function s(r,e,l,c,d,h){return i(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="modern-c-for-embedded-systems-tutorial—inline-functions-and-compiler-optimization" tabindex="-1">Modern C++ for Embedded Systems Tutorial—Inline Functions and Compiler Optimization <a class="header-anchor" href="#modern-c-for-embedded-systems-tutorial—inline-functions-and-compiler-optimization" aria-label="Permalink to &quot;Modern C++ for Embedded Systems Tutorial—Inline Functions and Compiler Optimization&quot;">​</a></h1><p>In embedded development, <code>inline</code> is a keyword almost every engineer uses. It seems simple and straightforward, even carrying a hint of &quot;performance guarantee&quot;: if a function is short, called frequently, and timing-sensitive, just <code>inline</code> it—seems like a no-brainer.</p><p>True, in the past, the <code>inline</code> keyword did serve this purpose. But in reality, in the era of modern C++ and with today&#39;s highly advanced compiler optimizations, <strong><code>inline</code> is not a performance optimization button, and it often does nothing at all.</strong></p><h2 id="inline-was-never-born-to-be-fast" tabindex="-1"><code>inline</code> Was Never Born to Be &quot;Fast&quot; <a class="header-anchor" href="#inline-was-never-born-to-be-fast" aria-label="Permalink to &quot;`inline` Was Never Born to Be &quot;Fast&quot;&quot;">​</a></h2><p>At the language level, the core purpose of <code>inline</code> is actually quite restrained. The modern C++ standard does not promise that &quot;if you write <code>inline</code>, the compiler will definitely expand the function body.&quot; The only thing it truly guarantees is this: <strong>it allows the function to have definitions in multiple translation units without violating the ODR.</strong></p><p>This is why a large number of small functions in header files, template functions, and <code>traits</code> utility functions naturally carry an <code>inline</code> vibe. It solves a &quot;linkage-level problem,&quot; not a &quot;performance problem.&quot; As for whether inlining actually occurs, that is entirely at the compiler&#39;s discretion. In the face of modern compilers, <code>inline</code> is more like a suggestion—&quot;I think you might consider expanding this&quot;—rather than a command.</p><hr><h2 id="so-are-function-calls-really-slow-in-embedded" tabindex="-1">So, Are Function Calls Really Slow in Embedded? <a class="header-anchor" href="#so-are-function-calls-really-slow-in-embedded" aria-label="Permalink to &quot;So, Are Function Calls Really Slow in Embedded?&quot;">​</a></h2><p>Much of the intuition surrounding <code>inline</code> stems from a fear of function call overhead. On architectures like Cortex-M, a function call indeed involves a jump, saving LR, parameter passing, and restoring the return path. If you stare at the assembly line by line, it&#39;s easy to conclude: this doesn&#39;t look cheap.</p><p>The problem is whether this cost <strong>actually lands on your performance bottleneck path</strong>.</p><p>In real-world embedded engineering, the time consumption of many functions doesn&#39;t lie in the &quot;call itself&quot; at all, but rather in peripheral access, bus waits, Flash reads, cache misses, or even interrupt preemption. Agonizing over whether to <code>inline</code> a GPIO read function is often optimizing at a completely irrelevant level.</p><p>More importantly, with optimizations enabled (even just <code>-O2</code>), short, side-effect-free functions with clear semantics <strong>will almost certainly be automatically inlined by the compiler, even if you don&#39;t write <code>inline</code>.</strong></p><p>When deciding whether to inline a function today, compilers consider a combination of factors: function body size, number of call sites, register pressure, instruction cache behavior, and even cross-file call graph analysis when LTO is enabled. The information it has far exceeds the little context you see when writing the code. This is why you often encounter this situation: you explicitly write <code>inline</code>, but the disassembly reveals the function still exists; whereas when you write nothing, the function is silently expanded.</p><hr><h2 id="an-inline-that-expands-into-a-call-isn-t-very-safe" tabindex="-1">An <code>inline</code> That Expands Into a Call Isn&#39;t Very Safe <a class="header-anchor" href="#an-inline-that-expands-into-a-call-isn-t-very-safe" aria-label="Permalink to &quot;An `inline` That Expands Into a Call Isn&#39;t Very Safe&quot;">​</a></h2><p>If the biggest risk of <code>inline</code> in PC development is &quot;doing nothing,&quot; then in embedded systems, its real risk is often <strong>code bloat</strong>.</p><p>The essence of inlining is copying. A frequently called small function, if expanded in multiple places, will have its instructions physically duplicated. On MCUs with tight Flash resources, this duplication cannot be ignored. A more subtle point is that larger code doesn&#39;t just consume Flash; it also affects instruction cache locality. Even on cores with an I-Cache, excessive inlining can lead to more cache misses, ultimately manifesting as a performance decrease rather than an improvement.</p><hr><h2 id="so-when-is-inline-truly-valuable" tabindex="-1">So, When Is <code>inline</code> Truly Valuable? <a class="header-anchor" href="#so-when-is-inline-truly-valuable" aria-label="Permalink to &quot;So, When Is `inline` Truly Valuable?&quot;">​</a></h2><p>In practice, the scenarios where <code>inline</code> truly shows its value are often not &quot;to save one function call,&quot; but rather to <strong>eliminate the cost of abstraction boundaries</strong>.</p><p>Examples include template functions, type-safe register access wrappers, and compile-time calculations involving <code>constexpr</code>. In these places, <code>inline</code> allows us to write highly expressive C++ code, while at the generated assembly level, it is virtually indistinguishable from hand-written C.</p><p>This is the most fascinating aspect of modern C++ in the embedded domain: <strong>abstraction is not a burden, but a semantic tool that can be completely optimized away.</strong></p><p>In an interrupt service routine (ISR) or an extreme hot path, <code>inline</code> might also be reasonable, but there is only ever one prerequisite: you have actually looked at the assembly and confirmed that it solves a real problem.</p>',23)])])}const m=n(a,[["render",s]]);export{p as __pageData,m as default};
