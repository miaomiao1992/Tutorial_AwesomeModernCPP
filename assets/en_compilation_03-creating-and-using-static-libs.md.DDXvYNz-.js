import{_ as t,o as i,c as a,a2 as o}from"./chunks/framework.CZcFxxdw.js";const m=JSON.parse('{"title":"In-depth Understanding of C/C++ Compilation and Linking Techniques 3: How to Create and Use Static Libraries","description":"","frontmatter":{"title":"In-depth Understanding of C/C++ Compilation and Linking Techniques 3: How to Create and Use Static Libraries","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":13,"order":3},"headers":[],"relativePath":"en/compilation/03-creating-and-using-static-libs.md","filePath":"en/compilation/03-creating-and-using-static-libs.md","lastUpdated":null}'),s={name:"en/compilation/03-creating-and-using-static-libs.md"};function r(n,e,l,d,c,h){return i(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="deep-dive-into-c-c-compilation-and-linking-part-3-how-to-create-and-use-static-libraries" tabindex="-1">Deep Dive into C/C++ Compilation and Linking Part 3: How to Create and Use Static Libraries <a class="header-anchor" href="#deep-dive-into-c-c-compilation-and-linking-part-3-how-to-create-and-use-static-libraries" aria-label="Permalink to &quot;Deep Dive into C/C++ Compilation and Linking Part 3: How to Create and Use Static Libraries&quot;">​</a></h1><p>In the previous blog post, I briefly covered the basic introduction to static and dynamic libraries. I have linked them here:</p><blockquote><p><a href="https://blog.csdn.net/charlie114514191/article/details/152921903" target="_blank" rel="noreferrer">Deep Dive into C/C++ Compilation and Linking - CSDN Blog</a></p><p><a href="https://blog.csdn.net/charlie114514191/article/details/154828385" target="_blank" rel="noreferrer">Deep Dive into C/C++ Compilation and Linking Part 2: Introduction to Dynamic and Static Libraries - CSDN Blog</a></p></blockquote><p>So earlier, we simply explained what the essence of a static library is. Although today, using dynamic libraries for code sharing is a more fundamental strategy, for the sake of completeness—and because I personally like using static libraries to package something that only depends on <code>C/C++</code> the most basic runtime (honestly, I have no solid technical reason for this choice; I just don&#39;t like feeding a massive chunk of relocatable files directly to the linker)—let&#39;s continue.</p><h2 id="how-do-we-create-a-static-library" tabindex="-1">How Do We Create a Static Library? <a class="header-anchor" href="#how-do-we-create-a-static-library" aria-label="Permalink to &quot;How Do We Create a Static Library?&quot;">​</a></h2><h3 id="ar-tool" tabindex="-1"><code>ar</code> Tool <a class="header-anchor" href="#ar-tool" aria-label="Permalink to &quot;`ar` Tool&quot;">​</a></h3><p>So a natural question arises: we previously learned the fundamental principle of a static library (an organic combination of several relocatable files), but how do we actually create one? The answer is a small yet powerful tool—<code>ar</code> (Archiver).</p><p>Let me briefly introduce <code>ar</code>! It is a tool used to create, modify, and extract <strong>archive files</strong>. These archive files typically end with the <code>.a</code> extension (where &quot;a&quot; stands for archive), and their most common use case is packaging object files (<code>.o</code> files) to create <strong>static libraries</strong>. On Linux, if we are creating a static library (at least a static library) and decide to name it <code>Charlie</code>, the generated library will generally be <code>libCharlie.a</code>.</p><p>Some might wonder why it must start with <code>lib</code>. Wouldn&#39;t generating <code>Charlie.a</code> be more intuitive? The core reason is: <strong>this is dictated by the working conventions of the linker when we perform linking later</strong>. Most commonly, when we <code>gcc/g++</code> compile and prepare to link objects, we dispatch <code>ld</code> to link the target libraries and relocatable files. Generally, upper-level build tools prefer using the <code>-L</code> flag to specify the search directory combined with <code>-l</code> (a lowercase L) to find the library. For example, when we try to provide the well-known <code>math</code> static library to <code>main.c</code> at a specified path, we might write something like this:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gcc main.c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">lmath</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The linker does not directly look for a file named <code>math</code>. Instead, following convention, it attempts to find a file named <strong><code>libmath.a</code></strong> (static library) or <strong><code>libmath.so</code></strong> (dynamic library). Simply put:</p><ul><li>The name following the <code>-l</code> flag (<code>math</code> in this example) is called the &quot;library name&quot;.</li><li>The linker automatically prepends the prefix <code>lib</code> to this name.</li><li>Then, depending on the situation (and priority), it appends a suffix like <code>.a</code> (static library) or <code>.so</code> (dynamic library) to form the complete filename.</li></ul><p>Therefore, <strong>naming the library file in the <code>lib&lt;name&gt;.a</code> format is to proactively cater to the linker&#39;s automatic search mechanism</strong>. If the library file is not named in this format, the linker cannot find it via the convenient <code>-l</code> option. You would have to link by directly specifying the full path to the library file, which is very cumbersome. Furthermore, this leads to a serious issue that we will revisit when discussing dynamic libraries (for static libraries, it doesn&#39;t matter since they get packaged into the target file).</p><h3 id="common-command-formats-for-ar" tabindex="-1">Common Command Formats for <code>ar</code> <a class="header-anchor" href="#common-command-formats-for-ar" aria-label="Permalink to &quot;Common Command Formats for `ar`&quot;">​</a></h3><p>The basic syntax of <code>ar</code> is relatively simple. It requires an <strong>operation code</strong> (similar to a main command) and some <strong>modifiers</strong> to specify the exact behavior.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [操作码][修饰符] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">归档文件名</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">文件...</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><table tabindex="0"><thead><tr><th><strong>Operation Code</strong></th><th><strong>Description</strong></th><th><strong>Common Modifiers</strong></th><th><strong>Example Command</strong></th></tr></thead><tbody><tr><td><strong><code>r</code></strong></td><td><strong>Insert/Replace</strong>: Adds files to the archive. If a file with the same name already exists, it replaces it.</td><td><code>v</code> (verbose output)</td><td><code>ar rv libmy.a file1.o file2.o</code></td></tr><tr><td><strong><code>t</code></strong></td><td><strong>List</strong>: Displays the list of files contained in the archive.</td><td><code>v</code> (verbose output)</td><td><code>ar t libmy.a</code></td></tr><tr><td><strong><code>x</code></strong></td><td><strong>Extract</strong>: Extracts (unpacks) files from the archive.</td><td><code>v</code> (verbose output)</td><td><code>ar xv libmy.a</code></td></tr></tbody></table><blockquote><p>Checking the man page is always a good idea: <a href="https://linux.die.net/man/1/ar" target="_blank" rel="noreferrer">ar(1) - Linux man page</a></p></blockquote><h3 id="what-about-windows" tabindex="-1">What About Windows? <a class="header-anchor" href="#what-about-windows" aria-label="Permalink to &quot;What About Windows?&quot;">​</a></h3><p>This is actually handled by the MSVC toolchain. However, few people do this manually on Windows. On Windows, most people delegate this task to the massive IDE: Visual Studio, or like me, use lightweight Visual Studio Code and delegate to CMake. For specific details, you can check the detailed build logs from CMake, but I won&#39;t expand on that here for the sake of brevity.</p><h2 id="where-do-we-use-static-libraries" tabindex="-1">Where Do We Use Static Libraries? <a class="header-anchor" href="#where-do-we-use-static-libraries" aria-label="Permalink to &quot;Where Do We Use Static Libraries?&quot;">​</a></h2><p>I thought about this carefully, combining my shallow engineering experience (which I can honestly say is almost non-existent) with the materials I have read. In fact, today, static libraries can almost entirely be replaced by dynamic libraries. However, in the following scenarios, using static libraries is clearly more appropriate. Of course, I use static libraries more often in embedded systems, so I will frame it this way:</p><ul><li><strong>Simplified distribution:</strong> We only need to distribute a single executable file, without carrying a bunch of <code>.dll</code> (Windows) or <code>.so</code>/<code>.dylib</code> (Linux/macOS) files.</li><li><strong>Version locking:</strong> We need to <strong>absolutely guarantee</strong> that our program uses a specific version of a library, free from interference by other versions on the user&#39;s system.</li><li><strong>Small tools or embedded systems:</strong> In environments with strict limitations on the number of files or dynamic linking support.</li></ul><h2 id="conversely-what-are-the-reasons-not-to-use-static-libraries" tabindex="-1">Conversely, What Are the Reasons Not to Use Static Libraries? <a class="header-anchor" href="#conversely-what-are-the-reasons-not-to-use-static-libraries" aria-label="Permalink to &quot;Conversely, What Are the Reasons Not to Use Static Libraries?&quot;">​</a></h2><p>Reviewing the previous blog post, we have already explained how static libraries work. So, it is easy to think of the first reason not to use them:</p><h4 id="executable-bloat" tabindex="-1">Executable Bloat <a class="header-anchor" href="#executable-bloat" aria-label="Permalink to &quot;Executable Bloat&quot;">​</a></h4><p>When focusing on <strong>interface reuse</strong>, using static libraries obviously causes the size of all dependent libraries and executables to increase dramatically (executable bloat). Therefore, <strong>for any module whose purpose is to provide functional interfaces to other dependencies and that operates independently, please use a dynamic library</strong>. In this case, keeping only one copy of the code dependency and letting the operating system and loader automatically coordinate all mapped symbol relationships is clearly the better approach.</p><h4 id="updates-require-recompilation-and-redistribution-hot-reloading-request" tabindex="-1">Updates Require Recompilation and Redistribution (Hot Reloading Request) <a class="header-anchor" href="#updates-require-recompilation-and-redistribution-hot-reloading-request" aria-label="Permalink to &quot;Updates Require Recompilation and Redistribution (Hot Reloading Request)&quot;">​</a></h4><p>In scenarios that prioritize <strong>hot reloading</strong>, using static libraries is clearly inappropriate. For example, when we cannot easily replace the executable file itself but only need to update a sub-dependency (say, a library we use has a vulnerability discovered by an enthusiastic open-source developer who promptly reports it to us)—that is, when we discover a security vulnerability or a bug in the library that needs fixing—with a static library, we must <strong>recompile and redistribute the entire application (static linking makes this code a part of the main body rather than a required dependency)</strong>.</p><h4 id="potential-symbol-collisions-and-version-management-issues-symbol-collisions" tabindex="-1">Potential Symbol Collisions and Version Management Issues (Symbol Collisions) <a class="header-anchor" href="#potential-symbol-collisions-and-version-management-issues-symbol-collisions" aria-label="Permalink to &quot;Potential Symbol Collisions and Version Management Issues (Symbol Collisions)&quot;">​</a></h4><p>If we link <strong>multiple versions</strong> of static libraries or static libraries with <strong>identical symbol names</strong> into the same executable, the compiler/linker will attempt to resolve them, but the risk is very high (if I remember correctly, it drops them randomly based on symbol strength and equality). This is truly dangerous; nobody likes playing a guessing game with their program.</p>',31)])])}const b=t(s,[["render",r]]);export{m as __pageData,b as default};
