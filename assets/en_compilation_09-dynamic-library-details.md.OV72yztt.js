import{_ as i,o as a,c as s,a2 as o}from"./chunks/framework.BuBU3QrY.js";const m=JSON.parse('{"title":"Deep Dive into C/C++ Compilation and Linking Techniques 9: Shared Library Details (Final)","description":"","frontmatter":{"title":"Deep Dive into C/C++ Compilation and Linking Techniques 9: Shared Library Details (Final)","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":13,"order":9,"translation":{"source":"documents/compilation/09-dynamic-library-details.md","source_hash":"3d81feb96f6f0cfd74cb5164d00b7320a92674ea432bfe50b6ed987efc14765c","translated_at":"2026-05-26T10:13:10.899158+00:00","engine":"anthropic","token_count":1646}},"headers":[],"relativePath":"en/compilation/09-dynamic-library-details.md","filePath":"en/compilation/09-dynamic-library-details.md","lastUpdated":null}'),n={name:"en/compilation/09-dynamic-library-details.md"};function t(l,e,r,d,c,h){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="deep-dive-into-c-c-compilation-and-linking-techniques-9-shared-library-details-finale" tabindex="-1">Deep Dive into C/C++ Compilation and Linking Techniques 9: Shared Library Details (Finale) <a class="header-anchor" href="#deep-dive-into-c-c-compilation-and-linking-techniques-9-shared-library-details-finale" aria-label="Permalink to &quot;Deep Dive into C/C++ Compilation and Linking Techniques 9: Shared Library Details (Finale)&quot;">​</a></h1><h2 id="preface" tabindex="-1">Preface <a class="header-anchor" href="#preface" aria-label="Permalink to &quot;Preface&quot;">​</a></h2><p>Next, we are going to discuss the details of shared libraries. In general, engineering development might not touch on these issues, but understanding how shared libraries work is always better than not. Therefore, drawing on <em>Advanced C/C++ Compilation Techniques</em>, the author will specifically revisit some of the details of shared libraries.</p><h2 id="_8-1-the-necessity-of-resolving-memory-addresses" tabindex="-1"><strong>8.1 The Necessity of Resolving Memory Addresses</strong> <a class="header-anchor" href="#_8-1-the-necessity-of-resolving-memory-addresses" aria-label="Permalink to &quot;**8.1 The Necessity of Resolving Memory Addresses**&quot;">​</a></h2><p>Before moving forward, let&#39;s cover a few assembly concepts.</p><p>Obviously, we know that the basic model of modern computers is the Turing machine: we know where the operands are, fetch them for computation, and put them back.</p><p>Taking x86 as an example, we need to know the address of a memory operand so that we can pass data back and forth between memory and the CPU.</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mov eax, ds:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">BAD10000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ; 将地址0xBAD10000装载到eax中</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">add eax, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ; 装载值自增</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mov ds:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">BAD10000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, eax; 写回操作</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Great. Knowing this, we must point out that the essence of a function call is also finding the function&#39;s address in the code segment—for example, when we want to call a trivial <code>add</code> function, we have to tell our <code>call</code> instruction where the <code>add</code> function is (that is, we need to provide the code segment address of the <code>add</code> function&#39;s entry point).</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">add </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">11451400</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ... ; Add Procedure</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">main:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ... ; Main Procedure</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> call </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">11451400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ; add absolute</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Of course, sometimes we also use <code>call</code> with a relative address, which is a bit more convenient.</p><h2 id="common-issues-in-reference-resolution" tabindex="-1">Common Issues in Reference Resolution <a class="header-anchor" href="#common-issues-in-reference-resolution" aria-label="Permalink to &quot;Common Issues in Reference Resolution&quot;">​</a></h2><p>Let&#39;s look at the simplest case! Suppose an executable file can only work further after loading a single shared library. The following points are obvious:</p><ul><li>The client binary provides a part of the process memory mapping with a fixed and predictable address range.</li><li>Only after dynamic loading is complete does it become a valid part of the process.</li><li>When the executable file calls one or several function implementations provided by the shared library (such as the shared library&#39;s interface), the connection is naturally established at that point.</li></ul><p>From the basic situation above, we can know one thing: the core issue of shared libraries is that <strong>the runtime location of the library code is uncertain</strong>. Whether it is a Windows DLL, a Linux <code>.so</code>, or a macOS <code>dylib</code>, they all share one common trait: <strong>shared libraries cannot determine their final load address at compile time</strong>.</p><p>Why is that? There are mainly three reasons:</p><h4 id="_1-address-conflicts-may-occur-between-multiple-shared-libraries" tabindex="-1"><strong>(1) Address conflicts may occur between multiple shared libraries</strong> <a class="header-anchor" href="#_1-address-conflicts-may-occur-between-multiple-shared-libraries" aria-label="Permalink to &quot;**(1) Address conflicts may occur between multiple shared libraries**&quot;">​</a></h4><p>Suppose two <code>.so</code> files both want to be mapped to the <code>0x400000</code> area in virtual memory; this will cause a conflict. To avoid conflicts, the operating system&#39;s loader must select a different, suitable base address.</p><h4 id="_2-aslr-address-space-layout-randomization" tabindex="-1"><strong>(2) ASLR (Address Space Layout Randomization)</strong> <a class="header-anchor" href="#_2-aslr-address-space-layout-randomization" aria-label="Permalink to &quot;**(2) ASLR (Address Space Layout Randomization)**&quot;">​</a></h4><p>Modern operating systems enable address randomization for security, so the load address of a shared library is different every time. This means: the compiler and linker cannot assume that a shared library will run at a fixed address.</p><h4 id="_3-the-same-shared-library-is-loaded-at-different-locations-in-different-processes" tabindex="-1"><strong>(3) The same shared library is loaded at different locations in different processes</strong> <a class="header-anchor" href="#_3-the-same-shared-library-is-loaded-at-different-locations-in-different-processes" aria-label="Permalink to &quot;**(3) The same shared library is loaded at different locations in different processes**&quot;">​</a></h4><p>Process address spaces are independent of each other, and the library&#39;s load location can be completely different in each process.</p><h2 id="address-conversion-is-the-solution" tabindex="-1">Address Conversion is the Solution <a class="header-anchor" href="#address-conversion-is-the-solution" aria-label="Permalink to &quot;Address Conversion is the Solution&quot;">​</a></h2><h4 id="case-we-want-to-use-exported-binary-symbols" tabindex="-1">Case: We want to use exported binary symbols <a class="header-anchor" href="#case-we-want-to-use-exported-binary-symbols" aria-label="Permalink to &quot;Case: We want to use exported binary symbols&quot;">​</a></h4><p>For example, if we want to use those exported symbols, such as the interfaces provided by the library—<code>create_window</code>, <code>init_all</code>, <code>deinit_all</code>, and so on. This is using exported binary symbols. In this case, the client program obviously needs to immediately know the loaded address, rather than the shared library&#39;s original symbol address (they are offset from zero!). Therefore, the old approach of having the linker complete all symbol resolution directly is obviously impossible. The determination of symbol addresses must be handled jointly with the loader.</p><h4 id="case-calling-private-symbols-internally" tabindex="-1">Case: Calling private symbols internally <a class="header-anchor" href="#case-calling-private-symbols-internally" aria-label="Permalink to &quot;Case: Calling private symbols internally&quot;">​</a></h4><p>Regardless, some private symbols cannot be found by the client program, but there is a more severe problem—what if these symbols are being called by exported symbols? What do we do then?</p><h2 id="linker-loader-cooperation—the-old-technique" tabindex="-1">Linker-Loader Cooperation—The Old Technique <a class="header-anchor" href="#linker-loader-cooperation—the-old-technique" aria-label="Permalink to &quot;Linker-Loader Cooperation—The Old Technique&quot;">​</a></h2><p>Now let&#39;s discuss linker-loader cooperation in detail. Understanding all the constraints described earlier, we can establish cooperation between the linker and the loader based on the following rules:</p><ul><li>The linker recognizes the limitations of its own symbol resolution.</li><li>The linker accurately counts the failed symbol references, prepares reference fixup hints, and embeds these hints into the binary file.</li><li>The loader accurately follows the linker&#39;s relocation hints and performs fixups based on these hints after completing the address conversion.</li></ul><h3 id="the-linker-recognizes-the-limitations-of-its-symbol-resolution" tabindex="-1">The Linker Recognizes the Limitations of Its Symbol Resolution <a class="header-anchor" href="#the-linker-recognizes-the-limitations-of-its-symbol-resolution" aria-label="Permalink to &quot;The Linker Recognizes the Limitations of Its Symbol Resolution&quot;">​</a></h3><p>When creating a shared library, in addition to clearly distinguishing the relationships between different parts of the code, the linker also needs to accurately identify which symbol references will become invalid when the code segment is loaded into different address ranges.</p><p>First, unlike executable files, the memory mapping address range of a shared library starts from zero. When the linker processes an executable file, it mostly does not set the starting point of the address range to zero. Second, before the loading phase, if the linker finds that the addresses of certain symbols cannot be resolved, it will stop resolving them and instead use temporary values to fill the unresolved symbols (usually using obviously incorrect values, such as 0). However, this does not mean the linker completely abandons the symbol resolution task. On the contrary, it only gives up on handling those symbols that truly cannot be resolved.</p><h3 id="next-step-the-linker-accurately-counts-failed-symbol-references-and-prepares-fixup-hints" tabindex="-1">Next Step: The Linker Accurately Counts Failed Symbol References and Prepares Fixup Hints <a class="header-anchor" href="#next-step-the-linker-accurately-counts-failed-symbol-references-and-prepares-fixup-hints" aria-label="Permalink to &quot;Next Step: The Linker Accurately Counts Failed Symbol References and Prepares Fixup Hints&quot;">​</a></h3><p>We can know exactly which resolved references will become invalid due to the loader&#39;s address conversion. As long as an assembly instruction requires an absolute address, the reference in that instruction will become invalid. During the linking phase of building the shared library, the linker can identify the places where absolute addresses appear and let the loader know this information through certain methods. To provide linker-loader cooperation support, the linker reserves some hints for the loader. These hints point out to the loader how to fix the errors caused by address conversion during dynamic loading. The binary format specification supports some new sections specifically reserved for such hints. Additionally, specific simple syntax is designed so that the linker can accurately point out the actions the loader needs to perform.</p><p>These sections in the binary file are called &quot;relocation sections,&quot; and the <code>.rel.dyn</code> section is the oldest relocation section. Generally, the linker writes relocation hints into the binary file so that the loader can read them. These hints specify the addresses the loader needs to patch after completing the final memory mapping layout of the entire process, and the correct actions the loader needs to perform to properly fix the unresolved references.</p><h3 id="the-loader-accurately-follows-the-linker-s-relocation-hints" tabindex="-1">The Loader Accurately Follows the Linker&#39;s Relocation Hints <a class="header-anchor" href="#the-loader-accurately-follows-the-linker-s-relocation-hints" aria-label="Permalink to &quot;The Loader Accurately Follows the Linker&#39;s Relocation Hints&quot;">​</a></h3><p>The final phase belongs to the loader. The loader reads the shared library created by the linker, reads the loader segments within the shared library (each segment holds multiple linker sections), and places all data into the process memory mapping, stored near the original executable file&#39;s code.</p><p>Finally, the loader locates the <code>.rel.dyn</code> section, reads the hints left by the linker, and patches the original shared library code according to these hints. After patching is complete, it is ready to use the memory mapping to start the process. Compared to handling basic tasks, when dealing with shared library loading, we need to provide the loader with more information.</p><h2 id="modern-linker-loader-cooperation-implementation-plt-got" tabindex="-1">Modern Linker-Loader Cooperation Implementation: PLT/GOT <a class="header-anchor" href="#modern-linker-loader-cooperation-implementation-plt-got" aria-label="Permalink to &quot;Modern Linker-Loader Cooperation Implementation: PLT/GOT&quot;">​</a></h2><h4 id="internal-mechanism-of-got-plt" tabindex="-1">Internal Mechanism of GOT / PLT <a class="header-anchor" href="#internal-mechanism-of-got-plt" aria-label="Permalink to &quot;Internal Mechanism of GOT / PLT&quot;">​</a></h4><p>The GOT (Global Offset Table) allows code to avoid relying on fixed addresses, and instead fetches the final address from the table. Of course, this obviously requires us to compile our code with <code>-fPIC</code> (do you now understand why Step 1 for shared libraries is to use PIC, position-independent code!)</p><p>Now, our call becomes something like <code>call [GOT + foo]</code>. To achieve this, once <code>foo</code>&#39;s address is determined, the <code>foo</code> entry in the GOT is written with the actual address. This way, we directly update it.</p><p>PLT works with GOT to implement lazy binding:</p><ul><li>First function call → PLT jumps to the resolver → updates the GOT → jumps directly to the correct address (no more resolving)</li></ul><p>Benefits of PLT:</p><ul><li>Speeds up program startup</li><li>Resolves symbols only when needed</li></ul><hr><h2 id="lazy-binding-process-explained" tabindex="-1"><strong>Lazy Binding Process Explained</strong> <a class="header-anchor" href="#lazy-binding-process-explained" aria-label="Permalink to &quot;**Lazy Binding Process Explained**&quot;">​</a></h2><p>Simply put, lazy binding means deferring the actual setting of the GOT table addresses until the very end; before that, it iteratively resolves all confirmed symbols.</p><ol><li><code>call foo</code> → jump to <code>PLT[foo]</code></li><li><code>PLT[foo]</code> calls the resolver <code>_dl_runtime_resolve</code></li><li>The resolver looks for the symbol <code>foo</code> across all shared libraries</li><li>Update <code>GOT[foo]</code> = the real address of <code>foo</code></li><li>Return to <code>foo</code></li><li>Subsequent calls jump directly to <code>GOT[foo]</code></li></ol><hr><h2 id="duplicate-symbols-in-dynamic-linking" tabindex="-1">Duplicate Symbols in Dynamic Linking <a class="header-anchor" href="#duplicate-symbols-in-dynamic-linking" aria-label="Permalink to &quot;Duplicate Symbols in Dynamic Linking&quot;">​</a></h2><p>In static linking, if two global symbols with the same name appear, the linker usually throws an error directly (Multiple Definition Error). But in the world of <strong>dynamic linking</strong>, the rules are completely different. That is why it is worth discussing separately.</p><h4 id="duplicate-symbol-definitions" tabindex="-1">Duplicate Symbol Definitions <a class="header-anchor" href="#duplicate-symbol-definitions" aria-label="Permalink to &quot;Duplicate Symbol Definitions&quot;">​</a></h4><p>In large projects, we often link multiple third-party libraries. Suppose your program links <code>libA.so</code> and <code>libB.so</code>, and coincidentally, the developers of both libraries defined a global function <code>void init()</code> or a global variable <code>int g_config</code>.</p><p>When your main program starts and loads these two libraries, two symbols named <code>init</code> will exist in memory.</p><h4 id="why-does-this-happen" tabindex="-1">Why Does This Happen? <a class="header-anchor" href="#why-does-this-happen" aria-label="Permalink to &quot;Why Does This Happen?&quot;">​</a></h4><ol><li><strong>Common naming</strong>: Using overly generic names (like <code>utils</code>, <code>log</code>, <code>init</code>) without using <code>static</code> to limit the scope.</li><li><strong>Diamond Dependency</strong>: The project depends on library A and library B, and both A and B internally statically link the same base library C (such as an older version of OpenSSL). This results in C&#39;s symbols having a separate copy in both A and B.</li><li><strong>Header file implementations</strong>: Defining global variables or non-inline functions in header files, which are then included by multiple <code>.c/.cpp</code> files.</li></ol><hr><h2 id="default-handling-of-duplicate-symbols" tabindex="-1">Default Handling of Duplicate Symbols <a class="header-anchor" href="#default-handling-of-duplicate-symbols" aria-label="Permalink to &quot;Default Handling of Duplicate Symbols&quot;">​</a></h2><p>The dynamic linker on Linux (<code>ld-linux</code>) adopts a specific set of rules to handle such conflicts, commonly known as <strong>Symbol Interposition</strong>.</p><h4 id="rule-first-match-wins" tabindex="-1">Rule: First Match Wins <a class="header-anchor" href="#rule-first-match-wins" aria-label="Permalink to &quot;Rule: First Match Wins&quot;">​</a></h4><p>By default, the dynamic linker uses a <strong>Breadth-First Search (BFS)</strong> order to look up symbols. It binds to the <strong>first</strong> matching symbol it finds in the Global Symbol Table and <strong>ignores</strong> all subsequent symbols with the same name.</p><h4 id="load-order-determines-everything" tabindex="-1">Load Order Determines Everything <a class="header-anchor" href="#load-order-determines-everything" aria-label="Permalink to &quot;Load Order Determines Everything&quot;">​</a></h4><p>This means that the <strong>link order</strong> or <strong>load order</strong> determines whose code the program actually calls.</p><p>Suppose <code>app</code> depends on <code>libA</code> and <code>libB</code>, and both have <code>func()</code>:</p><ul><li>If the link command is <code>gcc main.c -lA -lB</code>: when the main program calls <code>func()</code>, it will usually link to <code>libA</code>&#39;s version.</li><li><strong>The dangerous case</strong>: If code inside <code>libB</code> calls <code>func()</code>, according to ELF&#39;s global symbol binding rules, <code>libB</code> will also call <code>libA</code>&#39;s <code>func()</code>! This is known as &quot;symbol hijacking.&quot; <code>libB</code> thinks it is calling its own code, but it actually runs into <code>libA</code>, which can lead to logic errors or even crashes.</li></ul><blockquote><p><strong>Application scenario:</strong> The <code>LD_PRELOAD</code> environment variable leverages exactly this mechanism. By preloading a library containing a <code>malloc</code> implementation, we can override libc&#39;s standard <code>malloc</code>, thereby implementing memory leak detection tools (like Valgrind or jemalloc).</p></blockquote><hr><h2 id="handling-duplicates-during-shared-library-linking" tabindex="-1">Handling Duplicates During Shared Library Linking <a class="header-anchor" href="#handling-duplicates-during-shared-library-linking" aria-label="Permalink to &quot;Handling Duplicates During Shared Library Linking&quot;">​</a></h2><p>Since the default behavior is so dangerous, how do we protect our symbols from being hijacked when developing shared libraries, or avoid hijacking others?</p><h4 id="_1-linker-parameter-bsymbolic" tabindex="-1">1. Linker Parameter: <code>-Bsymbolic</code> <a class="header-anchor" href="#_1-linker-parameter-bsymbolic" aria-label="Permalink to &quot;1. Linker Parameter: \`-Bsymbolic\`&quot;">​</a></h4><p>When compiling a shared library, we can use the linker parameter <code>-Wl,-Bsymbolic</code>.</p><ul><li><strong>Effect</strong>: Forces the shared library to prioritize resolving global symbol references within itself.</li><li><strong>Result</strong>: If <code>libB</code> is compiled with this parameter, then when <code>libB</code> internally calls <code>func()</code>, it will definitely call <code>libB</code>&#39;s own version, and it will not be overridden by <code>libA</code> or the main program.</li></ul><h4 id="_2-symbol-visibility" tabindex="-1">2. Symbol Visibility <a class="header-anchor" href="#_2-symbol-visibility" aria-label="Permalink to &quot;2. Symbol Visibility&quot;">​</a></h4><p>This is a best practice in modern C++ development. Through the GCC/Clang <code>-fvisibility=hidden</code> parameter, all symbols are hidden by default, and only the necessary interfaces are exported.</p><ul><li><p><strong>Code example:</strong></p><div class="language-C vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 只有标记了 DEFAULT 的符号才会被导出到动态符号表</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__attribute__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">visibility</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> public_api</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 即使是全局函数，在外部看来也是不可见的，避免冲突</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> internal_helper</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div></li></ul><h4 id="_3-scope-control-with-dlopen" tabindex="-1">3. Scope Control with <code>dlopen</code> <a class="header-anchor" href="#_3-scope-control-with-dlopen" aria-label="Permalink to &quot;3. Scope Control with \`dlopen\`&quot;">​</a></h4><p>If using <code>dlopen</code> to manually load a library, you can specify the <code>RTLD_LOCAL</code> flag (which is the default value). This prevents the loaded library&#39;s symbols from entering the global symbol table, thereby avoiding affecting other libraries.</p><hr><h3 id="a-few-classic-examples" tabindex="-1">A Few Classic Examples <a class="header-anchor" href="#a-few-classic-examples" aria-label="Permalink to &quot;A Few Classic Examples&quot;">​</a></h3><h4 id="custom-memory-allocators" tabindex="-1">Custom Memory Allocators <a class="header-anchor" href="#custom-memory-allocators" aria-label="Permalink to &quot;Custom Memory Allocators&quot;">​</a></h4><p>Many high-performance services (like Redis, MySQL) link <code>jemalloc</code> or <code>tcmalloc</code>.</p><ul><li><strong>Phenomenon</strong>: These libraries define symbols like <code>malloc</code>, <code>free</code>, and <code>realloc</code> that are identical to those in Glibc.</li><li><strong>Mechanism</strong>: Because they are explicitly linked or preloaded, their symbols rank ahead of Glibc&#39;s in the global table.</li><li><strong>Result</strong>: All memory allocations for the entire process (including other third-party libraries that depend on Glibc) are automatically forwarded to <code>jemalloc</code>. This is a benign, intentional symbol conflict.</li></ul><h4 id="c-stl-version-conflicts" tabindex="-1">C++ STL Version Conflicts <a class="header-anchor" href="#c-stl-version-conflicts" aria-label="Permalink to &quot;C++ STL Version Conflicts&quot;">​</a></h4><p>This is a malicious case.</p><ul><li><strong>Scenario</strong>: The main program is compiled with GCC 4.8 and depends on <code>libStdOld.so</code>; a plugin is compiled with GCC 9.0 and depends on <code>libStdNew.so</code>.</li><li><strong>Problem</strong>: The internal implementations of <code>std::string</code> or <code>std::vector</code> might differ between versions, but their symbol names (Mangled Names) might remain consistent through partial compatibility, or a conflict might occur.</li><li><strong>Consequence</strong>: When objects are passed across libraries, because the memory layouts differ but the symbols are the same, the program might exhibit undefined behavior (UB), typically manifesting as inexplicable segfaults.</li></ul><hr><h4 id="linking-does-not-provide-any-kind-of-namespace-inheritance-tip-no-namespace-inheritance" tabindex="-1">Linking Does Not Provide Any Kind of Namespace Inheritance (Tip: No Namespace Inheritance) <a class="header-anchor" href="#linking-does-not-provide-any-kind-of-namespace-inheritance-tip-no-namespace-inheritance" aria-label="Permalink to &quot;Linking Does Not Provide Any Kind of Namespace Inheritance (Tip: No Namespace Inheritance)&quot;">​</a></h4><p>This point bears repeating! Many people think: &quot;If I put a function in a <code>namespace MyLib { ... }</code> in my C++ code, or if I compile my code into a <code>libMyLib.so</code>, then this library is like an independent container, and the variable name <code>count</code> inside it won&#39;t conflict with the outside.&quot;</p><p>But in reality, <strong>the linker is &quot;type-blind&quot; and &quot;structure-blind.&quot;</strong> We all know that <strong>C++ namespaces are just syntactic sugar:</strong> the compiler uses <strong>name mangling</strong> to turn <code>MyLib::foo()</code> into the string <code>_ZN5MyLib3fooEv</code>. To the linker, this is just a long string. If two libraries happen to generate the same mangled name, a conflict will still occur. And <strong>shared libraries are not namespaces:</strong> shared libraries are merely a file organization format. Once loaded into process memory, all exported symbols enter a flat, global symbol pool (Global Symbol Table). The global variable <code>g_context</code> in <code>libA.so</code> and <code>g_context</code> in <code>libB.so</code> are the exact same thing in the linker&#39;s eyes, unless you use visibility hiding or local binding.</p>`,92)])])}const u=i(n,[["render",t]]);export{m as __pageData,u as default};
