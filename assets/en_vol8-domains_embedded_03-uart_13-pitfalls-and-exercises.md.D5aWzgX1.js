import{_ as t,o as i,c as a,a2 as o}from"./chunks/framework.BuBU3QrY.js";const h=JSON.parse('{"title":"Part 43: Common Pitfalls and Practical Exercises — Getting Creative with UART","description":"","frontmatter":{"title":"Part 43: Common Pitfalls and Practical Exercises — Getting Creative with UART","description":"","tags":["cpp-modern","intermediate","stm32f1"],"difficulty":"intermediate","platform":"stm32f1","chapter":17,"order":13,"translation":{"source":"documents/vol8-domains/embedded/03-uart/13-pitfalls-and-exercises.md","source_hash":"703f55d71c3658a109167fe2bcc8253f3cd6caebfb5b1c27ce00f0f3399c6bb0","translated_at":"2026-05-26T12:18:34.119874+00:00","engine":"anthropic","token_count":1131}},"headers":[],"relativePath":"en/vol8-domains/embedded/03-uart/13-pitfalls-and-exercises.md","filePath":"en/vol8-domains/embedded/03-uart/13-pitfalls-and-exercises.md","lastUpdated":null}'),n={name:"en/vol8-domains/embedded/03-uart/13-pitfalls-and-exercises.md"};function r(s,e,l,d,c,p){return i(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="part-43-common-pitfalls-and-hands-on-exercises-—-mastering-uart" tabindex="-1">Part 43: Common Pitfalls and Hands-on Exercises — Mastering UART <a class="header-anchor" href="#part-43-common-pitfalls-and-hands-on-exercises-—-mastering-uart" aria-label="Permalink to &quot;Part 43: Common Pitfalls and Hands-on Exercises — Mastering UART&quot;">​</a></h1><blockquote><p>The final article in the UART tutorial. Pitfall avoidance + three exercises to help you truly make the learned knowledge your own.</p></blockquote><hr><h2 id="common-pitfalls" tabindex="-1">Common Pitfalls <a class="header-anchor" href="#common-pitfalls" aria-label="Permalink to &quot;Common Pitfalls&quot;">​</a></h2><h3 id="pitfall-1-tx-rx-crossover-wiring" tabindex="-1">Pitfall 1: TX/RX Crossover Wiring <a class="header-anchor" href="#pitfall-1-tx-rx-crossover-wiring" aria-label="Permalink to &quot;Pitfall 1: TX/RX Crossover Wiring&quot;">​</a></h3><p>This is the number one issue in UART debugging, bar none.</p><p><strong>Symptom</strong>: The terminal receives nothing, or doesn&#39;t receive the data being sent.</p><p><strong>Cause</strong>: Connecting the adapter&#39;s TX to the Blue Pill&#39;s TX (PA9), and the adapter&#39;s RX to the Blue Pill&#39;s RX (PA10). TX to TX means both sides are transmitting and nobody is listening — of course nothing is received.</p><p><strong>Fix</strong>: Remember &quot;crossover wiring&quot; — adapter TX to Blue Pill RX (PA10), adapter RX to Blue Pill TX (PA9). If you aren&#39;t sure which wire is TX and which is RX, swap them and try — it won&#39;t burn anything, it just won&#39;t work.</p><h3 id="pitfall-2-baud-rate-mismatch" tabindex="-1">Pitfall 2: Baud Rate Mismatch <a class="header-anchor" href="#pitfall-2-baud-rate-mismatch" aria-label="Permalink to &quot;Pitfall 2: Baud Rate Mismatch&quot;">​</a></h3><p><strong>Symptom</strong>: The terminal displays garbled text — looks like random characters.</p><p><strong>Cause</strong>: The baud rate set in the code doesn&#39;t match the terminal software&#39;s baud rate. For example, the code uses 115200, but the terminal is set to 9600. UART is an asynchronous protocol; both sides must operate at the exact same rate, otherwise all sampling points will be misaligned and the read data will be completely wrong.</p><p><strong>Fix</strong>: Confirm that the <code>UartConfig{.baud_rate = ...}</code> in the code exactly matches the terminal software&#39;s baud rate setting. It&#39;s not just the baud rate — data bits, parity bits, and stop bits must also match (standard configuration is 8N1).</p><h3 id="pitfall-3-ring-buffer-overflow" tabindex="-1">Pitfall 3: Ring Buffer Overflow <a class="header-anchor" href="#pitfall-3-ring-buffer-overflow" aria-label="Permalink to &quot;Pitfall 3: Ring Buffer Overflow&quot;">​</a></h3><p><strong>Symptom</strong>: The second half of a long string is lost during transmission, or command parsing occasionally fails.</p><p><strong>Cause</strong>: The ISR pushes bytes faster than the main loop pops them. Once the 128-byte buffer is full, <code>push()</code> returns false, and bytes are dropped. This happens when the PC rapidly sends a large amount of data (like pasting a long block of text), while the main loop is busy handling other things (like button debounce or sending a response).</p><p><strong>Fix</strong>: Increasing the buffer size is the most direct approach — change <code>CircularBuffer&lt;128&gt;</code> to <code>CircularBuffer&lt;256&gt;</code> or <code>CircularBuffer&lt;512&gt;</code>. Additionally, ensure there are no long-blocking operations in the main loop — each loop iteration should process all pending data as quickly as possible.</p><h3 id="pitfall-4-forgetting-volatile-on-the-ring-buffer" tabindex="-1">Pitfall 4: Forgetting volatile on the Ring Buffer <a class="header-anchor" href="#pitfall-4-forgetting-volatile-on-the-ring-buffer" aria-label="Permalink to &quot;Pitfall 4: Forgetting volatile on the Ring Buffer&quot;">​</a></h3><p><strong>Symptom</strong>: Seems to work fine, but occasionally loses data. Becomes more frequent when increasing the optimization level (<code>-O2</code>).</p><p><strong>Cause</strong>: The <code>head_</code> and <code>tail_</code> of the <code>CircularBuffer</code> are not declared as <code>volatile</code>. During compiler optimization, the <code>head_</code> read in the main loop gets cached into a register, and subsequent loops no longer re-read from memory — the ISR&#39;s push operation becomes invisible to the main loop.</p><p><strong>Fix</strong>: Ensure that <code>head_</code> and <code>tail_</code> are declared as <code>volatile size_t</code>. Our code already correctly uses <code>volatile</code> — but if you write your own ring buffer, don&#39;t forget this point.</p><h3 id="pitfall-5-printf-floating-point-vs-nano-specs" tabindex="-1">Pitfall 5: printf Floating-Point vs nano.specs <a class="header-anchor" href="#pitfall-5-printf-floating-point-vs-nano-specs" aria-label="Permalink to &quot;Pitfall 5: printf Floating-Point vs nano.specs&quot;">​</a></h3><p><strong>Symptom</strong>: <code>printf(&quot;%f&quot;, 3.14)</code> outputs garbled text or nothing at all.</p><p><strong>Cause</strong>: Our CMakeLists.txt uses the <code>-specs=nano.specs</code> linker flag, which links against the streamlined C library (nano newlib). The streamlined version does not support floating-point printf formatting — format specifiers like <code>%f</code> and <code>%g</code> do not work.</p><p><strong>Fix</strong>: Use integers to simulate floating-point output: <code>printf(&quot;%d.%02d&quot;, (int)(value * 100) / 100, (int)(value * 100) % 100)</code>. Alternatively, if Flash space is sufficient, remove <code>-specs=nano.specs</code> to link the full C library (Flash usage will increase by about 10-20 KB).</p><h3 id="pitfall-6-forgetting-to-restart-reception-in-the-callback" tabindex="-1">Pitfall 6: Forgetting to Restart Reception in the Callback <a class="header-anchor" href="#pitfall-6-forgetting-to-restart-reception-in-the-callback" aria-label="Permalink to &quot;Pitfall 6: Forgetting to Restart Reception in the Callback&quot;">​</a></h3><p><strong>Symptom</strong>: The first byte is received, but no further data is ever received.</p><p><strong>Cause</strong>: Forgetting to call <code>restart_receive()</code> in the <code>HAL_UART_RxCpltCallback()</code>. HAL does not automatically start the next round after completing a single-byte reception — you must manually call <code>HAL_UART_Receive_IT()</code> to re-enable reception. If you forget, RXNEIE is not re-enabled, and the next arriving byte will not trigger an interrupt.</p><p><strong>Fix</strong>: Ensure the last line in the callback is <code>restart_receive()</code>. This is the easiest step to miss in interrupt-driven reception — it doesn&#39;t throw errors or crash, it just &quot;silently fails.&quot;</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-add-a-status-command-easy" tabindex="-1">Exercise 1: Add a STATUS Command (Easy) <a class="header-anchor" href="#exercise-1-add-a-status-command-easy" aria-label="Permalink to &quot;Exercise 1: Add a STATUS Command (Easy)&quot;">​</a></h3><p>Add a new command <code>STATUS</code> in <code>handle_command()</code> that returns the current LED state (ON or OFF).</p><p>Hint: You need a way to track the LED&#39;s current state. The simplest method is to use a <code>bool</code> variable, updating it each time <code>led.on()</code> or <code>led.off()</code> is called. Alternatively, you could read the actual logic level of PC13 — but note that PC13 is active-low (the Blue Pill&#39;s onboard LED is active-low).</p><p>Goal: Type &quot;STATUS&quot; in the terminal, and the chip returns &quot;LED is ON&quot; or &quot;LED is OFF&quot;. Understand how to extend the existing command processing framework.</p><h3 id="exercise-2-echo-mode-toggle-medium" tabindex="-1">Exercise 2: ECHO Mode Toggle (Medium) <a class="header-anchor" href="#exercise-2-echo-mode-toggle-medium" aria-label="Permalink to &quot;Exercise 2: ECHO Mode Toggle (Medium)&quot;">​</a></h3><p>Implement an ECHO mode: when enabled, every received byte is immediately sent back as-is. Add &quot;ECHO ON&quot; and &quot;ECHO OFF&quot; commands to toggle the mode.</p><p>Hint: In the UART reception section of the main loop, add an <code>bool echo_mode = false</code> flag. When <code>echo_mode</code> is true, immediately <code>send_string()</code> each popped byte back. Note: the echo should happen before line parsing — after a byte is popped, echo it first, then append it to the line buffer.</p><p>Goal: After typing &quot;ECHO ON&quot;, every character you type in the terminal will be echoed (you can see what you are typing). After typing &quot;ECHO OFF&quot;, echoing stops. Understand how to add real-time response logic within the interrupt-reception + main-loop-consumption framework.</p><h3 id="exercise-3-interrupt-driven-transmission-transmit-ring-buffer-challenge" tabindex="-1">Exercise 3: Interrupt-Driven Transmission + Transmit Ring Buffer (Challenge) <a class="header-anchor" href="#exercise-3-interrupt-driven-transmission-transmit-ring-buffer-challenge" aria-label="Permalink to &quot;Exercise 3: Interrupt-Driven Transmission + Transmit Ring Buffer (Challenge)&quot;">​</a></h3><p>In our code, reception is interrupt-driven, but transmission is still blocking. This exercise requires you to implement interrupt-driven transmission.</p><p>Hint: You will need:</p><ol><li>A transmit-direction ring buffer (<code>CircularBuffer&lt;256&gt; tx_ring</code>)</li><li>In the main loop, push to <code>tx_ring</code> instead of directly calling <code>HAL_UART_Transmit</code> when data needs to be sent</li><li>Start interrupt transmission: <code>HAL_UART_Transmit_IT(&amp;huart, &amp;byte, 1)</code></li><li>In the <code>HAL_UART_TxCpltCallback()</code>, check if <code>tx_ring</code> still has data — if yes, keep sending; if no, stop</li><li>Pay attention to TXEIE (Transmit Interrupt Enable) management — only enable it when there is pending data, and disable it when done</li></ol><p>The challenge of this exercise lies in the fact that transmission is &quot;started on demand&quot; — unlike reception, which runs continuously. You need to handle edge cases like &quot;how to stop the interrupt when the ring buffer is empty&quot; and &quot;how to start sending the first byte.&quot;</p><p>Goal: Understand the symmetry between interrupt-driven transmission and reception, and master the complete interrupt-driven UART architecture with dual ring buffers.</p><hr><h2 id="uart-tutorial-recap" tabindex="-1">UART Tutorial Recap <a class="header-anchor" href="#uart-tutorial-recap" aria-label="Permalink to &quot;UART Tutorial Recap&quot;">​</a></h2><p>We have completed 13 articles. Let&#39;s review our learning path:</p><p><strong>Phase 1: Motivation (Part 31)</strong></p><ul><li>Derived communication requirements from LED (output) and Button (input)</li><li>What UART is, and why we chose it</li><li>Final result preview and hardware preparation</li></ul><p><strong>Phase 2: Hardware Fundamentals (Parts 32-33)</strong></p><ul><li>UART protocol details: start bit, data bits, parity bit, stop bit, baud rate, oversampling</li><li>STM32 USART peripheral: three instances, key registers, GPIO alternate functions, NVIC preview</li></ul><p><strong>Phase 3: HAL + Blocking I/O (Parts 34-35)</strong></p><ul><li>HAL initialization and blocking transmission</li><li>printf redirection, the fatal problem with blocking reception</li></ul><p><strong>Phase 4: Interrupt-Driven (Parts 36-38)</strong></p><ul><li>Cortex-M3 interrupt mechanism and NVIC</li><li>Lock-free SPSC ring buffer</li><li>UART IRQ handling and callback chain</li></ul><p><strong>Phase 5: C++ Abstraction (Parts 39-42)</strong></p><ul><li><code>std::expected</code> error handling</li><li>UART driver template: zero-size abstraction, <code>if constexpr</code>, <code>static inline</code></li><li>Concepts constraints + UartManager</li><li>Command processor and complete code walkthrough</li></ul><p><strong>Phase 6: Summary (Part 43)</strong></p><ul><li>6 common pitfalls and 3 progressive exercises</li></ul><p>Summary of C++ features used:</p><ul><li><code>std::expected&lt;T, E&gt;</code> (C++23) — type-safe error handling</li><li><code>std::span</code> (C++20) — safe contiguous memory view</li><li><code>std::string_view</code> (C++17) — zero-copy string view</li><li><code>consteval</code> (C++20) — compile-time baud rate validation</li><li>Concepts (C++20) — constraining callback signatures</li><li><code>static inline</code> members (C++17) — template singletons</li><li><code>if constexpr</code> (C++17) — compile-time hardware dispatch</li><li><code>enum class : uintptr_t</code> — base address encoding</li><li><code>volatile</code> — ISR visibility guarantees</li><li><code>extern &quot;C&quot;</code> — ISR and printf bridging</li><li><code>[[maybe_unused]]</code> (C++17) — suppressing unused parameter warnings</li><li>Designated initializer (C++20) — <code>UartConfig{.baud_rate = 115200}</code></li></ul><p>Every feature solved a real problem in the specific context of the UART driver. From error handling to type constraints, from compile-time dispatch to ISR bridging — modern C++ in the embedded domain is not &quot;just for show&quot;; it genuinely makes code safer, more maintainable, and more efficient.</p><p>With this, the UART tutorial is complete. We covered everything from protocol principles to interrupt-driven design, from C-style HAL calls to C++23 templates and Concepts. Your STM32 can now not only light up LEDs and read buttons on its own, but also communicate bidirectionally with a PC — this is a qualitative leap. Moving forward, whether you build SPI sensor drivers, read EEPROMs via I2C, or put together a complete embedded web server, UART communication will remain your foundational tool for debugging and verification.</p>',64)])])}const m=t(n,[["render",r]]);export{h as __pageData,m as default};
