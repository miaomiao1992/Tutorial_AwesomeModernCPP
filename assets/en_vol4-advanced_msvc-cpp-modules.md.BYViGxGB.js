import{_ as t,o,c as i,ag as n}from"./chunks/framework.Cu2CLAe3.js";const u=JSON.parse('{"title":"Understanding MSVC C++ Modules in One Article: Principles, Motivations, and Engineering Practices","description":"","frontmatter":{"title":"Understanding MSVC C++ Modules in One Article: Principles, Motivations, and Engineering Practices","description":"","tags":["cpp-modern","host","intermediate"],"difficulty":"intermediate","platform":"host","chapter":11,"order":8},"headers":[],"relativePath":"en/vol4-advanced/msvc-cpp-modules.md","filePath":"en/vol4-advanced/msvc-cpp-modules.md","lastUpdated":null}'),a={name:"en/vol4-advanced/msvc-cpp-modules.md"};function s(r,e,l,d,c,h){return o(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="understanding-msvc-c-modules-principles-motivation-and-engineering-practice" tabindex="-1">Understanding MSVC C++ Modules: Principles, Motivation, and Engineering Practice <a class="header-anchor" href="#understanding-msvc-c-modules-principles-motivation-and-engineering-practice" aria-label="Permalink to &quot;Understanding MSVC C++ Modules: Principles, Motivation, and Engineering Practice&quot;">​</a></h1><p>A quick pointer: if you don&#39;t already know how to use modules on MSVC, I would seriously recommend that you try them first before drawing any conclusions.</p><ul><li><a href="https://blog.csdn.net/charlie114514191/article/details/155929743" target="_blank" rel="noreferrer">How to quickly use C++ modules on VS2026 — A complete hands-on guide - CSDN Blog</a></li><li><a href="https://zhuanlan.zhihu.com/p/1983806788118783552" target="_blank" rel="noreferrer">How to quickly use C++ modules on VS2026 — A complete hands-on guide - Article by 老老老陈醋 - Zhihu</a></li><li><a href="https://awesome-embedded-learning-studio.github.io/Tutorial_AwesomeModernCPP/%E7%8E%AF%E5%A2%83%E9%85%8D%E7%BD%AE/%E5%A6%82%E4%BD%95%E5%BF%AB%E9%80%9F%E5%9C%A8VS2026%E4%B8%8A%E4%BD%BF%E7%94%A8C%2B%2B%E6%A8%A1%E5%9D%97%E2%80%94%E5%AE%8C%E6%95%B4%E4%B8%8A%E6%89%8B%E6%8C%87%E5%8D%97/" target="_blank" rel="noreferrer">How to quickly use C++ modules on VS2026 — A complete hands-on guide - Tutorial_AwesomeModernCPP Documentation</a></li></ul><hr><h2 id="why-do-we-need-modules-—-starting-with-the-fundamental-flaws-of-include" tabindex="-1">Why Do We Need Modules? — Starting with the Fundamental Flaws of <code>#include</code> <a class="header-anchor" href="#why-do-we-need-modules-—-starting-with-the-fundamental-flaws-of-include" aria-label="Permalink to &quot;Why Do We Need Modules? — Starting with the Fundamental Flaws of `#include`&quot;">​</a></h2><p>For a very long time, C++ only really had one &quot;module system&quot;:</p><div class="language-cpp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;foo.h&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>I believe everyone knows how <code>#include</code> works, so I won&#39;t spell it out—it&#39;s purely text replacement. This dependency mechanism based on <code>#include</code> sometimes feels more like something that was discovered rather than deliberately designed (given the history of the C language).</p><p>When the compiler sees <code>#include &lt;vector&gt;</code>, <strong>it doesn&#39;t think you are &quot;depending on a library&quot;</strong>. Instead, it takes the contents of the <code>&lt;vector&gt;</code> header file, <strong>copies them verbatim into the current <code>.cpp</code></strong>, and continues compiling.</p><p>This might sound harmless, but I believe anyone doing real engineering has experienced these issues:</p><h4 id="problem-1-compilation-speed-disaster-exponential-amplification" tabindex="-1">Problem 1: Compilation Speed Disaster (Exponential Amplification) <a class="header-anchor" href="#problem-1-compilation-speed-disaster-exponential-amplification" aria-label="Permalink to &quot;Problem 1: Compilation Speed Disaster (Exponential Amplification)&quot;">​</a></h4><p>The core issue with the header file mechanism is <strong>repeated parsing</strong>. Every <code>.cpp</code> file needs to re-parse all the headers it <code>#include</code>, such as <code>&lt;vector&gt;</code>, <code>&lt;string&gt;</code>, and <code>&lt;iostream&gt;</code>. When dealing with <strong>templates, macros, and conditional compilation</strong>, this repeated work becomes a performance nightmare, causing compilation time to grow exponentially.</p><p><strong>Precompiled headers (PCH)</strong> merely <strong>cache</strong> the parsing results; they do not fundamentally address the <strong>structural flaw</strong> of repeated parsing. Essentially, this is because the <strong>compiler doesn&#39;t know which declarations are &quot;already-processed module interfaces&quot;</strong>, so it blindly processes them over and over again.</p><h4 id="problem-2-uncontrollable-macro-pollution" tabindex="-1">Problem 2: Uncontrollable Macro Pollution <a class="header-anchor" href="#problem-2-uncontrollable-macro-pollution" aria-label="Permalink to &quot;Problem 2: Uncontrollable Macro Pollution&quot;">​</a></h4><p><strong>Macros are unscoped</strong>, which is the root cause of uncontrollable macro pollution. Once a macro like <code>#define min(a,b) ...</code> is defined and introduced via <code>#include</code>, it <strong>permanently pollutes all subsequent code</strong> until the end of the file or until it is hit by <code>#undef</code>. (This is why you&#39;ll see some projects habitually <code>#undef</code> their defined macros—you don&#39;t want a macro you defined to blow up because someone else messed up the include order, right! For example, including a library like <code>&lt;windows.h&gt;</code> might introduce a massive number of macros that could accidentally replace functions or variables with the same names in your code. The compiler <strong>cannot prevent or isolate</strong> this global macro pollution.</p><h4 id="problem-3-tight-coupling-of-interface-and-implementation-transitive-dependencies" tabindex="-1">Problem 3: Tight Coupling of Interface and Implementation (Transitive Dependencies) <a class="header-anchor" href="#problem-3-tight-coupling-of-interface-and-implementation-transitive-dependencies" aria-label="Permalink to &quot;Problem 3: Tight Coupling of Interface and Implementation (Transitive Dependencies)&quot;">​</a></h4><p>The header file mechanism forces the exposure of unnecessary implementation details in the interface (<code>.h</code> files). For example, even if a class <code>Foo</code> only uses <code>std::vector&lt;int&gt;</code> internally:</p><div class="language-c++ vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c++</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// foo.h</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // &lt;-- 不必要的暴露</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;int&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>You simply want to use the <code>Foo</code> class, but you are forced to bring in <strong>all of <code>&lt;vector&gt;</code>&#39;s dependencies</strong> via <code>#include &quot;foo.h&quot;</code>. This is known as <strong>transitive includes</strong>: users are forced to depend on all the headers required by the underlying implementation details of the interface, leading to a network-like explosion of compilation dependencies.</p><h4 id="problem-4-too-many-implicit-rules-odr-abi-and-more" tabindex="-1">Problem 4: Too Many Implicit Rules: ODR, ABI, and More <a class="header-anchor" href="#problem-4-too-many-implicit-rules-odr-abi-and-more" aria-label="Permalink to &quot;Problem 4: Too Many Implicit Rules: ODR, ABI, and More&quot;">​</a></h4><p>The header file mechanism brings a series of complex and implicit rules, such as <code>inline</code>, template definitions, <code>static</code> variables, and implementing functions inside headers. The most dangerous of these is the <strong>one definition rule (ODR)</strong>. ODR violations often pass the compilation stage (because each translation unit only sees one definition), but they <strong>only surface during the linking stage</strong>, resulting in hard-to-debug &quot;linker errors&quot; that greatly increase code fragility.</p><hr><h2 id="the-core-idea-of-c-modules-making-the-compiler-truly-understand-modules" tabindex="-1">The Core Idea of C++ Modules: <strong>Making the Compiler Truly &quot;Understand Modules&quot;</strong> <a class="header-anchor" href="#the-core-idea-of-c-modules-making-the-compiler-truly-understand-modules" aria-label="Permalink to &quot;The Core Idea of C++ Modules: **Making the Compiler Truly &quot;Understand Modules&quot;**&quot;">​</a></h2><p>So, being the smart developer you are, you know that since these problems exist, modules are here to solve them! (Although I must complain that using modules in my current project feels like a mixed bag, so I&#39;m still experimenting). Simply put: <strong>Modules = compiler-understandable, cacheable, and isolatable interface units</strong></p><h4 id="the-import-keyword-include" tabindex="-1">The <code>import</code> Keyword ≠ <code>#include</code> <a class="header-anchor" href="#the-import-keyword-include" aria-label="Permalink to &quot;The `import` Keyword ≠ `#include`&quot;">​</a></h4><p><code>import std;</code> simply imports the current standard library modules into our code. It tells our MSVC compiler: &quot;Please import the <strong>compiled interface information of the <code>std</code> module</strong> into the current translation unit.&quot;</p><h4 id="the-smallest-unit-of-a-module-bmis-binary-module-interface" tabindex="-1">The Smallest Unit of a Module: BMIs (Binary Module Interface) <a class="header-anchor" href="#the-smallest-unit-of-a-module-bmis-binary-module-interface" aria-label="Permalink to &quot;The Smallest Unit of a Module: BMIs (Binary Module Interface)&quot;">​</a></h4><p>In MSVC, each module interface unit is compiled into a <strong><code>.ifc</code> file</strong>. This is an intermediate artifact of the module, designed to easily integrate into existing build systems. It stores the serialized results of the frontend AST—structured descriptions of types, functions, and templates (honestly, my first reaction was &quot;a C++ version of a <code>.class</code> file (Java)&quot;).</p><h4 id="workflow-differences" tabindex="-1">Workflow Differences <a class="header-anchor" href="#workflow-differences" aria-label="Permalink to &quot;Workflow Differences&quot;">​</a></h4><p>Previously, header file processing relied on the preprocessor, directly pasting headers into source files to form a single compilation unit. Now, modules handle this much better: the module is compiled only once, and when you use it, the <code>.ifc</code> file is loaded directly, significantly cutting down compilation time. Design characteristics of MSVC Modules (very practical)</p><h2 id="what-exactly-happens-with-import-std" tabindex="-1">What Exactly Happens with <code>import std;</code>? <a class="header-anchor" href="#what-exactly-happens-with-import-std" aria-label="Permalink to &quot;What Exactly Happens with `import std;`?&quot;">​</a></h2><p>When you write <code>import std;</code>, MSVC will:</p><ol><li><p>Look up the standard library module <code>std</code></p></li><li><p>Load its <code>.ifc</code> file (pre-compiled officially by the STL)</p></li><li><p>Inject all exported symbols into the current TU</p></li><li><p><strong>Not introduce any macros</strong> (this is extremely important). This is also why the <code>min/max</code> macro issue naturally disappears in the world of Modules.</p><p>Note that modules <strong>do not export macros by default</strong>. Macros do not propagate across <code>import</code> boundaries, so the macros you write cannot leak into dependent files.</p></li></ol><hr><h2 id="when-should-we-use-msvc-modules-today" tabindex="-1">When Should We Use MSVC Modules Today? <a class="header-anchor" href="#when-should-we-use-msvc-modules-today" aria-label="Permalink to &quot;When Should We Use MSVC Modules Today?&quot;">​</a></h2><p>As mentioned above, C++ Modules is a structural solution to the traditional header file mechanism. However, when applying it to production environments—especially under MSVC (Visual Studio)—we need to use it strategically.</p><h4 id="strongly-recommended-use-cases" tabindex="-1">Strongly Recommended Use Cases <a class="header-anchor" href="#strongly-recommended-use-cases" aria-label="Permalink to &quot;Strongly Recommended Use Cases&quot;">​</a></h4><h4 id="_1-using-import-std-to-replace-standard-library-headers" tabindex="-1">1. Using <code>import std;</code> to Replace Standard Library Headers <a class="header-anchor" href="#_1-using-import-std-to-replace-standard-library-headers" aria-label="Permalink to &quot;1. Using `import std;` to Replace Standard Library Headers&quot;">​</a></h4><p>This is currently the safest and most valuable use case for Modules. We have now thoroughly solved the <strong>compilation speed disaster</strong> and <strong>macro pollution</strong> issues caused by standard library headers (like <code>&lt;vector&gt;</code>, <code>&lt;string&gt;</code>, <code>&lt;iostream&gt;</code>).</p><p>Moreover, with just one <code>import std;</code>, we no longer need to painstakingly write a bunch of includes. The compiler only needs to process the pre-compiled Standard Library Module interface once, greatly boosting compilation speed. Internal macros from the standard library also won&#39;t pollute your code.</p><h4 id="_2-modularization-within-new-projects-business-module-isolation" tabindex="-1">2. Modularization Within New Projects (Business Module Isolation) <a class="header-anchor" href="#_2-modularization-within-new-projects-business-module-isolation" aria-label="Permalink to &quot;2. Modularization Within New Projects (Business Module Isolation)&quot;">​</a></h4><p>For newly created projects primarily targeting the Windows platform or for internal use, consider dividing the internal business logic into independent Modules. User code only needs to <code>import MyModule;</code>, without being forced to <code>#include</code> all the headers depended upon internally by the module. In terms of syntax, business logic is organized into <code>.ixx</code> or <code>.cppm</code> module interface files, and <code>export</code> only exposes the necessary interfaces. <strong>The interface and implementation are thoroughly decoupled</strong>. When changing internal implementation details and private dependencies of a module, user code depending on that module <strong>does not need to be recompiled</strong> (unless the interface itself changes).</p><h4 id="use-with-caution" tabindex="-1">Use With Caution <a class="header-anchor" href="#use-with-caution" aria-label="Permalink to &quot;Use With Caution&quot;">​</a></h4><h4 id="_1-public-interfaces-of-large-cross-platform-libraries" tabindex="-1">1. Public Interfaces of Large Cross-Platform Libraries <a class="header-anchor" href="#_1-public-interfaces-of-large-cross-platform-libraries" aria-label="Permalink to &quot;1. Public Interfaces of Large Cross-Platform Libraries&quot;">​</a></h4><p>If what we are doing is: developing a <strong>public/open-source library</strong> that needs to be stably used by multiple compilers (such as MSVC, GCC, and Clang), please be cautious about using Modules for its public API. After all, this feature hasn&#39;t been around for many years, and the Modules <strong>implementations across mainstream compilers still have differences</strong> and potential bugs. As a library ready for distribution, it seems it would still bring additional configuration complexity for the library&#39;s users.</p><h4 id="_2-projects-requiring-completely-consistent-behavior-across-gcc-clang" tabindex="-1">2. Projects Requiring Completely Consistent Behavior Across GCC / Clang <a class="header-anchor" href="#_2-projects-requiring-completely-consistent-behavior-across-gcc-clang" aria-label="Permalink to &quot;2. Projects Requiring Completely Consistent Behavior Across GCC / Clang&quot;">​</a></h4><p>If your project needs to achieve <strong>completely consistent and highly stable</strong> behavior across different platforms and compilers (such as embedded systems, high-integrity financial applications), potential implementation differences in Modules could introduce risks. After all, the semantics of Modules (especially in complex scenarios involving <strong>import order, linking, and ODR</strong>) might have subtle differences across compilers.</p><p>On this matter, conservatively relying on traditional header files is currently the best way to guarantee cross-platform behavioral consistency, because it relies on the <code>#include</code> preprocessing semantics that have been mature for decades.</p><table tabindex="0"><thead><tr><th><strong>Scenario</strong></th><th><strong>Recommendation Level</strong></th><th><strong>Reason / Value</strong></th></tr></thead><tbody><tr><td><strong>Using <code>import std;</code></strong></td><td><strong>✅ Strongly Recommended</strong></td><td>Solves standard library compilation speed and macro pollution issues; high value, extremely low risk.</td></tr><tr><td><strong>New projects / internal business modularization</strong></td><td><strong>✅ Recommended</strong></td><td>Eliminates transitive dependencies, decouples interface from implementation, improves internal compilation efficiency.</td></tr><tr><td><strong>Public / cross-platform library APIs</strong></td><td><strong>⚠️ Use With Caution</strong></td><td>Cross-compiler implementation differences and toolchain maturity issues may affect compatibility.</td></tr><tr><td><strong>Extremely strict behavioral consistency requirements</strong></td><td><strong>⚠️ Use With Caution</strong></td><td>Avoids unpredictable behavior caused by potential compiler implementation differences.</td></tr></tbody></table>',49)])])}const m=t(a,[["render",s]]);export{u as __pageData,m as default};
